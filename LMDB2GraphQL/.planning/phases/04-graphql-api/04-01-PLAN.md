---
phase: 04-graphql-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/config.rs
  - src/lib.rs
  - src/graphql/mod.rs
  - src/graphql/types.rs
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05]
must_haves:
  truths:
    - "The four GraphQL crates (async-graphql 7.2.1, async-graphql-axum 7.2.1, axum 0.8.9, tokio 1.52.3) resolve and the crate compiles"
    - "Config exposes a bind_address field defaulting to 0.0.0.0:8080 when omitted from YAML"
    - "The GraphQL output types Event, EventsPage, AuthorGroup, StatsResult exist with camelCase schema field names"
    - "The GraphQL input types EventFilterInput and TagFilterInput exist (ids/authors/kinds/since/until + single optional tag)"
    - "A decoded_event_to_gql mapping converts DecodedEvent to Event using raw_json passthrough (never re-serializing NostrEvent)"
  artifacts:
    - path: "src/graphql/types.rs"
      provides: "Event/EventsPage/AuthorGroup/StatsResult SimpleObjects + EventFilterInput/TagFilterInput InputObjects + decoded_event_to_gql"
      contains: "SimpleObject"
    - path: "src/graphql/mod.rs"
      provides: "graphql module declaration (types, schema, resolvers submodules)"
      contains: "pub mod types"
    - path: "src/config.rs"
      provides: "bind_address config field with 0.0.0.0:8080 default"
      contains: "bind_address"
    - path: "Cargo.toml"
      provides: "async-graphql, async-graphql-axum, axum, tokio dependencies"
      contains: "async-graphql"
  key_links:
    - from: "src/graphql/types.rs"
      to: "src/lmdb/types.rs (DecodedEvent)"
      via: "decoded_event_to_gql mapping fn"
      pattern: "DecodedEvent"
    - from: "src/lib.rs"
      to: "src/graphql/mod.rs"
      via: "pub mod graphql"
      pattern: "pub mod graphql"
---

<objective>
Lay the Phase-4 GraphQL adapter foundation: add the four locked crates, extend Config
with the `bind_address` field, and define every GraphQL schema TYPE (output objects +
input objects) plus the `DecodedEvent → Event` mapping — WITHOUT any resolver or server
wiring (that is Plan 04-02). These are the contracts Plan 04-02's resolvers consume.

Purpose: Establish stable, compile-checked GraphQL type contracts and the dependency
surface so the resolver/server plan composes against fixed signatures (interface-first
ordering). All work is mechanical mapping; no query logic is reimplemented.

Output: Compiling crate with the GraphQL types module, extended Config, and dependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-graphql-api/04-CONTEXT.md
@.planning/phases/04-graphql-api/04-RESEARCH.md
@.planning/phases/04-graphql-api/04-PATTERNS.md
@src/lmdb/types.rs
@src/config.rs
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GraphQL crate dependencies (legitimacy-audited)</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (current dependency table — heed 0.22.1, tracing, tracing-subscriber already present)
    - .planning/phases/04-graphql-api/04-RESEARCH.md "Package Legitimacy Audit" table (all four packages verdict OK/Approved, verified against crates.io 2026-06-13) and "Installation (additions to Cargo.toml)" block
  </read_first>
  <action>
    Add exactly these four dependencies to `[dependencies]` in Cargo.toml at the pinned
    versions from RESEARCH.md: `async-graphql = { version = "7.2.1", features = ["tracing"] }`,
    `async-graphql-axum = "7.2.1"`, `axum = "0.8.9"`, and
    `tokio = { version = "1.52.3", features = ["full"] }`. Keep async-graphql and
    async-graphql-axum at the EXACT same minor version (RESEARCH Pitfall 3 — version
    mismatch causes trait-impl conflicts). Do NOT add `rusqlite`/`sqlx` (stale Approach-A
    stack — ignore CLAUDE.md SQLite entries per CONTEXT canonical_refs). All four packages
    are pre-audited as legitimate (OK/Approved, no [ASSUMED]/[SUS]/[SLOP]) in RESEARCH.md's
    Package Legitimacy Audit, so no install checkpoint is required.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spam && cargo build --all-targets 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'async-graphql = ' Cargo.toml` returns 1 with version `7.2.1`
    - `grep 'async-graphql-axum = "7.2.1"' Cargo.toml` matches
    - `grep 'axum = "0.8.9"' Cargo.toml` matches
    - `grep 'tokio = ' Cargo.toml` matches with version `1.52.3` and `features = ["full"]`
    - `cargo build --all-targets` exits 0 (crates resolve and link)
  </acceptance_criteria>
  <done>All four crates resolve at pinned versions and the existing crate still compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add bind_address config field</name>
  <files>src/config.rs</files>
  <read_first>
    - src/config.rs (the `Config` struct lines ~13-29, the `default_map_size` fn pattern lines ~32-36, and the existing tests lines ~64-131 using `tempfile::tempdir()`)
    - .planning/phases/04-graphql-api/04-PATTERNS.md "src/config.rs (config, modify)" section
    - .planning/phases/04-graphql-api/04-RESEARCH.md Open Question 1 (recommendation: `bind_address: String` default `"0.0.0.0:8080"`)
  </read_first>
  <action>
    Add a `pub bind_address: String` field to `Config` with `#[serde(default = "default_bind_address")]`,
    following the existing `map_size` / `default_map_size` pattern (per Claude's-Discretion
    recommendation in RESEARCH Open Question 1 and CONTEXT D-discretion). Add a
    `fn default_bind_address() -> String` returning `"0.0.0.0:8080".to_string()`. Doc-comment
    the field: `/// HTTP bind address for the GraphQL server. Default: 0.0.0.0:8080.`
    Add a test (mirroring the existing `test_map_size_default` / `test_load_from_tempdir`
    style, using `tempfile::tempdir()` — NEVER `~/deepfry/`) asserting `bind_address` defaults
    to `"0.0.0.0:8080"` when omitted from the YAML.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spam && cargo test --all-targets config:: 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub bind_address: String' src/config.rs` returns 1
    - `grep 'default_bind_address' src/config.rs` matches the fn and the serde default attribute
    - `grep '0.0.0.0:8080' src/config.rs` matches
    - The new config default test passes under `cargo test --all-targets config::`
  </acceptance_criteria>
  <done>Config carries `bind_address` defaulting to `0.0.0.0:8080`; default verified by test.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Define GraphQL output + input types and the DecodedEvent mapping</name>
  <files>src/graphql/mod.rs, src/graphql/types.rs, src/lib.rs</files>
  <read_first>
    - src/lmdb/types.rs (NostrEvent fields: id/pubkey/created_at:u64/kind:u64/tags:Vec&lt;Vec&lt;String&gt;&gt;/content/sig; DecodedEvent { event, raw_json: Vec&lt;u8&gt; })
    - src/query/mod.rs (the `pub mod` submodule declaration pattern to mirror in graphql/mod.rs)
    - src/lib.rs (existing `pub mod` declarations to extend)
    - .planning/phases/04-graphql-api/04-PATTERNS.md "src/graphql/types.rs" and "src/graphql/mod.rs" sections
    - .planning/phases/04-graphql-api/04-RESEARCH.md Pattern 6 (output types), "EventFilterInput" + "TagFilterInput" code blocks, "Mapping DecodedEvent to GraphQL Event", and Pitfall 2 (u64→i64) + Pitfall 5 (raw_json from_utf8_lossy)
  </read_first>
  <action>
    Create `src/graphql/mod.rs` declaring `pub mod types;`, `pub mod schema;`, and
    `pub mod resolvers;` (schema/resolvers are stubbed/filled in Plan 04-02; declare them
    now only if it compiles — otherwise declare just `pub mod types;` and let 04-02 add the
    rest). Create `src/graphql/types.rs` with:
    (a) `#[derive(SimpleObject)] pub struct Event` — fields `id: String`, `pubkey: String`,
    `kind: i64`, `created_at: i64`, `content: String`, `sig: String`, `tags: Vec<Vec<String>>`
    (maps to `[[String!]!]` per D-02), `raw: String` (the byte-exact passthrough per D-01).
    Use `i64` for kind/created_at (RESEARCH Pitfall 2 — GraphQL Int is 32-bit).
    (b) `#[derive(SimpleObject)] pub struct EventsPage` — `events: Vec<Event>`,
    `end_cursor: Option<String>`, `has_more: bool` (D-03; SimpleObject auto-renames to
    `endCursor`/`hasMore`).
    (c) `#[derive(SimpleObject)] pub struct AuthorGroup` — `author: String`, `events: Vec<Event>` (D-07).
    (d) `#[derive(SimpleObject)] pub struct StatsResult` — `event_count: i64`, `max_lev_id: i64`,
    `db_version: i32` (D-09; renames to `eventCount`/`maxLevId`/`dbVersion`).
    (e) `#[derive(InputObject, Default)] pub struct EventFilterInput` — `ids: Option<Vec<String>>`,
    `authors: Option<Vec<String>>`, `kinds: Option<Vec<i64>>`, `since: Option<i64>`,
    `until: Option<i64>`, `tag: Option<TagFilterInput>` (single optional tag for v1 per
    RESEARCH Open Question 2; multi-tag is v2).
    (f) `#[derive(InputObject)] pub struct TagFilterInput` — `name: String`, `values: Vec<String>`.
    (g) `pub fn decoded_event_to_gql(d: DecodedEvent) -> Event` — maps fields, casting
    `kind`/`created_at` to `i64`, and sets `raw: String::from_utf8_lossy(&d.raw_json).into_owned()`
    (D-01 / Pitfall 5 — NEVER `serde_json::to_string` the NostrEvent struct; the raw bytes
    are the exact JSON strfry stored).
    Doc-comment every public type citing the D-XX / API-XX it implements (house style).
    Add `pub mod graphql;` to `src/lib.rs` following the existing `pub mod` pattern.
    Do NOT add resolvers, the Query root, AppState, schema builder, or any engine call here —
    those belong to Plan 04-02.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spam && cargo build --all-targets 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SimpleObject' src/graphql/types.rs` returns at least 4 (Event/EventsPage/AuthorGroup/StatsResult)
    - `grep -c 'InputObject' src/graphql/types.rs` returns at least 2 (EventFilterInput/TagFilterInput)
    - `grep 'pub fn decoded_event_to_gql' src/graphql/types.rs` matches
    - `grep -v '^[[:space:]]*//' src/graphql/types.rs | grep -c 'serde_json::to_string'` returns 0 (raw passthrough only, D-01)
    - `grep 'from_utf8_lossy' src/graphql/types.rs` matches (raw field uses byte passthrough)
    - `grep 'pub mod graphql' src/lib.rs` matches
    - `grep 'pub mod types' src/graphql/mod.rs` matches
    - `cargo build --all-targets` exits 0
  </acceptance_criteria>
  <done>All GraphQL types and the DecodedEvent→Event mapping compile; `graphql` module is wired into lib.rs.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-01 portion)

New modules / files:
- `src/graphql/mod.rs` — module root (`pub mod types;` + forward declarations)
- `src/graphql/types.rs` — all GraphQL schema types + mapping fn

GraphQL output types: `Event` (D-01/D-02), `EventsPage` (D-03), `AuthorGroup` (D-07), `StatsResult` (D-09)
GraphQL input types: `EventFilterInput`, `TagFilterInput` (single optional `tag`, D-02 mapping target)
Mapping fn: `decoded_event_to_gql(DecodedEvent) -> Event` (raw_json passthrough, D-01)
Config field: `bind_address: String` (default `0.0.0.0:8080`)
Cargo deps: `async-graphql` 7.2.1, `async-graphql-axum` 7.2.1, `axum` 0.8.9, `tokio` 1.52.3

(Plan 04-02 produces: `Query` root, resolver fns, `AppState`, `AppSchema`/`build_schema`,
`map_query_error`, `build_router`/server wiring, `main.rs` server startup.)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/dependency supply chain | New crates pulled from crates.io cross into the build |
| (resolver/HTTP boundaries) | Deferred to Plan 04-02 (no HTTP surface introduced here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | crates.io installs (async-graphql/-axum/axum/tokio) | mitigate | RESEARCH.md Package Legitimacy Audit verdicts all OK/Approved (verified crates.io 2026-06-13); versions pinned exactly in Cargo.toml; no [ASSUMED]/[SUS]/[SLOP] packages so no blocking human checkpoint required |
| T-04-RAW | Information Disclosure | `decoded_event_to_gql` raw field | accept | `raw` deliberately exposes the byte-exact JSON strfry already serves publicly (D-01); no new disclosure beyond strfry's own data; uses `from_utf8_lossy` (no panic, Pitfall 5) |
| T-04-INT | Tampering | u64→i64 field casts (kind/created_at) | accept | Nostr kinds/timestamps stay within i64 range; cast avoids GraphQL 32-bit Int truncation (Pitfall 2); no silent wrap for in-range values |
</threat_model>

<verification>
- `cargo build --all-targets` compiles cleanly (deps resolve, types compile, mapping fn type-checks).
- `cargo test --all-targets config::` passes (bind_address default test green).
- Note: bare `cargo test`/`cargo clippy` may fail on the doctest/toolchain-pin step (STATE.md
  pending non-blocking item) — use `--all-targets`. Do NOT attempt to fix the toolchain pin in Phase 4.
</verification>

<success_criteria>
Maps to phase success criteria (foundation portion): the type contracts for SC-1 (events filter),
SC-2 (tag filter input), SC-3 (latestPerAuthor group shape), SC-4 (stats shape), and SC-5
(page object + cursor field) all exist and compile. SC-6 (no mutations) is realized in Plan 04-02's
query-only schema. Concretely:
- Cargo deps present at pinned versions; crate compiles.
- `bind_address` config field defaults to `0.0.0.0:8080`.
- `Event`, `EventsPage`, `AuthorGroup`, `StatsResult`, `EventFilterInput`, `TagFilterInput`,
  and `decoded_event_to_gql` exist and compile.
</success_criteria>

<output>
Create `.planning/phases/04-graphql-api/04-01-SUMMARY.md` when done.
</output>
