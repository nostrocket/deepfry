---
phase: 07-distinct-author-enumeration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/graphql/types.rs
  - src/graphql/resolvers.rs
  - tests/authors_test.rs
  - CLAUDE.md
autonomous: true
requirements: [API-07]
must_haves:
  truths:
    - "A GraphQL query `{ authors { authors hasMore endCursor } }` returns the fixture's distinct hex pubkeys [79be..., c604...] with no errors"
    - "authors(limit: 1) returns one pubkey, hasMore=true, endCursor=Some(hex of that pubkey)"
    - "Feeding the returned endCursor back as `after` yields the next page; iterating to exhaustion covers [PK1, PK2] exactly once and ends with hasMore=false, endCursor=null"
    - "limit is clamped to <=500 (default 100) exactly like events() — passing 9999 does not error and does not pass 0/9999 to the engine"
    - "A malformed `after` cursor (non-hex, or wrong length) returns a client-facing GraphQL error whose message does NOT contain the offending input bytes"
    - "The schema SDL still exposes no `type Mutation`"
  artifacts:
    - path: "src/graphql/types.rs"
      provides: "AuthorsPage SimpleObject mirroring EventsPage"
      contains: "struct AuthorsPage"
    - path: "src/graphql/resolvers.rs"
      provides: "authors resolver on the Query root"
      contains: "async fn authors"
    - path: "tests/authors_test.rs"
      provides: "fixture integration tests for the authors query"
      min_lines: 60
  key_links:
    - from: "src/graphql/resolvers.rs"
      to: "src/query/authors.rs"
      via: "spawn_blocking(move || distinct_authors(&env, after.as_ref(), limit))"
      pattern: "distinct_authors"
    - from: "src/graphql/resolvers.rs"
      to: "src/graphql/types.rs"
      via: "returns AuthorsPage { authors, has_more, end_cursor }"
      pattern: "AuthorsPage"
  prohibitions:
    - "MUST NOT echo the offending `after` bytes in the error message (fail-closed, mirror T-04-CUR/T-04-HEX)"
    - "MUST NOT pass limit=0 to the engine (0 has no special meaning in distinct_authors but keep the clamp identical to events(): max(1).min(500), default 100)"
    - "MUST NOT add a per-author count field — pubkeys-only output (CONTEXT Output-shape decision)"
    - "MUST NOT reuse PageCursor — the authors cursor is the last pubkey (hex), a different domain (CONTEXT cursor decision)"
---

<objective>
Expose the Plan 07-01 engine function as a read-only GraphQL `authors(after, limit)` query returning a paginated `AuthorsPage` of distinct hex pubkeys (API-07), and correct the stale `CLAUDE.md` "What NOT to Use" entry that claims opening any `Event__*` sub-DB is silently wrong.

Purpose: Give consumers a way to paginate the complete distinct-author set — a capability `latestPerAuthor` (needs a caller-supplied author list) and `events()` (O(total events)) cannot serve.

Output: `AuthorsPage` type, the `authors` resolver, fixture integration tests in `tests/authors_test.rs`, and a corrected `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distinct-author-enumeration/07-CONTEXT.md

@src/graphql/types.rs
@src/graphql/resolvers.rs
@src/graphql/schema.rs
@.planning/phases/07-distinct-author-enumeration/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthorsPage GraphQL type + authors resolver</name>
  <read_first>
    - src/graphql/types.rs — EventsPage SimpleObject (mirror its shape and snake_case→camelCase auto-rename: end_cursor→endCursor, has_more→hasMore)
    - src/graphql/resolvers.rs — events() resolver: the limit clamp expression `limit.map(|l| (l.max(1) as usize).min(500)).unwrap_or(100)` (D-04/D-05), the `ctx.data_unchecked::<AppState>()` retrieval, the clone-env-before-closure + spawn_blocking pattern (Pitfall 1, RoTxn is !Send), the task-join error mapping, and map_query_error (T-04-LEAK). Also note router.rs decode_hex usage referenced from engine — but here decode the hex `after` inline or via the existing pub(crate) hex helper without echoing bytes.
    - src/query/authors.rs (from Plan 07-01) — distinct_authors signature: `(env, after: Option<&[u8;32]>, limit) -> Result<(Vec<[u8;32]>, Option<[u8;32]>), QueryError>`
  </read_first>
  <files>src/graphql/types.rs, src/graphql/resolvers.rs</files>
  <action>
    In `src/graphql/types.rs` add `#[derive(SimpleObject)] pub struct AuthorsPage { pub authors: Vec<String>, pub end_cursor: Option<String>, pub has_more: bool }` with a doc comment mirroring EventsPage (note authors are 64-char lowercase hex pubkeys; end_cursor renamed to endCursor, has_more to hasMore; cursor is the last pubkey hex, opaque to consumers, fail-closed on decode). In `src/graphql/resolvers.rs` add an `async fn authors(&self, ctx, after: Option<String>, limit: Option<i32>) -> GqlResult<AuthorsPage>` to the `#[Object] impl Query`. Implement: retrieve AppState via `ctx.data_unchecked::<AppState>()`. Clamp limit identically to events(): `let effective_limit = limit.map(|l| (l.max(1) as usize).min(500)).unwrap_or(100);`. Decode `after` FAIL-CLOSED to exactly 32 bytes: when Some(s), decode the hex string AND require exactly 32 bytes. CRITICAL — `router::decode_hex` only rejects ODD-length hex; an even-but-wrong-length string (e.g. 30 or 34 chars → 15 or 17 bytes) decodes successfully, so `decode_hex` alone is NOT fail-closed for the 32-byte contract. Pipe it through `.try_into::<[u8; 32]>()` exactly as `crate::query::engine::decode_hex_32` (engine.rs ~line 569) and `router::decode_pubkey_warn` (router.rs ~line 320) do: decode to a `Vec<u8>`, then `<[u8; 32]>::try_into(vec)` — i.e. reject non-hex chars, odd length, AND any byte length != 32. On rejection return a client-facing `async_graphql::Error` (extension code "INVALID_CURSOR") whose message describes the failure generically and does NOT include the offending input string (mirror T-04-CUR/T-04-HEX — no byte echo). When `after` is None, the decoded cursor is None. Clone `env` BEFORE the spawn_blocking closure (Pitfall 1). Call `tokio::task::spawn_blocking(move || distinct_authors(&env, after_bytes.as_ref(), effective_limit))`, await it, map the JoinError to a task error and the QueryError via `map_query_error`. Map the engine result `(Vec<[u8;32]>, Option<[u8;32]>)` to AuthorsPage: `authors = pubkeys.iter().map(|pk| hex_encode_lowercase(pk)).collect()`, `end_cursor = next.map(|pk| hex_encode_lowercase(&pk))`, `has_more = next.is_some()`. NOTE: there is NO existing lowercase-hex ENCODER in the codebase — router.rs has only `decode_hex`/`nibble` (decoders), and engine.rs has only `decode_hex_32`. Add a private `fn hex_encode_lowercase(pk: &[u8; 32]) -> String` in resolvers.rs that produces a 64-char lowercase hex string from the 32 bytes (fold over the bytes building a `String::with_capacity(64)` via `write!(s, "{:02x}", b)` or `push_str(&format!("{:02x}", b))`); use it for BOTH the `authors` list entries and `endCursor`. Import `AuthorsPage` and `distinct_authors` at the top of resolvers.rs alongside the existing imports.
  </action>
  <verify>
    <automated>cargo test --all-targets --lib graphql 2>&1 | tail -20 && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'struct AuthorsPage' src/graphql/types.rs` == 1
    - `grep -c 'async fn authors' src/graphql/resolvers.rs` == 1
    - `grep -c 'distinct_authors' src/graphql/resolvers.rs` >= 1
    - The limit clamp matches events(): `grep -c '(l.max(1) as usize).min(500)' src/graphql/resolvers.rs` >= 2 (events + authors)
    - A dedicated lowercase-hex encoder is added (none existed): `grep -c 'fn hex_encode_lowercase' src/graphql/resolvers.rs` == 1
    - The `after` decode is 32-byte fail-closed (not just `decode_hex`): the authors resolver region pipes the decoded bytes through a `[u8; 32]` `try_into` — `grep -c 'try_into' src/graphql/resolvers.rs` >= 1 AND the resolver does NOT accept a bare `decode_hex` result of arbitrary length as the cursor.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <done>AuthorsPage exists, the authors resolver clamps limit, decodes `after` fail-closed without echoing bytes, calls distinct_authors via spawn_blocking with env cloned before the closure, and maps the result to hex AuthorsPage; the crate builds.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture integration tests for the authors query + no-mutation regression</name>
  <read_first>
    - src/graphql/resolvers.rs tests — open_test_env / make_app_state helpers and the schema.execute("{ ... }") pattern; the existing test_no_mutation_in_schema_sdl asserting the SDL has no `type Mutation`
    - tests/cors_test.rs — an existing integration-test file showing how a test in tests/ builds an AppState + schema against the fixture (mirror its setup)
    - tests/fixture/golden_vectors/Event__pubkey.json — distinct pubkeys: PK1=79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798, PK2=c6047f9441ed7d6d3045406e95c07cd85c778e4b8cef3ca7abac09b95c709ee5 (byte-ascending PK1 then PK2)
  </read_first>
  <files>tests/authors_test.rs</files>
  <action>
    Create `tests/authors_test.rs` (integration test). Set up an AppState + schema against the fixture exactly as the in-crate resolver tests / tests/cors_test.rs do (copy tests/fixture/{data,lock}.mdb to a tempdir, open_fixture_env, read_meta, build_schema). Add these tests: (1) `{ authors { authors hasMore endCursor } }` returns no errors and `authors == ["79be667ef9dcbbac55a06295ce870b07029bfcdb2dce28d959f2815b16f81798", "c6047f9441ed7d6d3045406e95c07cd85c778e4b8cef3ca7abac09b95c709ee5"]` in that exact byte-ascending order, hasMore=false, endCursor=null. (2) `authors(limit: 1)` returns exactly one pubkey (PK1), hasMore=true, endCursor == hex(PK1). (3) Multi-page: query limit=1, then feed the returned endCursor back as `after`, repeat until hasMore=false; assert the concatenation of pages equals [PK1, PK2] exactly once each and the final page has endCursor=null. (4) Fail-closed bad cursor — THREE inputs, each returning a non-empty errors array with NO echo of the offending input substring in any error message: (a) `authors(after: "zzzz")` (non-hex chars); (b) `authors(after: "79be")` (4 chars → 2 bytes, too short); (c) `authors(after: "79be66")` (6 chars → 3 bytes — EVEN length but NOT 32 bytes; this is the case a bare `decode_hex` would wrongly accept, proving the `try_into::<[u8;32]>` 32-byte gate). For each, assert the errors array is non-empty AND that no error message contains the offending literal (`"zzzz"`, `"79be"`, `"79be66"` respectively). (5) No-mutation regression: render `schema.sdl()` and assert it does not contain `type Mutation`. Note: the limit-clamp behavior is already covered by the in-crate clamp unit test pattern; you may add a clamp assertion here via `authors(limit: 9999)` returning no errors if convenient, but it is not required.
  </action>
  <verify>
    <automated>cargo test --test authors_test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `tests/authors_test.rs` exists and contains at least 5 `#[tokio::test]` or `#[test]` functions: `grep -c '#\[tokio::test\]\|#\[test\]' tests/authors_test.rs` >= 5
    - The bad-cursor test exercises all THREE inputs including the even-but-not-64-char case: `grep -c '79be66' tests/authors_test.rs` >= 1 (the 6-char even-length cursor that proves the 32-byte try_into gate, not just the odd/short cases)
    - `cargo test --test authors_test` passes — all behaviors green (exact distinct set + order, single-page cursor, multi-page exact-once coverage, fail-closed no-echo bad cursor for non-hex / odd-short / even-but-not-32, no `type Mutation` in SDL)
    - The bad-cursor test asserts no error message contains the offending input substring (verifiable: the test source contains an assertion that the error string does not contain the bad input).
  </acceptance_criteria>
  <done>tests/authors_test.rs proves the authors query returns the fixture's exact distinct pubkey set in byte-ascending order, paginates exact-once across pages, fails closed on a bad cursor without echoing bytes, and the schema still has no mutation surface; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the stale CLAUDE.md "What NOT to Use" Event__* entry</name>
  <read_first>
    - CLAUDE.md — the "## What NOT to Use" table, specifically the row beginning `| Opening any \`Event__*\` sub-DB |` (~line 146) which states opening any Event__* sub-DB makes range scans "silently wrong". This is obsolete: Phase 1 already opens all six Event__* indexes with reimplemented golpe comparators behind a startup self-check gate (see src/lmdb/indexes.rs open_index_* helpers and the comparator self-check).
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    Edit the single stale row in the `## What NOT to Use` table of `CLAUDE.md`. Replace the row that warns against opening any `Event__*` sub-DB with corrected guidance: opening an `Event__*` sub-DB is supported and correct PROVIDED the matching reimplemented golpe comparator is registered (via the `open_index_*` helpers in src/lmdb/indexes.rs) and the startup comparator self-check has passed; the genuine footgun is opening such a sub-DB WITHOUT registering its comparator (heed/LMDB then falls back to memcmp ordering and range scans are silently wrong). Keep the table format intact (three columns: Avoid | Why | Use Instead). Do not change any other row. Do not introduce a fenced code block.
  </action>
  <verify>
    <automated>grep -c 'silently wrong' CLAUDE.md; grep -ci 'open_index' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The obsolete blanket claim is gone: the table no longer states that opening ANY `Event__*` sub-DB is silently wrong. Verifiable: `grep -c 'Opening any .Event__\*. sub-DB | Custom golpe comparators not present' CLAUDE.md` == 0
    - The corrected row references registering the comparator / the open_index helpers: `grep -ci 'open_index\|register.*comparator\|without.*comparator' CLAUDE.md` >= 1
    - The "What NOT to Use" table still parses as a 3-column markdown table (header `| Avoid | Why | Use Instead |` still present): `grep -c '| Avoid | Why | Use Instead |' CLAUDE.md` == 1
  </acceptance_criteria>
  <done>The stale "Opening any Event__* sub-DB" warning is corrected to reflect that comparator-registered opens are supported and only comparator-less opens are unsafe; the table format is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GraphQL client → resolver | The `after` and `limit` args are untrusted client input crossing into the service. `after` is hex-decoded fail-closed; `limit` is clamped. |
| resolver → engine | The resolver passes a validated `[u8;32]` and a clamped `limit` to the read-only engine fn; no LMDB access happens at the resolver layer beyond spawn_blocking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-CUR | Information Disclosure | `after` cursor decode | mitigate | Decode hex fail-closed; reject non-hex / wrong-length with a generic client error that does NOT echo the offending bytes (mirror T-04-CUR/T-04-HEX). Test 4 asserts no-echo. |
| T-07-DOS | Denial of Service | `limit` arg | mitigate | Clamp `limit` to [1,500] default 100 at the resolver before calling the engine — identical to events() (D-04/D-05). Each page bounded to `limit` seeks. |
| T-07-LEAK | Information Disclosure | engine QueryError surfacing | mitigate | Reuse `map_query_error`: Lmdb/Payload errors are logged and returned as opaque "internal error"; only INVALID_CURSOR is client-facing. |
| T-07-SEND | Tampering | RoTxn !Send across spawn_blocking | mitigate | Clone `env` BEFORE the closure (Pitfall 1); the engine opens/drops its own short RoTxn inside spawn_blocking. |
| T-07-MUT | Elevation of Privilege | mutation surface | mitigate | No mutation is added; schema remains EmptyMutation. Test 5 asserts the SDL has no `type Mutation` (API-06 preserved). |
| T-07-SC | Tampering | dependency installs | accept | No new crates added — AuthorsPage/resolver/tests reuse existing deps (async-graphql, heed, tokio). No package-legitimacy gate needed. |
</threat_model>

<verification>
- `cargo test --all-targets` passes (full suite — no regression).
- `cargo test --test authors_test` passes (the five integration behaviors).
- `cargo build` succeeds; `cargo clippy --all-targets -- -D warnings` is clean (mind the STATE.md toolchain caveat — prefer `--all-targets`).
- Schema SDL contains `authors` query field and no `type Mutation`.
- No new dependency added to Cargo.toml (reuse existing crates).
</verification>

<success_criteria>
- A consumer can query `authors(after, limit)` and receive `AuthorsPage { authors: [String!]!, hasMore, endCursor }` of distinct hex pubkeys (API-07).
- `limit` is clamped to the same ceiling as `events()`; a malformed `after` fails closed without echoing bytes.
- The resolver runs via spawn_blocking with env cloned before the closure (read-only invariant preserved).
- The stale CLAUDE.md guidance is corrected.
</success_criteria>

<output>
Create `.planning/phases/07-distinct-author-enumeration/07-02-SUMMARY.md` when done.
</output>
