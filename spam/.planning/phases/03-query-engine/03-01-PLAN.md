---
phase: 03-query-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/query/mod.rs
  - src/query/filter.rs
autonomous: true
requirements: [QRY-01]
must_haves:
  truths:
    - "A NostrFilter can be constructed with any combination of ids/authors/kinds/tags/since/until/limit, including all-None (default feed)"
    - "A PageCursor round-trips through encode()->decode() preserving (created_at, lev_id)"
    - "A malformed cursor string (bad base64 or wrong length) returns QueryError::CursorDecode, never panics"
    - "The query module compiles and is exposed from the library crate"
  artifacts:
    - path: "src/query/filter.rs"
      provides: "NostrFilter, TagFilter, PageCursor, QueryError input/contract types"
      contains: "pub struct NostrFilter"
      min_lines: 80
    - path: "src/query/mod.rs"
      provides: "query submodule declarations"
      contains: "pub mod filter"
  key_links:
    - from: "src/lib.rs"
      to: "src/query/mod.rs"
      via: "pub mod query"
      pattern: "pub mod query"
    - from: "src/query/filter.rs"
      to: "base64"
      via: "PageCursor encode/decode"
      pattern: "base64"
---

<objective>
Establish the Phase-3 query-engine module skeleton and its input/output contracts: the `NostrFilter` / `TagFilter` input types (D-01..D-04), the opaque `PageCursor` with byte-exact encode/decode (D-11), and the `QueryError` error boundary (Claude's Discretion: thiserror house style). This is the contract-first foundation every other Phase-3 plan builds against.

Purpose: Subsequent plans (router, merge, hydrate, engine) receive these types in their plan text rather than reverse-engineering them. Defining cursor encode/decode here means the engine plan only has to *call* it.
Output: `src/query/` module wired into the crate; `filter.rs` with all engine-facing types; `base64` dependency added; full unit-test coverage of cursor round-trip and fail-closed decode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-engine/03-CONTEXT.md
@.planning/phases/03-query-engine/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire query module + add base64 dependency</name>
  <read_first>
    - src/lib.rs (the file being modified — current content: `pub mod lmdb;` + `pub mod config;`)
    - src/lmdb/mod.rs (analog for module-declaration file pattern: one `pub mod` per submodule, alphabetical)
    - Cargo.toml (the file being modified — current `[dependencies]` block; confirm base64 not already present)
    - .planning/phases/03-query-engine/03-PATTERNS.md (§"src/lib.rs", §"src/query/mod.rs", §"No Analog Found" base64 row)
  </read_first>
  <action>
    Add `base64 = "0.22"` to Cargo.toml `[dependencies]` (used by PageCursor encode/decode in D-11). base64 0.22 is the current stable major; it is a long-established, widely-used crate (treat as legitimate — it is not a freshly-published or typosquat package, so no separate legitimacy checkpoint is required, but note its presence in the SUMMARY).
    Add `pub mod query;` to src/lib.rs alongside the existing `pub mod lmdb;` and `pub mod config;`.
    Create src/query/mod.rs declaring the five Phase-3 submodules in alphabetical order: `pub mod engine;`, `pub mod filter;`, `pub mod hydrate;`, `pub mod merge;`, `pub mod router;`. Because engine/hydrate/merge/router are created in later plans, this file will not compile alone until those exist — so in THIS plan declare ONLY `pub mod filter;` and add a `// engine/hydrate/merge/router added in plans 03-02..03-04` comment listing the rest. Add the remaining `pub mod` lines as their plans land.
  </action>
  <verify>
    <automated>grep -q "pub mod query" src/lib.rs && grep -q 'base64' Cargo.toml && grep -q "pub mod filter" src/query/mod.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/lib.rs contains the line `pub mod query;`
    - Cargo.toml `[dependencies]` contains a `base64 = "0.22"` entry
    - src/query/mod.rs contains `pub mod filter;`
    - `grep -q "pub mod query" src/lib.rs` exits 0
  </acceptance_criteria>
  <done>The query module is reachable from the crate root and base64 is an available dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define filter/cursor/error contract types in filter.rs</name>
  <read_first>
    - src/query/filter.rs (the file being created)
    - src/lmdb/types.rs (analog: struct/doc-comment verbosity + `#[derive(Debug, Clone)]` conventions; NostrEvent/DecodedEvent/LevId definitions this phase consumes)
    - src/lmdb/indexes.rs lines 1-69 (IndexError thiserror house style: `#[from]` upstream + named-field struct variants; the index key-format table the cursor/router depend on)
    - src/lmdb/payload.rs lines 1-50 (PayloadError enum, for the `#[from]` wiring of QueryError::Payload)
    - .planning/phases/03-query-engine/03-PATTERNS.md (§"src/query/filter.rs" — full type definitions; §"src/query/engine.rs" cursor encode/decode block)
    - .planning/phases/03-query-engine/03-CONTEXT.md (D-01..D-04, D-08, D-10, D-11)
  </read_first>
  <behavior>
    - Test 1: a NostrFilter with all fields None/empty and limit=0 constructs via `NostrFilter::default()` (Default derive) — proves the empty-filter (D-04) path is representable.
    - Test 2: `PageCursor { created_at: 1720000000, lev_id: 11 }.encode()` then `PageCursor::decode(&s)` returns a cursor with identical created_at and lev_id (round-trip).
    - Test 3: `PageCursor::decode("not!base64!!")` returns `Err(QueryError::CursorDecode { .. })` (malformed base64, no panic).
    - Test 4: `PageCursor::decode(&base64-of-3-bytes)` returns `Err(QueryError::CursorDecode { .. })` (wrong length != 16, no panic).
  </behavior>
  <action>
    Create src/query/filter.rs. Define, per 03-PATTERNS.md §"src/query/filter.rs":
    - `NostrFilter` (`#[derive(Debug, Clone, Default)]`): fields `ids: Option<Vec<String>>`, `authors: Option<Vec<String>>`, `kinds: Option<Vec<u64>>`, `tags: Option<Vec<TagFilter>>`, `since: Option<u64>`, `until: Option<u64>`, `limit: usize`. NOT serde-deserializable — constructed by Phase-4 resolvers. Document each field's routing target per D-02 in doc comments. since/until are pushed into scan bounds (D-03), the rest are residual predicates (D-01).
    - `TagFilter` (`#[derive(Debug, Clone)]`): `name: String`, `values: Vec<String>` — a `#<name>` predicate matched against `tags[i][0]`/`tags[i][1]` (QRY-02).
    - `PageCursor` (`#[derive(Debug, Clone)]`): `created_at: u64`, `lev_id: u64`. Opaque cursor encoding the last emitted `(created_at, lev_id)` pair per D-10/D-11.
    - `impl PageCursor` with `encode(&self) -> String` = `base64::STANDARD.encode(created_at.to_le_bytes() ‖ lev_id.to_le_bytes())` (16 raw bytes, created_at first), and `decode(s: &str) -> Result<PageCursor, QueryError>` that base64-decodes, requires exactly 16 bytes, and reconstructs the two LE u64s. Use `base64::{engine::general_purpose::STANDARD, Engine as _}`.
    - `QueryError` (`#[derive(Debug, thiserror::Error)]`): variants `Lmdb(#[from] crate::lmdb::indexes::IndexError)`, `Payload(#[from] crate::lmdb::payload::PayloadError)`, and `CursorDecode { reason: String }` with message `"Cursor decode error: {reason}"`. CursorDecode is the fail-closed boundary for the untrusted-cursor threat (T-03-CUR).
    Mirror src/lmdb/types.rs doc-comment verbosity and spec/decision-citation style. Place the cursor round-trip + malformed-cursor tests in a `#[cfg(test)] mod tests` block (no fixture env needed — these are pure-function tests).
  </action>
  <verify>
    <automated>cargo test --lib query::filter 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/query/filter.rs contains `pub struct NostrFilter`, `pub struct TagFilter`, `pub struct PageCursor`, `pub enum QueryError`
    - src/query/filter.rs contains `pub fn encode(&self) -> String` and `pub fn decode(` for PageCursor
    - `cargo test --lib query::filter` exits 0 with the four behavior tests passing
    - PageCursor::decode on malformed/wrong-length input returns `QueryError::CursorDecode` (asserted by test, no `unwrap`/panic on the untrusted path)
  </acceptance_criteria>
  <done>All engine-facing contract types exist, cursor encode/decode round-trips, and malformed cursor input fails closed with QueryError::CursorDecode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Phase-4 resolver) → engine | `PageCursor` strings originate from a client and are echoed back through Phase 4; the engine must treat decoded cursor bytes as untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-CUR | Tampering | `PageCursor::decode` | mitigate | Decode validates base64 success AND exact 16-byte length before constructing; on either failure returns `QueryError::CursorDecode` (no `unwrap`, no out-of-bounds slice, no panic). Verified by malformed-input + wrong-length unit tests in Task 2. |
| T-03-SC | Tampering | `base64` crate install | accept | `base64` 0.22 is a long-established, ubiquitous Rust crate (not freshly published / not a typosquat). No `[ASSUMED]`/`[SUS]` package in this plan; no install legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo build --all-targets` succeeds with the new query module and base64 dep.
- `cargo test --lib query::filter` passes all four behavior tests.
- No write transaction, no `.create()`, no new sub-DB open introduced (this plan touches no LMDB code).
</verification>

<success_criteria>
- src/query/ is a compiling module exposed from the crate root.
- NostrFilter / TagFilter / PageCursor / QueryError are defined exactly per D-01..D-04, D-10, D-11.
- PageCursor encode/decode round-trips and fails closed on malformed input.
</success_criteria>

<output>
Create `.planning/phases/03-query-engine/03-01-SUMMARY.md` when done.
</output>
