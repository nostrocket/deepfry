---
phase: 04-batch-triage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/analysis/thresholds.ts
  - src/analysis/chunk.ts
  - src/analysis/chunk.test.ts
  - src/analysis/mergeByAuthor.ts
  - src/analysis/mergeByAuthor.test.ts
  - src/analysis/triage.ts
  - src/analysis/triage.test.ts
  - src/analysis/batchImport.ts
  - src/analysis/batchImport.test.ts
  - src/queries/latestPerAuthor.graphql.ts
  - src/queries/authors.graphql.ts
  - src/hooks/useLatestPerAuthor.ts
  - src/hooks/useAuthorEnumeration.ts
autonomous: true
requirements: [BATCH-01, BATCH-02, BATCH-03, BATCH-04]
must_haves:
  truths:
    - "A deduped input pubkey set is chunked into <=500-author chunks (the <=1000 author cap binds before the 256 KiB body budget at perAuthor=5)"
    - "latestPerAuthor results are LEFT-JOINED to the full deduped input set keyed strictly by author; an input author with no returned group appears as a row with events:[] (an explicit 0-events row)"
    - "triageAuthor maps a per-author event window through the existing analyzers to { eventCount, burst, nearDup, tagFanOut } with no clean/ok/safe field"
    - "Pasted/uploaded tokens are split on whitespace/newlines/commas, each routed through parseIdentifier, normalized to lowercase hex, deduped, and counted as valid / duplicates / unparseable with the unparseable tokens preserved"
    - "useLatestPerAuthor fetches each chunk through classify() BEFORE reading data, retains partial results on a failed chunk, and degrades a 413 chunk by halve-and-retry bottoming out at chunk length 1"
    - "useAuthorEnumeration pages the authors query with the opaque cursor passed verbatim until hasMore is false, exposes a Stop control and a live running count, and performs a bounded INVALID_CURSOR restart"
  prohibitions:
    - "MUST NOT index-zip the latestPerAuthor response against the input array (zero-match authors are omitted by the server)"
    - "MUST NOT add a clean/ok/safe/score field to TriageIndicators"
    - "MUST NOT select raw in the latestPerAuthor document"
    - "MUST NOT construct, parse, or cross-use cursors — both events and authors cursors are opaque, passed verbatim"
    - "MUST NOT re-implement nip19 — every token goes through parseIdentifier"
    - "MUST NOT fire chunks unbounded/parallel-flooded — sequential (or small bounded concurrency) with pacing"
  artifacts:
    - path: "src/analysis/chunk.ts"
      provides: "Pure dual-axis chunk sizing + chunkAuthors slicer"
      exports: ["chunkAuthors", "chunkSize", "byteBudgetAuthors", "SAFE_BYTES_PER_AUTHOR", "BODY_LIMIT_BYTES"]
    - path: "src/analysis/mergeByAuthor.ts"
      provides: "Left-join input set against AuthorGroup[] keyed by author"
      exports: ["mergeByAuthor", "TriageRow"]
    - path: "src/analysis/triage.ts"
      provides: "Per-author analyzer fan-in adapter"
      exports: ["triageAuthor", "TriageIndicators"]
    - path: "src/analysis/batchImport.ts"
      provides: "Tokenize + normalize + dedupe + count import parser"
      exports: ["parseBatchInput", "BatchImportResult"]
    - path: "src/queries/latestPerAuthor.graphql.ts"
      provides: "latestPerAuthor graphql() document (id pubkey kind createdAt content tags, raw EXCLUDED)"
      exports: ["LatestPerAuthorDocument"]
    - path: "src/queries/authors.graphql.ts"
      provides: "authors graphql() document (after, limit -> authors endCursor hasMore)"
      exports: ["AuthorsDocument"]
    - path: "src/hooks/useLatestPerAuthor.ts"
      provides: "Chunk loop hook (classify-gated, 413 halve-retry, partial-on-error, runId stale-drop)"
      exports: ["useLatestPerAuthor"]
    - path: "src/hooks/useAuthorEnumeration.ts"
      provides: "authors enumeration loop hook (opaque cursor, Stop, running count, bounded INVALID_CURSOR restart)"
      exports: ["useAuthorEnumeration"]
  key_links:
    - from: "src/hooks/useLatestPerAuthor.ts"
      to: "src/analysis/chunk.ts"
      via: "chunkAuthors(dedupedHexes, chunkSize()) splits the input before the fetch loop"
      pattern: "chunkAuthors\\("
    - from: "src/hooks/useLatestPerAuthor.ts"
      to: "src/analysis/mergeByAuthor.ts"
      via: "mergeByAuthor(dedupedInputHexes, accumulatedGroups) after each chunk"
      pattern: "mergeByAuthor\\("
    - from: "src/hooks/useLatestPerAuthor.ts"
      to: "src/transport/errors.ts"
      via: "classify(result) before reading result.data on every chunk"
      pattern: "classify\\("
    - from: "src/analysis/triage.ts"
      to: "src/analysis/rate.ts"
      via: "analyzeRate over the per-author createdAt window"
      pattern: "analyzeRate\\("
    - from: "src/analysis/batchImport.ts"
      to: "src/identifier/identifier.ts"
      via: "parseIdentifier per token (single nip19 site)"
      pattern: "parseIdentifier\\("
---

## Phase Goal

**As an** investigating analyst, **I want to** import or enumerate a list of suspect pubkeys and triage them all in one chunked, match-by-author pass, **so that** I can screen many authors at once and drill into the suspicious ones without truncating, misattributing, or overloading the relay.

## Acceptance Criteria

- [ ] `npm test` passes including the four new pure-module test files
- [ ] `npm run codegen` succeeds and produces typed `LatestPerAuthorDocument` / `AuthorsDocument`
- [ ] `npm run build` (`tsc -b && vite build`) typechecks with the two new hooks consuming codegen types

<objective>
Build the pure logic core + transport hooks that power batch triage: the chunk-sizing math, the load-bearing left-join merge-by-author, the per-author analyzer adapter, the import tokenizer, the two new graphql() documents, and the two fetch hooks (chunk loop + authors enumeration loop). This is the data slice — Plan 04-02 mounts the views on top of it.

Purpose: Concentrate the genuinely new logic (chunk math, left-join, 413 degrade, enumeration loop) in pure, unit-tested modules following the Phase 1-3 analog shapes, so the views in 04-02 are thin composition.
Output: 4 pure modules (+ tests), 2 query documents, codegen run, 2 hooks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-batch-triage/04-CONTEXT.md
@.planning/phases/04-batch-triage/04-RESEARCH.md
@.planning/phases/04-batch-triage/04-PATTERNS.md

# Reused source (read the cited line ranges in each task's <read_first>)
@src/hooks/useAuthorWindow.ts
@src/transport/errors.ts
@src/analysis/thresholds.ts
@src/identifier/identifier.ts
@src/queries/events.graphql.ts
</context>

## Artifacts this phase produces (Plan 04-01)

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `TRIAGE` (in `src/analysis/thresholds.ts`) | new const block | `kind:1, perAuthor:5, chunkAuthors:500, largeSetWarn:1000, enumLimit:500, maxFileBytes` |
| `src/analysis/chunk.ts` → `chunkAuthors`, `chunkSize`, `byteBudgetAuthors`, `SAFE_BYTES_PER_AUTHOR`, `SAFE_FIXED_OVERHEAD`, `BODY_LIMIT_BYTES` | new pure module | dual-axis sizing |
| `src/analysis/mergeByAuthor.ts` → `mergeByAuthor`, `TriageRow` | new pure module | left-join, load-bearing |
| `src/analysis/triage.ts` → `triageAuthor`, `TriageIndicators` | new pure adapter | 4 analyzers → 4 indicators |
| `src/analysis/batchImport.ts` → `parseBatchInput`, `BatchImportResult` | new pure module | tokenize/dedupe/count |
| `src/queries/latestPerAuthor.graphql.ts` → `LatestPerAuthorDocument` | new query doc | 6 fields, raw excluded |
| `src/queries/authors.graphql.ts` → `AuthorsDocument` | new query doc | cursor page |
| `src/hooks/useLatestPerAuthor.ts` → `useLatestPerAuthor` | new hook | chunk loop |
| `src/hooks/useAuthorEnumeration.ts` → `useAuthorEnumeration` | new hook | enumeration loop |

<!-- planner-discipline-allow: parseIdentifier -->
<!-- planner-discipline-allow: nip19 -->
<!-- planner-discipline-allow: raw -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TRIAGE thresholds + pure chunk / merge / triage / import modules (TDD)</name>
  <files>src/analysis/thresholds.ts, src/analysis/chunk.ts, src/analysis/chunk.test.ts, src/analysis/mergeByAuthor.ts, src/analysis/mergeByAuthor.test.ts, src/analysis/triage.ts, src/analysis/triage.test.ts, src/analysis/batchImport.ts, src/analysis/batchImport.test.ts</files>
  <read_first>
    - Modifying: src/analysis/thresholds.ts (existing BURST lines 13-17, NEAR_DUP 25-28, TAGS 35-39 — copy the `export const X = { ... } as const` shape + the "single tunable home / honesty is threshold-independent" doc-comment voice)
    - Analog (chunk.ts module shape): src/analysis/rate.ts lines 1-20 (doc-header + `import { BURST } from './thresholds'` single-tunable-home import) and its exported-pure-helper style
    - Analog (mergeByAuthor pure-derive shape): src/hooks/useAuthorWindow.ts lines 71-87 `deriveWindowMeta` (extracted-pure-for-testability comment + signature); WindowEvent interface at lines 34-41 (REUSE — do not define a second event type)
    - Analog (triage adapter fan-in): src/analysis/tags.ts lines 32-37 (TagsResult signal-present-flags, no clean field) — confirmed signatures: `analyzeRate(createdAts: number[]): RateResult` with `.burstDetected` (rate.ts:84/:45); `nearDup(events: {id:string;content:string}[]): NearDupResult` with `.duplicateCount` (nearDup.ts:103/:95); `analyzeTags(events:{id:string;tags:string[][]}[]): TagsResult` with `.massMention`/`.stuffing` (tags.ts:57/:35-37)
    - Analog (batchImport): src/identifier/identifier.ts lines 30-43 — `ParseResult = { ok:true; hex; npub; sourceKind } | { ok:false; reason }`; `parseIdentifier(raw)`; `isHexPubkey(s)`
    - Analog (all test files): src/analysis/nearDup.test.ts (describe/it/expect structure, Node vitest env)
  </read_first>
  <action>
First extend `thresholds.ts`: add an `export const TRIAGE = { kind: 1, perAuthor: 5, chunkAuthors: 500, largeSetWarn: 1000, enumLimit: 500, maxFileBytes: 5 * 1024 * 1024 } as const` block, with a doc-comment in the same voice as BURST/NEAR_DUP/TAGS noting these are Claude's-discretion tunables (RESEARCH A1-A5) and the honesty posture is threshold-independent. `kind=1` = text notes (BATCH-02), `perAuthor=5` = deliberately tiny first-pass window, `chunkAuthors=500` = conservative static chunk (the <=1000 author cap binds before 256 KiB), `largeSetWarn=1000` = non-blocking warn, `enumLimit=500` = authors() page ceiling (contract §12), `maxFileBytes` = upload bound (V12).

Create `chunk.ts` (pure): export `SAFE_BYTES_PER_AUTHOR = 80` (measured marginal 67, +UTF-8/proxy margin), `SAFE_FIXED_OVERHEAD = 4096`, `BODY_LIMIT_BYTES = 256 * 1024`; `byteBudgetAuthors(): number` returns `Math.floor((BODY_LIMIT_BYTES - SAFE_FIXED_OVERHEAD) / SAFE_BYTES_PER_AUTHOR)` (~3225); `chunkSize(): number` returns `Math.min(TRIAGE.chunkAuthors, 1000, byteBudgetAuthors())` (= 500 in practice — document that the author cap binds first); `chunkAuthors(hexes: string[], size: number): string[][]` is a plain slice loop. The 413 halve-and-retry recursion lives in the HOOK (Task 3), not here — `chunk.ts` stays pure and network-free.

Create `mergeByAuthor.ts` (pure, LOAD-BEARING): `export interface TriageRow { author: string; events: WindowEvent[] }` (import `WindowEvent` from `../hooks/useAuthorWindow` — do NOT define a second event type). `mergeByAuthor(inputHexes: string[], groups: { author: string; events: WindowEvent[] }[]): TriageRow[]` builds `new Map<string, WindowEvent[]>()` keyed strictly by `g.author`, then `inputHexes.map(hex => ({ author: hex, events: byAuthor.get(hex) ?? [] }))` — a LEFT JOIN from the full input set. Add a code comment pinning the rationale: latestPerAuthor OMITS zero-match authors (contract §5/§8), so `groups[i].author !== inputHexes[i]`; a missing author yields `events: []` which renders as an explicit "0 events" row. (Do not place the literal grep-target string in a head-comment that the test negative-greps; describe by concept.)

Create `triage.ts` (pure adapter): `export interface TriageIndicators { eventCount: number; burst: boolean; nearDup: boolean; tagFanOut: boolean }` (NO clean/ok/safe/score field — asymmetry carried from Phases 2-3). `triageAuthor(events: WindowEvent[]): TriageIndicators` runs `analyzeRate(events.map(e => e.createdAt)).burstDetected`, `nearDup(events.map(e => ({ id: e.id, content: e.content }))).duplicateCount > 0`, `analyzeTags(events.map(e => ({ id: e.id, tags: e.tags }))).massMention || .stuffing` → `tagFanOut`, and `eventCount: events.length` (0..perAuthor neutral denominator, never "worse"). isSaneTs is applied INSIDE analyzeRate — pass raw createdAt.

Create `batchImport.ts` (pure): `export interface BatchImportResult { validHexes: string[]; duplicateCount: number; unparseable: string[] }`. `parseBatchInput(text: string): BatchImportResult` splits on `/[\s,]+/`, drops empty tokens, routes EACH token through `parseIdentifier` (single nip19 site — never re-implement), collects `.ok` results' lowercase `hex` into a `Set` (dedupe), counts duplicates (a valid hex already in the Set), and pushes `.ok === false` raw tokens into `unparseable` (preserved verbatim — never silently dropped; note/nsec land here via parseIdentifier's existing rejection). Return `validHexes` as the deduped array.

Write the four `.test.ts` files (Node vitest, nearDup.test.ts structure):
- chunk.test.ts: `chunkSize()` returns 500; `byteBudgetAuthors()` > 1000 (never the binding axis); `chunkAuthors` of 1100 hexes at size 500 yields lengths [500,500,100]; empty input → [].
- mergeByAuthor.test.ts: THE load-bearing pins — (a) an input author absent from `groups` yields a row with `events: []`; (b) when `groups` is in a DIFFERENT order than `inputHexes` AND omits some input authors, every output row's `author` equals its input hex and carries that author's own events (proves match-by-author, not index-zip); (c) output length === inputHexes length.
- triage.test.ts: indicators map correctly; specifically `tagFanOut` is true when massMention OR stuffing trips; a 0-event input → `{ eventCount: 0, burst: false, nearDup: false, tagFanOut: false }`; assert no `clean`/`score` key exists on the result.
- batchImport.test.ts: mixed npub + hex + duplicate + a note + garbage → correct `validHexes` (deduped lowercase), `duplicateCount`, and `unparseable` containing the note + garbage verbatim.

NO fenced code blocks belong in the written module head-comments that any test negative-greps; keep prohibition language conceptual.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes; chunk.test.ts, mergeByAuthor.test.ts, triage.test.ts, batchImport.test.ts all green
    - `grep -n "chunkSize\|chunkAuthors\|byteBudgetAuthors" src/analysis/chunk.ts` shows all three exports
    - `grep -c "events: byAuthor.get" src/analysis/mergeByAuthor.ts` >= 1 (left-join present)
    - `grep -vE '^\s*//' src/analysis/triage.ts | grep -c -iE 'clean|safe|score'` == 0 (no verdict field in code)
    - `grep -c "parseIdentifier" src/analysis/batchImport.ts` >= 1 and `grep -c "nip19" src/analysis/batchImport.ts` == 0 (single nip19 site reused, none added)
    - `grep -c "TRIAGE" src/analysis/thresholds.ts` >= 1
  </acceptance_criteria>
  <done>Four pure modules + TRIAGE thresholds exist and are unit-tested; the left-join is pinned by a test where input order differs from response order and some authors are omitted.</done>
</task>

<task type="auto">
  <name>Task 2: New graphql() documents + [BLOCKING] codegen</name>
  <files>src/queries/latestPerAuthor.graphql.ts, src/queries/authors.graphql.ts, src/gql/graphql.ts, src/gql/gql.ts</files>
  <read_first>
    - Analog: src/queries/events.graphql.ts lines 1, 17-32 (the `import { graphql } from '../gql'` + `export const XDocument = graphql(\`...\`)` shape + the field-selection rationale comment; raw is EXCLUDED there for the same large-payload reason)
    - Second-doc-in-dir analog: src/queries/rawEvent.graphql.ts
    - Schema (do not edit): schema.graphql:12 `latestPerAuthor(kind: Int!, perAuthor: Int!, authors: [String!]!): [AuthorGroup!]!`; :40 `AuthorGroup { author events }`; :13 `authors(after: String, limit: Int): AuthorsPage!`; :34 `AuthorsPage { authors endCursor hasMore }`
    - Codegen config: codegen.ts:13 (schema = checked-in `./schema.graphql`, NOT live introspection — depth-12 limit rejects introspection)
  </read_first>
  <action>
Create `src/queries/latestPerAuthor.graphql.ts`: `import { graphql } from '../gql'` then `export const LatestPerAuthorDocument = graphql(\`query LatestPerAuthor($kind: Int!, $perAuthor: Int!, $authors: [String!]!) { latestPerAuthor(kind: $kind, perAuthor: $perAuthor, authors: $authors) { author events { id pubkey kind createdAt content tags } } }\`)`. Select EXACTLY the same six fields events.graphql.ts selects — `tags` feeds tagFanOut, `content` feeds nearDup (RESEARCH Pitfall 6). EXCLUDE `raw` (large, unused — same rationale as events.graphql.ts). Add a one-line comment noting tags+content are required for indicators and raw is excluded.

Create `src/queries/authors.graphql.ts`: `export const AuthorsDocument = graphql(\`query Authors($after: String, $limit: Int) { authors(after: $after, limit: $limit) { authors endCursor hasMore } }\`)`. The cursor (`endCursor`) is opaque — a comment should note it is passed back verbatim, never constructed.

Then run `npm run codegen` (BLOCKING — it reads the checked-in schema.graphql and regenerates `src/gql/*`; the hooks in Task 3 import the generated `*Document` types). Commit the regenerated `src/gql/graphql.ts` + `src/gql/gql.ts`.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npm run codegen 2>&1 | tail -15 && grep -c "LatestPerAuthor\|Authors" src/gql/graphql.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run codegen` exits 0
    - `grep -c "raw" src/queries/latestPerAuthor.graphql.ts` == 0 (raw NOT selected)
    - `grep -c "id pubkey kind createdAt content tags\|tags" src/queries/latestPerAuthor.graphql.ts` >= 1 (tags selected)
    - `src/gql/graphql.ts` contains generated types referencing `LatestPerAuthor` and `Authors`
    - `npx tsc -b 2>&1 | head` shows no errors attributable to the new documents
  </acceptance_criteria>
  <done>Both query documents exist with the correct selections (raw excluded), codegen ran, and generated types are available for the hooks.</done>
</task>

<task type="auto">
  <name>Task 3: useLatestPerAuthor (chunk loop) + useAuthorEnumeration (enumeration loop)</name>
  <files>src/hooks/useLatestPerAuthor.ts, src/hooks/useAuthorEnumeration.ts</files>
  <read_first>
    - Analog (BOTH hooks): src/hooks/useAuthorWindow.ts — throw-guard `.toPromise().catch(() => 'THREW')` (lines 17-19 doc + the fetch body), classify-before-data discipline, `runId` stale-drop ref + in-flight ref (lines 95-100), opaque-cursor `after.current = page.endCursor ?? null`, INVALID_CURSOR bounded restart (cursor doc lines 9-11)
    - classify(): src/transport/errors.ts — 7-kind union; relevant kinds `PAYLOAD_TOO_LARGE` (413), `NOT_READY` (503), `INVALID_CURSOR`, `TOO_MANY_AUTHORS`, `INTERNAL`, `NETWORK`; `classify(result)` returns null only when safe to read `result.data`
    - client: src/transport/client.ts (POST-only urql `client`)
    - chunk/merge: src/analysis/chunk.ts (`chunkAuthors`, `chunkSize`), src/analysis/mergeByAuthor.ts (`mergeByAuthor`, `TriageRow`) from Task 1
    - thresholds: TRIAGE (kind, perAuthor, enumLimit) from Task 1
    - documents: LatestPerAuthorDocument / AuthorsDocument from Task 2 (codegen-typed)
  </read_first>
  <action>
Create `useLatestPerAuthor.ts`. Import `client`, `classify`/`ApiError`, `LatestPerAuthorDocument`, `chunkAuthors`/`chunkSize`, `mergeByAuthor`/`TriageRow`, `TRIAGE`. Expose `useLatestPerAuthor(): { rows: TriageRow[]; triagedCount: number; totalCount: number; loading: boolean; error: ApiError | null; chunkErrors: { index: number; error: ApiError }[]; run: (dedupedInputHexes: string[]) => void; retryChunk: (index: number) => void }`. `run(hexes)` sets `totalCount = hexes.length`, increments a `runId` ref (stale-drop), splits via `chunkAuthors(hexes, chunkSize())`, and iterates chunks SEQUENTIALLY (pacing — never parallel-flood; small bounded concurrency is allowed but default sequential). For each chunk call a `fetchChunk(chunk)` that does `client.query(LatestPerAuthorDocument, { kind: TRIAGE.kind, perAuthor: TRIAGE.perAuthor, authors: chunk }, { requestPolicy: 'network-only' }).toPromise().catch(() => 'THREW')`, drops the result if `myRun !== runId.current`, then `classify(result)` BEFORE reading data. Branches: `PAYLOAD_TOO_LARGE && chunk.length > 1` → halve (`mid = Math.ceil(chunk.length/2)`) and recurse on both halves, concatenating groups (bottoms out at length 1); `NOT_READY`/`INVALID_CURSOR`/`THREW`/NETWORK → record a recoverable chunkError (index) + retain partial results + leave the chunk's authors retryable; `INTERNAL` → record a hard chunkError (generic, no server message). On success read `result.data?.latestPerAuthor` and accumulate the groups. After each chunk resolves, re-run `mergeByAuthor(dedupedInputHexes, accumulatedGroups)` (incremental re-merge — RESEARCH Open Q1) and set `rows` + `triagedCount` (authors covered by resolved chunks). `retryChunk(index)` re-issues only that chunk's authors. The hook NEVER kills the whole batch on a chunk error.

Create `useAuthorEnumeration.ts`. Import `client`, `classify`/`ApiError`, `AuthorsDocument`, `TRIAGE`. Expose `useAuthorEnumeration(): { authors: string[]; runningCount: number; enumerating: boolean; stopped: boolean; complete: boolean; error: ApiError | null; start: () => void; stop: () => void }`. `start()` resets state, sets a `stopRequested` ref false, and runs a loop: `after` starts null, `seen = new Set<string>()`, `cursorRetry` ref budget 0. Each iteration: if `stopRequested.current` break; `client.query(AuthorsDocument, { after, limit: TRIAGE.enumLimit }, { requestPolicy: 'network-only' }).toPromise().catch(() => 'THREW')`; `classify` BEFORE data. On `INVALID_CURSOR`: bounded restart — if `after === null || cursorRetry.current >= 1` surface the error and stop; else `cursorRetry.current += 1; after = null; seen.clear()` and continue (mirror useAuthorWindow's guard so a looping lens can't spin forever). On any other error: break, retaining the partial set. On success: add each `page.authors` hex to `seen`, set `runningCount = seen.size` (live snapshot count), set `after = page.endCursor ?? null` (opaque, verbatim). Loop while `after && !stopRequested.current`. Mark `complete` when the loop exits with `hasMore` false; `stopped` when exited via the Stop control. `stop()` flips `stopRequested.current = true`. Expose the discovered `authors` as `[...seen]` so they feed the SAME useLatestPerAuthor pipeline (one code path — BATCH-04).
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npx tsc -b 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0 (both hooks typecheck against codegen types)
    - `grep -c "classify(" src/hooks/useLatestPerAuthor.ts` >= 1 AND it appears before `result.data` in the fetch body (manual: classify gate precedes data read)
    - `grep -c "PAYLOAD_TOO_LARGE" src/hooks/useLatestPerAuthor.ts` >= 1 (413 halve-retry branch present)
    - `grep -c "mergeByAuthor\|chunkAuthors" src/hooks/useLatestPerAuthor.ts` >= 2 (uses both pure modules)
    - `grep -c "INVALID_CURSOR" src/hooks/useAuthorEnumeration.ts` >= 1 AND `grep -c "endCursor" src/hooks/useAuthorEnumeration.ts` >= 1 (bounded restart + opaque cursor)
    - `grep -c "stopRequested\|stop" src/hooks/useAuthorEnumeration.ts` >= 1 (Stop control)
    - `grep -c "requestPolicy: 'network-only'" src/hooks/useLatestPerAuthor.ts src/hooks/useAuthorEnumeration.ts` >= 2 (honesty: re-triage hits network)
  </acceptance_criteria>
  <done>Both hooks compile against the generated types, fetch through classify() before data, retain partial results on chunk error with retry, degrade 413 by halve-and-retry, and the enumeration loop pages the opaque cursor with Stop + bounded INVALID_CURSOR restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted/uploaded/enumerated tokens → app | author-controlled untrusted text crosses here |
| app → lens (latestPerAuthor / authors POST) | request bodies must respect TOO_MANY_AUTHORS (<=1000) and 413 (256 KiB) |
| lens → app (GraphQL on HTTP 200) | errors arrive on 200; data must not be read before classify() |
| opaque cursor (authors endCursor) | server-issued token, passed back verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (data integrity) | mergeByAuthor | mitigate | strict left-join keyed by `author`; never index-zip (response omits zero-match authors). Pinned by comment + a test where response order differs from input and omits authors. |
| T-04-02 | Denial of Service (self) | useLatestPerAuthor / useAuthorEnumeration | mitigate | chunk size <=500 (cap binds before 256 KiB); sequential pacing; bounded INVALID_CURSOR retry; Stop control; max-file-size constant (TRIAGE.maxFileBytes). |
| T-04-03 | Information disclosure | classify() INTERNAL on a chunk | mitigate | INTERNAL kind carries no server message; hook records a generic hard chunkError, never echoes internals. |
| T-04-04 | Tampering | author/createdAt/kind from the lens | mitigate | forgeable createdAt bounds-checked via isSaneTs inside analyzeRate/analyzeKinds (applied per-author in triageAuthor). |
| T-04-05 | Tampering | opaque authors cursor | mitigate | endCursor passed verbatim; never constructed, parsed, or cross-used with the events cursor. |
| T-04-SC | Tampering | npm installs | mitigate | none — this phase installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A); no install task, no checkpoint needed. |
</threat_model>

<verification>
- `npm test` green (four new pure-module suites).
- `npm run codegen` clean; generated types present.
- `npx tsc -b` clean (hooks consume codegen types).
- Left-join pin test fails if mergeByAuthor is changed to index-zip.
</verification>

<success_criteria>
The pure logic core, the two query documents (codegen-typed, raw excluded), and the two transport hooks (chunk loop + enumeration loop) exist, are unit-tested where pure, typecheck, and honor every prohibition (no index-zip, no clean/score field, no raw in the query, opaque cursors, single nip19 site, sequential pacing).
</success_criteria>

<output>
Create `.planning/phases/04-batch-triage/04-01-SUMMARY.md` when done.
</output>
