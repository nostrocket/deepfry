---
phase: 01-foundation-stats-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/hooks/useStatsPoll.ts
  - src/views/StatsDashboard.tsx
  - src/views/StatsDashboard.module.css
  - src/App.tsx
autonomous: true
requirements: [STATS-01, STATS-02]
must_haves:
  truths:
    - "The dashboard shows live eventCount, maxLevId, dbVersion, and pinnedStrfryVersion from codegen-typed data"
    - "Stats poll on a seconds-scale interval, pause when the tab is hidden, and surface a non-intrusive 'corpus changed' nudge on maxLevId increase without auto-refetching"
    - "Every distinct classifier state and the empty-corpus / poll-paused states render a distinct, non-blank treatment per the UI-SPEC"
    - "A 'Refresh stats' control re-pulls stats on demand (the only CTA)"
  artifacts:
    - path: "src/hooks/useStatsPoll.ts"
      provides: "setTimeout-reschedule poll + Page Visibility pause + maxLevId-diff nudge flag"
      exports: ["useStatsPoll"]
    - path: "src/views/StatsDashboard.tsx"
      provides: "Four stat cards + connection/error states + corpus-changed nudge + refresh CTA"
      exports: ["StatsDashboard"]
    - path: "src/views/StatsDashboard.module.css"
      provides: "Hand-rolled CSS for the dashboard, consuming tokens.css"
  key_links:
    - from: "src/hooks/useStatsPoll.ts"
      to: "src/queries/stats.graphql.ts"
      via: "queries StatsDocument via the urql client"
      pattern: "StatsDocument"
    - from: "src/views/StatsDashboard.tsx"
      to: "src/transport/errors.ts"
      via: "branches on classify() result to pick a state treatment"
      pattern: "classify|ApiError"
    - from: "src/App.tsx"
      to: "src/views/StatsDashboard.tsx"
      via: "mounts StatsDashboard after readiness resolves"
      pattern: "StatsDashboard"
  prohibitions:
    - "NO setInterval (use setTimeout-reschedule so slow requests never stack)"
    - "NO auto-refetch of any window on maxLevId change (nudge flag only)"
    - "NO sub-second polling (seconds-scale; default 5000ms as a tunable constant)"
    - "NO dangerouslySetInnerHTML — pinnedStrfryVersion rendered as escaped plaintext"
    - "NO blank panel or null-crash for any state (every state distinct and non-blank)"
    - "NO treating eventCount=0 as an error (calm fact, neutral styling)"
    - "NO use of the teal accent outside the corpus-changed nudge and the live-poll dot"
---

<objective>
Walking-skeleton slice 3: replace the raw stats render with the polished, honest dashboard. Add `useStatsPoll` (setTimeout-reschedule, Page Visibility pause, maxLevId-diff nudge flag — never auto-refetch) and `StatsDashboard` rendering the four scalars plus the complete distinct-state set from the approved UI-SPEC (STATS-01, STATS-02).

Purpose: Deliver the user-visible capability — an analyst launches the tool and watches live corpus stats with an honest change-nudge. This is the slice that makes the phase goal true. The full UI state set is built now because Phases 2–4 inherit it.
Output: `hooks/useStatsPoll.ts`, `views/StatsDashboard.tsx` (+ CSS module), and App.tsx mounting the dashboard after readiness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-stats-dashboard/01-CONTEXT.md
@.planning/phases/01-foundation-stats-dashboard/01-RESEARCH.md
@.planning/phases/01-foundation-stats-dashboard/01-UI-SPEC.md
@contract.md
@.planning/phases/01-foundation-stats-dashboard/01-01-SUMMARY.md
@.planning/phases/01-foundation-stats-dashboard/01-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (01-03) produces:
- Files: src/hooks/useStatsPoll.ts, src/views/StatsDashboard.tsx, src/views/StatsDashboard.module.css; modifies src/App.tsx
- Exported symbols: useStatsPoll (useStatsPoll.ts); StatsDashboard (StatsDashboard.tsx)
- Default poll interval 5000ms exposed as a tunable constant
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useStatsPoll — setTimeout-reschedule, hidden-tab pause, maxLevId-diff nudge</name>
  <files>src/hooks/useStatsPoll.ts</files>
  <read_first>
    - .planning/phases/01-foundation-stats-dashboard/01-RESEARCH.md § "Code Examples" → "useStatsPoll with hidden-tab pause + maxLevId diff" and § "Pattern 4: Poll-and-diff for 'corpus changed'"
    - contract.md § 9 (poll maxLevId on a sane seconds interval; no push), § 5 (maxLevId is monotonic insert counter)
    - .planning/phases/01-foundation-stats-dashboard/01-CONTEXT.md § "Locked from Research" (setTimeout not setInterval; Page Visibility pause; maxLevId-diff flips a nudge flag ONLY; default 5000ms tunable constant)
  </read_first>
  <behavior>
    - The hook queries StatsDocument via the urql client on a seconds-scale interval (default 5000ms, exposed as a named tunable constant / hook arg)
    - When document.visibilityState is 'hidden', the tick skips the network call and reschedules (no work on hidden tab)
    - When maxLevId increases versus the last seen value, hasNewData flips to true (nudge flag) — it never triggers a refetch of any window
    - acknowledge() resets hasNewData to false
    - returns { stats, hasNewData, acknowledge } and exposes a paused/visibility signal so the view can show the poll-paused state
  </behavior>
  <action>
    Write src/hooks/useStatsPoll.ts per RESEARCH "useStatsPoll" code example: `useStatsPoll(intervalMs = 5000)` using setTimeout RESCHEDULE (never setInterval) so a slow request never stacks overlapping polls; pause the network call when document.visibilityState === 'hidden' and resume via a 'visibilitychange' listener; keep the last maxLevId in a ref and flip a hasNewData nudge flag when it strictly increases. Expose the default interval as a named constant the planner/dev can tune (RESEARCH Assumption A3: any 2–10s value satisfies "seconds-scale"). Return { stats, hasNewData, acknowledge, isPaused } (or equivalent visibility signal). The nudge flag must NEVER auto-refetch — the analyst decides (Pitfall: aggressive polling). Clean up the timer and listener on unmount.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; grep -q "setTimeout" src/hooks/useStatsPoll.ts && ! grep -q "setInterval" src/hooks/useStatsPoll.ts && grep -q "visibilitychange\|visibilityState" src/hooks/useStatsPoll.ts && echo POLL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - useStatsPoll uses setTimeout (NOT setInterval) — grep confirms no setInterval
    - Page Visibility (visibilityState/visibilitychange) is used to pause on hidden tab
    - maxLevId increase flips hasNewData; nothing auto-refetches
    - Default interval is a named tunable constant defaulting to 5000ms
    - Timer + listener cleaned up on unmount
  </acceptance_criteria>
  <done>Stats poll on a seconds-scale interval, pause cleanly when the tab is hidden, and a maxLevId increase flips a nudge flag only — no aggressive polling, no auto-refetch.</done>
</task>

<task type="auto">
  <name>Task 2: StatsDashboard view — four cards + complete distinct-state set + nudge + refresh</name>
  <files>src/views/StatsDashboard.tsx, src/views/StatsDashboard.module.css, src/App.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-stats-dashboard/01-UI-SPEC.md — ALL of: Design System (hand-rolled CSS, mono-for-data/sans-for-chrome, weights 400/600), Spacing Scale, Typography, Color (60/30/10, accent reserved to nudge + live-poll dot, semantic state colors), State Treatments table, Copywriting Contract (every copy string verbatim)
    - .planning/phases/01-foundation-stats-dashboard/01-RESEARCH.md § "Code Examples" → "Typed stats query + usage"
    - contract.md § 5 (StatsResult fields), § 7 (errors-on-200)
    - src/transport/errors.ts (classify / ApiError) and src/hooks/useStatsPoll.ts from this phase
  </read_first>
  <action>
    Write src/views/StatsDashboard.tsx consuming useStatsPoll. Render the four stat cards (Event count → eventCount, Max levId → maxLevId, DB version → dbVersion, Pinned strfry version → pinnedStrfryVersion) in a 2×2 grid at ≥ md width, single column below — Label in sans 13/600 muted, Value in mono 28/600 primary. Large integers (eventCount, maxLevId) formatted with Intl.NumberFormat. Render pinnedStrfryVersion as escaped plaintext via JSX interpolation — never dangerouslySetInnerHTML.

    Implement the COMPLETE distinct, non-blank state set from the UI-SPEC State Treatments table, each branched off classify() (ApiError union from 01-02) plus the loaded/empty/paused states: Loaded/live (cards + live-poll accent dot when polling and tab visible); Empty corpus (eventCount===0 renders cards with value 0 + calm caption "No events in corpus yet" / body copy — NEUTRAL, not an error); Corpus changed (non-intrusive teal nudge badge "Corpus changed — refresh to update.", dismissible, does NOT auto-refetch); Poll paused (accent dot dimmed/neutral + optional "Paused (tab hidden)"); INVALID_CURSOR (amber "Pagination expired — reloading from the top."); TOO_MANY_AUTHORS (amber "Too many authors in one request — narrowing the batch."); 503 not-ready after first connect (amber "Relay is warming up — retrying…"); 413 (amber "Request too large — trimming and retrying."); validation (amber, server message verbatim); internal (red "Something went wrong reading the corpus. Retrying shortly." — generic, no internals); network (red "Can't reach the relay…" with the setup hint). Use EXACTLY the UI-SPEC copy strings. Every state pairs color with a text label and/or shape (dot/badge) — color is never the sole signal.

    Add the "Refresh stats" CTA (the ONLY action this phase) that re-pulls stats on demand and acknowledges the nudge.

    Write src/views/StatsDashboard.module.css (hand-rolled, consuming the tokens.css custom properties from 01-01). The teal accent appears in EXACTLY two places: the corpus-changed nudge and the live-poll active dot — nowhere else.

    Modify src/App.tsx to mount <StatsDashboard /> after waitForReady resolves (replacing the raw stats render from 01-01/01-02).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; test -d dist && ! grep -rq "dangerouslySetInnerHTML" src/ && grep -q "Intl.NumberFormat" src/views/StatsDashboard.tsx && grep -q "Corpus changed" src/views/StatsDashboard.tsx && echo DASH_OK</automated>
    <human-check>Run `npm run dev` against the live lens: confirm the 2×2 stat cards render with real values (mono data / sans labels), the live-poll dot animates in teal while the tab is visible, switching tabs dims the dot (poll-paused), and — if the corpus changes (or with a forced maxLevId bump) — the teal "Corpus changed — refresh to update." nudge appears without auto-refetching until Refresh stats is clicked. Confirm eventCount=0 reads as a calm "No events in corpus yet", not an error.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces dist/
    - All four StatsResult scalars render; integers use Intl.NumberFormat
    - No dangerouslySetInnerHTML anywhere in src/
    - Every UI-SPEC state treatment is present with its verbatim copy string; each pairs color with label/shape
    - eventCount=0 renders neutral (no amber/red); INTERNAL state shows generic copy (no raw server message)
    - Teal accent appears only on the nudge and the live-poll dot
    - "Refresh stats" is the only CTA and re-pulls on demand
    - Manual: live dashboard polls, pauses on hidden tab, nudges on change without auto-refetch
  </acceptance_criteria>
  <done>An analyst launches the tool and watches live corpus stats — four typed scalars, a seconds-scale poll that pauses on a hidden tab, and an honest non-intrusive "corpus changed" nudge — with a complete distinct-state UI and no auto-refetch. The phase goal is true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend scalars → rendered UI | `pinnedStrfryVersion` (String) and numeric scalars cross into JSX; must render as escaped text |
| backend internals → error states | INTERNAL state must stay generic; validation messages are user-safe to show |

## STRIDE Threat Register (ASVS L1; block on `high`)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering (XSS) | StatsDashboard rendering `pinnedStrfryVersion` | mitigate | Render via JSX interpolation (React escapes by default); never `dangerouslySetInnerHTML`. (V5) |
| T-01-08 | Information Disclosure | INTERNAL error state copy | mitigate | Show the generic UI-SPEC string "Something went wrong reading the corpus. Retrying shortly."; never echo raw server internals. Validation messages shown verbatim are user-safe per contract § 7. (V7) |
| T-01-09 | Denial of Service (self-inflicted) | poll cadence in `useStatsPoll` | mitigate | Seconds-scale interval, pause on hidden tab, nudge-flag-only (no auto-refetch). (V7) |

No `high`-severity threats: only backend-controlled scalars rendered (escaped), no user input this phase.
</threat_model>

<verification>
- `npm run build` exits 0 and produces dist/
- useStatsPoll: setTimeout (no setInterval), Page Visibility pause, nudge-flag-only
- StatsDashboard: four scalars, Intl.NumberFormat, no dangerouslySetInnerHTML, all UI-SPEC states + copy, teal only on nudge + dot
- App mounts StatsDashboard post-readiness
- Manual: live polling, hidden-tab pause, change nudge without auto-refetch, empty-corpus calm
</verification>

<success_criteria>
The stats dashboard shows live eventCount/maxLevId/dbVersion/pinnedStrfryVersion from codegen-typed data; polling is seconds-scale, pauses on a hidden tab, and surfaces a non-intrusive corpus-changed nudge without aggressive auto-refetch; every state is distinct and non-blank with the approved copy and color discipline (STATS-01, STATS-02).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-stats-dashboard/01-03-SUMMARY.md` when done.
</output>
