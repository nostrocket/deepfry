---
phase: 02-suspect-entry-drill-down-core
plan: 03
type: tdd
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/analysis/thresholds.ts
  - src/analysis/rate.ts
  - src/analysis/rate.test.ts
  - src/views/RatePanel.tsx
  - src/views/RatePanel.module.css
  - src/views/AuthorDrillDown.tsx
files_modified_shared_note: "AuthorDrillDown.tsx is also written in 02-02; this plan ADDS the RatePanel mount only. Wave 3 (after 02-02) so there is no same-wave file conflict."
autonomous: false
requirements: [DRILL-01, DRILL-05]

must_haves:
  truths:
    - "analyzeRate over createdAt timestamps containing a tight cluster (>= minEvents within windowSec) returns burstDetected: true (DRILL-01)"
    - "analyzeRate over sparse/quiet timestamps returns burstDetected: false — which the panel presents as INCONCLUSIVE, never 'clean'"
    - "analyzeRate bounds-checks each createdAt with isSaneTs (Number.isSafeInteger && 0..4102444800); out-of-range values are counted in rejectedCount, never silently dropped or mis-computed (DRILL-01 forgeable-createdAt)"
    - "analyzeRate with fewer than 2 sane timestamps returns burstDetected:false, bins:[], tightestIntervalSec:null (no crash, no negative interval)"
    - "The RatePanel renders hand-rolled CSS/SVG bars scaled to the max bin; a detected burst is marked with an amber tint AND the text label 'burst'"
    - "The forgeable-createdAt caveat is ALWAYS rendered beside the rate panel, non-dismissible"
    - "The window-size indicator is rendered on the rate-panel surface too (DRILL-05 — every signal surface)"
  prohibitions:
    - "rate.ts / thresholds.ts import nothing from React, urql, src/transport, or src/queries (pure modules, Node vitest env)"
    - "RateResult has no field named 'clean'/'ok'/'safe'; burstDetected:false is structurally inconclusive, never a clean verdict (asymmetry)"
    - "No green/success color and no teal accent anywhere in the rate panel (only neutral + amber-for-burst)"
    - "The rate panel never renders without BOTH the forgeable caveat AND the window indicator present"
    - "createdAt out-of-safe-range is flagged (rejectedCount), never used to compute a time range or interval"
  artifacts:
    - path: "src/analysis/thresholds.ts"
      provides: "BURST tunable constants { windowSec, minEvents, binSec } — single tunable home"
      exports: ["BURST"]
    - path: "src/analysis/rate.ts"
      provides: "analyzeRate(createdAts) → RateResult (asymmetric, bounds-checked); isSaneTs; RateResult type"
      exports: ["analyzeRate", "isSaneTs", "RateResult"]
    - path: "src/analysis/rate.test.ts"
      provides: "Vitest coverage: burst-present, quiet, <2 events, forged/out-of-range rejection, negative-interval guard"
      contains: "describe("
    - path: "src/views/RatePanel.tsx"
      provides: "Posting-rate panel: CSS/SVG bars, amber burst marker + label, persistent forgeable caveat, co-located WindowIndicator"
      exports: ["RatePanel"]
  key_links:
    - from: "src/analysis/rate.ts"
      to: "src/analysis/thresholds.ts"
      via: "import { BURST } from './thresholds' — windowSec/minEvents/binSec drive the sliding-window burst + binning"
      pattern: "BURST"
    - from: "src/views/RatePanel.tsx"
      to: "src/analysis/rate.ts"
      via: "analyzeRate(events.map(e => e.createdAt)) → RateResult drives the bars + burst marker"
      pattern: "analyzeRate"
    - from: "src/views/AuthorDrillDown.tsx"
      to: "src/views/RatePanel.tsx"
      via: "renders <RatePanel events={events} windowMeta={windowMeta} /> on the drill-down, re-running as the window widens"
      pattern: "RatePanel"
---

<objective>
Refine the 02-02 drill-down slice by adding the spam signal that gives the timeline its forensic
edge: an asymmetric posting-rate / burst analyzer (DRILL-01). This is a pure, bounds-checked module
over the fetched window's author-claimed `createdAt` values, surfaced as hand-rolled CSS/SVG rate
bars with a permanent "createdAt is author-claimed and forgeable" caveat and its own non-removable
window-size indicator. After this plan a real user can do something new: read a burst signal —
correctly framed as suspicious-when-present, inconclusive-when-absent — against the same honest
denominator. Analytics re-derive live as Load more widens the window (the 02-02 hook drives it).

Purpose: Lock the asymmetric-burst epistemics (no clean state, forgeable caveat, bounds-checked
forged timestamps) as pure unit-tested logic, then render it within the project's CSS-token /
no-chart-lib constraints, completing the Phase 2 core-value slice.
Output: src/analysis/{thresholds,rate,rate.test}.ts, src/views/RatePanel.tsx (+ CSS), and a small
edit to AuthorDrillDown.tsx mounting the panel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-suspect-entry-drill-down-core/02-RESEARCH.md
@.planning/phases/02-suspect-entry-drill-down-core/02-PATTERNS.md
@.planning/phases/02-suspect-entry-drill-down-core/02-UI-SPEC.md
@src/hooks/useStatsPoll.ts
@src/views/StatsDashboard.tsx
@src/views/StatsDashboard.module.css
@src/styles/tokens.css
@src/views/WindowIndicator.tsx
@src/views/AuthorDrillDown.tsx
@src/hooks/useAuthorWindow.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: thresholds.ts + failing rate analyzer tests (RED)</name>
  <read_first>
    - .planning/phases/02-suspect-entry-drill-down-core/02-RESEARCH.md (Pattern 3 lines 289-350 — analyzeRate body, isSaneTs bounds, thresholds; Pitfall 3 lines 440-444)
    - .planning/phases/02-suspect-entry-drill-down-core/02-PATTERNS.md ("src/analysis/rate.ts" + "src/analysis/thresholds.ts" sections; "Test convention")
    - src/hooks/useStatsPoll.ts (POLL_INTERVAL_MS — the single-named-tunable convention; shouldNudge — pure-function-extracted-for-test convention)
    - src/hooks/useStatsPoll.test.ts (vitest convention)
    - src/transport/errors.ts (named-interface / discriminated-result convention)
  </read_first>
  <files>src/analysis/thresholds.ts, src/analysis/rate.test.ts, src/analysis/rate.ts</files>
  <behavior>
    Test cases (assert on RateResult, per DRILL-01 asymmetry + bounds-checking):
    - Tight cluster: BURST.minEvents timestamps within BURST.windowSec seconds → burstDetected: true
    - Sparse: timestamps each > windowSec apart → burstDetected: false; assert RateResult has NO 'clean'/'ok'/'safe' field (asymmetry is structural)
    - Fewer than 2 sane timestamps (0 and 1 element) → burstDetected:false, bins:[], tightestIntervalSec:null (no throw)
    - Forged/out-of-range: include a negative value, a value > 4_102_444_800 (post-2100), and a value beyond Number.MAX_SAFE_INTEGER → each excluded from analysis and counted in rejectedCount; analyzedCount counts only sane ones
    - Mixed forged + valid burst: rejectedCount reflects the forged ones; burstDetected still derives correctly from the sane subset; tightestIntervalSec is never negative (timestamps sorted ascending before interval math)
    - tightestIntervalSec equals the smallest gap between consecutive sane timestamps
  </behavior>
  <action>
    Create `src/analysis/thresholds.ts` exporting `export const BURST = { windowSec: 60, minEvents:
    5, binSec: 3600 } as const` (RESEARCH defaults) with a doc-comment noting these are sane
    literature-grounded defaults and corpus-validation is deferred to Phase 3 (STATE) — the honesty
    posture (denominator + caveat) holds regardless of threshold choice. This is the single tunable
    home (CONTEXT discretion), mirroring the POLL_INTERVAL_MS convention.
    Create `src/analysis/rate.test.ts` importing `analyzeRate` (and `RateResult`, `isSaneTs`) from
    `./rate` and `BURST` from `./thresholds`; write the cases in <behavior> (vitest describe/it/
    expect, Node env). Create a minimal `src/analysis/rate.ts` stub exporting `analyzeRate` that
    returns a fixed empty RateResult so the suite compiles and runs RED. Do NOT implement the real
    interval/burst math here (Task 2). Keep success-criteria literals out of prose comments.
  </action>
  <verify>
    <automated>npm test -- src/analysis/rate.test.ts 2>&1 | grep -Eiq 'fail|✗|×|expected' && echo RED-OK</automated>
  </verify>
  <done>thresholds.ts exports BURST; rate.test.ts runs and FAILS (RED) against the stub; both files are pure (no React/transport imports).</done>
  <acceptance_criteria>
    - `npm test -- src/analysis/rate.test.ts` exits non-zero (RED)
    - `grep -q "windowSec" src/analysis/thresholds.ts` and `grep -q "minEvents" src/analysis/thresholds.ts` and `grep -q "binSec" src/analysis/thresholds.ts`
    - rate.test.ts contains a `describe(` block and asserts burstDetected true/false + rejectedCount on forged input
    - `grep -LqE "from 'react'|@urql|transport/|queries/" src/analysis/rate.ts src/analysis/thresholds.ts` (pure modules — no UI/transport imports)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: implement analyzeRate — asymmetric, bounds-checked (GREEN)</name>
  <read_first>
    - .planning/phases/02-suspect-entry-drill-down-core/02-RESEARCH.md (Pattern 3 full body lines 289-340; Pitfall 3; Anti-Patterns line 405 — no green/clean state)
    - .planning/phases/02-suspect-entry-drill-down-core/02-PATTERNS.md ("src/analysis/rate.ts" — named result interface, bounds-check-don't-mis-compute, asymmetry-is-structural)
    - src/analysis/rate.test.ts (the contract to satisfy)
    - src/transport/errors.ts (doc-comment-first + named-interface convention)
  </read_first>
  <files>src/analysis/rate.ts</files>
  <action>
    Replace the stub. Export `RateResult { analyzedCount: number; rejectedCount: number; bins:
    { start: number; count: number }[]; burstDetected: boolean; tightestIntervalSec: number | null
    }` — note there is NO 'clean'/'ok'/'safe' field (asymmetry is structural). Export
    `const MIN_TS = 0`, `const MAX_TS = 4_102_444_800` (2100-01-01), and `isSaneTs(t: number):
    boolean = Number.isSafeInteger(t) && t >= MIN_TS && t <= MAX_TS`. Implement `analyzeRate(
    createdAts: number[]): RateResult`:
    (1) `sane = createdAts.filter(isSaneTs).sort((a,b) => a-b)`; `rejectedCount = createdAts.length
        - sane.length` (forged/out-of-range flagged, never silently dropped — DRILL-01 / Pitfall 3).
    (2) if `sane.length < 2` → return analyzedCount=sane.length, rejectedCount, bins:[],
        burstDetected:false, tightestIntervalSec:null.
    (3) tightestIntervalSec = min gap between consecutive sane (ascending — never negative).
    (4) burstDetected: a sliding window over sorted sane timestamps — for each i, advance j while
        `sane[j] - sane[i] <= BURST.windowSec`; if `j - i >= BURST.minEvents` → burst = true, break.
    (5) bins: a `binByInterval(sane, BURST.binSec)` helper grouping sane timestamps into fixed-width
        bins ({ start, count }) for the CSS/SVG bars.
    Return all fields. Doc-comment cites DRILL-01 (asymmetric — burst suspicious, quiet
    inconclusive), the forgeable/64-bit createdAt bounds rationale, and that thresholds.ts is the
    tunable home. Pure module — no React/transport/network imports.
  </action>
  <verify>
    <automated>npm test -- src/analysis/rate.test.ts</automated>
  </verify>
  <done>All rate.test.ts cases pass (GREEN): burst detection, quiet → inconclusive, <2-event guard, forged-timestamp rejection into rejectedCount, non-negative tightestIntervalSec. `npm run build` typechecks.</done>
  <acceptance_criteria>
    - `npm test -- src/analysis/rate.test.ts` exits 0 (all pass)
    - `grep -q "isSaneTs" src/analysis/rate.ts` and `grep -q "4_102_444_800\|4102444800" src/analysis/rate.ts` (bounds check present)
    - `grep -q "rejectedCount" src/analysis/rate.ts` (forged values counted, not dropped)
    - `grep -vE '^\s*(//|\*|/\*)' src/analysis/rate.ts | grep -ciE '\bclean\b|\bsafe\b' | grep -qx 0` (no clean/safe verdict field — asymmetry)
    - `npx tsc -b` reports no type errors for src/analysis/
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RatePanel (CSS/SVG bars + caveat + indicator) mounted in the drill-down</name>
  <read_first>
    - src/views/StatsDashboard.tsx (StatCards map-render shape lines 81-99; formatInt)
    - src/views/StatsDashboard.module.css (semantic .recoverable amber :282-292; .stateDot; token usage)
    - src/styles/tokens.css (the only permitted tokens — no new tokens; --text-muted, --border, --recoverable)
    - src/views/WindowIndicator.tsx (the DRILL-05 component to co-locate on this surface)
    - src/views/AuthorDrillDown.tsx (the view to mount RatePanel into — created in 02-02; add the panel beside the timeline)
    - src/hooks/useAuthorWindow.ts (events + windowMeta shapes consumed by the panel)
    - src/analysis/rate.ts + src/analysis/thresholds.ts (analyzeRate + RateResult)
    - .planning/phases/02-suspect-entry-drill-down-core/02-UI-SPEC.md (Rate/burst-panel color rule; Forgeable-createdAt caveat; Copywriting Contract — VERBATIM strings: "Posting rate", "burst", caveat line)
    - .planning/phases/02-suspect-entry-drill-down-core/02-PATTERNS.md ("RatePanel.tsx" section; "No Analog Found" — SVG bars are new)
  </read_first>
  <files>src/views/RatePanel.tsx, src/views/RatePanel.module.css, src/views/AuthorDrillDown.tsx</files>
  <action>
    (a) `src/views/RatePanel.tsx` — props `{ events: WindowEvent[]; windowMeta: WindowMeta }`.
    Compute `const rate = analyzeRate(events.map(e => e.createdAt))`. Panel title "Posting rate"
    (label). Render hand-rolled bars from `rate.bins` (map-render like StatCards): each bin a
    `<div>` or `<svg>` rect whose height scales to the max bin count; neutral `--text-muted` fill
    on a `--border` baseline. Asymmetric color rule (load-bearing): if `rate.burstDetected`, mark
    the relevant region with an amber (`--recoverable`) tint AND a text label "burst" (color is
    never the sole signal — pair with the label + the spike shape). NO green, NO success color, NO
    teal accent anywhere in this panel; quiet bars stay neutral and must NOT read as "clean".
    If `rate.rejectedCount > 0`, show a small muted note that N timestamps were out-of-range and
    excluded (flagged, not mis-computed). ALWAYS render, beside the chart, the non-dismissible
    forgeable caveat with the VERBATIM string "Timing is author-claimed and forgeable — a burst
    suggests suspicious automation, but quiet timing does not prove the author is clean." ALWAYS
    render `<WindowIndicator meta={windowMeta} />` on this surface (DRILL-05 — every signal
    surface). All values escaped plaintext. Sibling CSS module: tokens only; neutral bars, amber
    burst tint via the .recoverable semantic; no accent, no green.

    (b) Edit `src/views/AuthorDrillDown.tsx` — in the loaded timeline branch (≥1 event), render
    `<RatePanel events={events} windowMeta={windowMeta} />` beside/below the timeline so it
    re-derives live as Load more widens the window. Do not add the panel to the zero-match empty
    branch (no events to analyze there; the timeline-surface indicator already covers DRILL-05 for
    that state). Keep the existing imports/structure from 02-02 intact.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>RatePanel renders CSS/SVG bars, an amber+label burst marker when burstDetected, a rejectedCount note when forged timestamps exist, the always-present forgeable caveat, and a co-located window indicator; it is mounted in the drill-down's loaded timeline branch and re-derives as the window widens. `npm run build` passes. No green/accent in the panel; no dangerouslySetInnerHTML.</done>
  <acceptance_criteria>
    - `grep -q "analyzeRate" src/views/RatePanel.tsx` and `grep -q "WindowIndicator" src/views/RatePanel.tsx` (analyzer driven + indicator co-located)
    - `grep -q "Timing is author-claimed and forgeable" src/views/RatePanel.tsx` (verbatim caveat) and `grep -q "Posting rate" src/views/RatePanel.tsx`
    - `grep -q "burst" src/views/RatePanel.tsx` (burst text label — color not sole signal)
    - `grep -Lq "accent" src/views/RatePanel.module.css` and `grep -ciE 'success|green' src/views/RatePanel.module.css | grep -qx 0` (no accent, no green/success color)
    - `grep -L "dangerouslySetInnerHTML" src/views/RatePanel.tsx` lists the file (no raw HTML)
    - `grep -q "RatePanel" src/views/AuthorDrillDown.tsx` (mounted in the drill-down)
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — asymmetric burst signal + honesty posture</name>
  <what-built>The completed Phase 2 core slice: the drill-down now shows the posting-rate / burst
  panel (hand-rolled CSS/SVG bars) with asymmetric interpretation, the permanent forgeable caveat,
  the rejected-timestamp note, and its own window indicator — re-deriving live as Load more widens
  the window. Run against the live lens.</what-built>
  <how-to-verify>
    1. `npm run dev`; inspect an author with many events. Confirm a "Posting rate" panel renders
       bars beside/below the timeline, with NO green/teal anywhere in the panel.
    2. Confirm the caveat "Timing is author-claimed and forgeable — a burst suggests suspicious
       automation, but quiet timing does not prove the author is clean." is always shown beside the
       panel and cannot be dismissed.
    3. Confirm the window indicator is present on the rate-panel surface too (DRILL-05 holds on
       every signal surface), and that it re-derives when you click "Load more".
    4. If the author has a tight burst, confirm it is marked amber + labeled "burst". For a quiet
       author, confirm the panel stays neutral and is NOT presented as "clean" anywhere.
    5. (If reproducible) an author with an obviously forged far-future/negative createdAt: confirm a
       muted "out-of-range excluded" note appears and the time range / bars are not corrupted.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. green leak, caveat missing, indicator absent, forged timestamp corrupts the chart).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lens event data → analyzer | Author-claimed, forgeable `createdAt` (64-bit) feeds the rate math |
| analyzer → render | RateResult drives bars + the burst verdict shown to the analyst |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering / DoS | forged `createdAt` (far-future, negative, > MAX_SAFE_INTEGER) | mitigate | isSaneTs bounds-check (Number.isSafeInteger && 0..4102444800); out-of-range counted in rejectedCount and surfaced, never used in range/interval math; sort-before-interval prevents negative gaps. Unit-tested. |
| T-02-11 | Repudiation / false reasoning | quiet timing read as "clean author" | mitigate | Asymmetric analyzer (no clean/safe field — structural); permanent forgeable caveat beside the panel; no green/success color; window indicator frames N as a denominator. |
| T-02-12 | Tampering (XSS) | rendered analyzer/content values | mitigate | All panel text escaped plaintext via JSX; no dangerouslySetInnerHTML (grep-gated). |
</threat_model>

<verification>
- `npm test -- src/analysis/` passes (burst, quiet, <2-event, forged-rejection, non-negative interval).
- grep gates: rate.ts pure + bounds-checked + no clean/safe field; RatePanel has the verbatim caveat + burst label + window indicator, no accent/green, no raw HTML.
- `npm run build` passes; RatePanel mounted in the drill-down.
- Human checkpoint (Task 4) confirms the asymmetric signal + honesty posture on the live lens.
</verification>

<success_criteria>
- DRILL-01: timeline (02-02) + asymmetric burst/rate indicator with the persistent forgeable caveat; burst present = suspicious (amber + label), burst absent = inconclusive (neutral, never clean); forged timestamps flagged not mis-computed.
- DRILL-05: the window-size indicator is present on the rate-panel surface as well as the timeline (every signal surface).
</success_criteria>

<output>
Create `.planning/phases/02-suspect-entry-drill-down-core/02-03-SUMMARY.md` when done.
</output>
