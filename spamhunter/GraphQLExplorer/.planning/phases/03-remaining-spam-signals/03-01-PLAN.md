---
phase: 03-remaining-spam-signals
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/analysis/thresholds.ts
  - src/analysis/nearDup.ts
  - src/analysis/nearDup.test.ts
  - src/analysis/tags.ts
  - src/analysis/tags.test.ts
  - src/analysis/kinds.ts
  - src/analysis/kinds.test.ts
  - src/analysis/kindNames.ts
autonomous: true
requirements: [DRILL-02, DRILL-03, DRILL-04]

must_haves:
  truths:
    - "nearDup(events) groups exact-duplicate and near-duplicate posts (two-stage: normalized-hash bucket → word-shingle k=3 Jaccard ≥0.8 → union-find clusters) over the fetched window (DRILL-02)"
    - "analyzeTags(events) aggregates p/e/t tags into top-N mentions, top-N hashtags, an e-reference count, per-event high-tag-count outliers, and per-event massMention/stuffing signal flags (p-mention fan-out over TAGS.massMention, t-hashtag count over TAGS.stuffing), counting malformed tag rows rather than throwing (DRILL-03)"
    - "analyzeKinds(events) produces a kind histogram and an out-of-range count, reusing isSaneTs for createdAt and a Number.isSafeInteger(kind) && kind >= 0 bounds-check for kind (DRILL-04)"
    - "Each analyzer returns inconclusive empty results for degenerate input (0 or 1 comparable items) without crashing or computing negative/NaN values"
    - "Every analyzer test asserts the result object exposes no clean/ok/safe key (asymmetry is structural)"
  artifacts:
    - path: "src/analysis/nearDup.ts"
      provides: "Pure two-stage near-duplicate detector → NearDupResult; normalizeContent/shingles/jaccard/DSU helpers"
      exports: ["nearDup", "NearDupResult", "normalizeContent", "shingles", "jaccard"]
      min_lines: 60
    - path: "src/analysis/tags.ts"
      provides: "Pure p/e/t aggregator → TagsResult (with per-event massMention/stuffing signal flags driving the UI-SPEC amber 'high mention fan-out' / 'hashtag stuffing' labels)"
      exports: ["analyzeTags", "TagsResult"]
      min_lines: 40
    - path: "src/analysis/kinds.ts"
      provides: "Pure kind histogram + bounds flag → KindsResult"
      exports: ["analyzeKinds", "KindsResult"]
      min_lines: 30
    - path: "src/analysis/kindNames.ts"
      provides: "NIP kind-number → human name lookup table"
      exports: ["KIND_NAMES"]
    - path: "src/analysis/thresholds.ts"
      provides: "NEAR_DUP and TAGS tunables alongside the existing BURST block"
      contains: "NEAR_DUP"
    - path: "src/analysis/nearDup.test.ts"
      provides: "TDD fixtures for nearDup (exact bucket, near-dup cluster, transitive union-find, degenerate input, asymmetry)"
    - path: "src/analysis/tags.test.ts"
      provides: "TDD fixtures for analyzeTags (p/e/t counts, malformed rows, high-tag-count outlier, massMention flag, stuffing flag, asymmetry)"
    - path: "src/analysis/kinds.test.ts"
      provides: "TDD fixtures for analyzekinds (histogram, forged kind/createdAt flagged, asymmetry)"
  prohibitions:
    - "NO clean/ok/safe field on any analyzer result interface (structural asymmetry — mirror RateResult)"
    - "NO external runtime dependency (union-find, shingles, Jaccard are hand-rolled per CONTEXT/RESEARCH)"
    - "NO re-implementation of isSaneTs — import it from ./rate"
    - "NO stripping of URLs/mentions/punctuation in normalizeContent (CONTEXT: over-merges distinct spam variants)"
    - "NO throwing on a malformed tag row or out-of-range kind/createdAt — count/flag and continue"
    - "NO React/transport/network import in any analysis/ module (pure functions only)"
---

<objective>
Build the three pure, zero-dependency analyzers that power Phase 3's forensic panels — `nearDup` (DRILL-02), `analyzeTags` (DRILL-03), and `analyzeKinds` (DRILL-04) — plus the `KIND_NAMES` lookup and the `NEAR_DUP`/`TAGS` threshold additions. TDD (RED→GREEN), unit-tested against fixtures, zero network, mirroring the Phase 2 `rate.ts` + `rate.test.ts` conventions exactly.

Purpose: The analyzers are the load-bearing logic for the whole phase; building and testing them in isolation (zero transport coupling) keeps them deterministic and lets slice 03-02 compose them into panels without re-deciding algorithm behavior. This is a vertical-slice prerequisite: slice 03-02's first task is a failing panel test that these analyzers make green.

Output: `src/analysis/nearDup.ts`, `tags.ts`, `kinds.ts`, `kindNames.ts`, their `.test.ts` siblings, and the extended `thresholds.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-spam-signals/03-CONTEXT.md
@.planning/phases/03-remaining-spam-signals/03-RESEARCH.md
@.planning/phases/03-remaining-spam-signals/03-PATTERNS.md

# The single pure-analyzer blueprint — shape, header, isSaneTs, degenerate guard, no-clean-field.
@src/analysis/rate.ts
@src/analysis/rate.test.ts
@src/analysis/thresholds.ts
# WindowEvent shape the analyzers consume (gains `tags` in slice 03-02; type defensively against string[][]).
@src/hooks/useAuthorWindow.ts
</context>

## Artifacts this phase produces (slice 03-01)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `NEAR_DUP` (in `thresholds.ts`) | const | `{ k: 3, jaccard: 0.8 } as const` — corpus-validatable default |
| `TAGS` (in `thresholds.ts`) | const | `{ highTagCount, massMention, stuffing } as const` — conservative defaults |
| `normalizeContent(s)` | fn | NFC + lowercase + collapse whitespace + trim |
| `shingles(normalized, k)` | fn | word-shingle Set; `< k` words → whole-text shingle |
| `jaccard(a, b)` | fn | Jaccard over two Sets |
| `nearDup(events)` | fn | → `NearDupResult` |
| `NearDupResult` | interface | `analyzedCount; clusters; duplicateCount` — no clean field |
| `analyzeTags(events)` | fn | → `TagsResult` |
| `TagsResult` | interface | `analyzedCount; malformedTagRows; topMentions; topHashtags; eventRefCount; outlierEvents` (each outlier entry carries `massMention`/`stuffing` flags); plus aggregate `massMention`/`stuffing` booleans — no clean field |
| `analyzeKinds(events)` | fn | → `KindsResult` |
| `KindsResult` | interface | `analyzedCount; outOfRangeCount; bins` — no clean field |
| `KIND_NAMES` (in `kindNames.ts`) | const | NIP kind-number → name lookup |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend thresholds.ts and build the nearDup two-stage analyzer (DRILL-02)</name>
  <files>src/analysis/thresholds.ts, src/analysis/nearDup.ts, src/analysis/nearDup.test.ts</files>
  <read_first>
    - src/analysis/thresholds.ts (EDIT target — the `BURST = {...} as const` block at lines 13-17 is the template for the additive NEAR_DUP/TAGS blocks; mirror its `as const` + literature-default + corpus-validation-deferred comment style)
    - src/analysis/rate.ts (ANALOG — module header lines 1-20 state PURE/no-React + the asymmetry rule; RateResult interface lines 37-48 = named result with denominator + flagged-but-counted field + NO clean key; the `< 2` degenerate guard at lines 91-99)
    - src/analysis/rate.test.ts (ANALOG — vitest Node-env imports lines 13-15; the MANDATORY no-clean-field asymmetry test lines 35-43; degenerate-input test style)
    - .planning/phases/03-remaining-spam-signals/03-RESEARCH.md (Pattern 1, lines 181-228 — the normalize→shingle→jaccard→DSU pipeline source, copied from here since there is no codebase analog for this internal logic; Pitfall 1 lines 309-313 for the O(n²) bound)
  </read_first>
  <behavior>
    - Exact-duplicate posts (identical after NFC + lowercase + whitespace-collapse + trim) land in one cluster tagged kind 'exact'.
    - Posts differing only in case/whitespace/Unicode normal form are treated as exact duplicates (normalizeContent collapses them).
    - Two posts with word-shingle (k=3) Jaccard >= NEAR_DUP.jaccard land in one cluster tagged kind 'near'.
    - Transitive near-duplicates (A≈B, B≈C, A≉C) are unioned into ONE cluster via union-find — order-independent.
    - Posts with no duplicate and no near-duplicate produce NO cluster (not a singleton cluster).
    - duplicateCount = total member count across all clusters (the "X" in "X of N fetched").
    - 0 events → { analyzedCount: 0, clusters: [], duplicateCount: 0 }; 1 event → analyzedCount 1, no clusters (nothing to compare), no crash.
    - Short posts (< k words) fall back to a whole-text shingle (not an empty Set) so they can still match an identical short post.
    - Result object exposes NO clean/ok/safe key.
  </behavior>
  <action>
    Append to `thresholds.ts` (additive — do not modify the existing `BURST` export): `export const NEAR_DUP = { k: 3, jaccard: 0.8 } as const` and `export const TAGS = { highTagCount: 20, massMention: 20, stuffing: 15 } as const`, each preceded by a comment matching the existing block's style (literature-grounded default, corpus-validation deferred per STATE Phase-3 note, honesty posture is threshold-independent). The TAGS numbers are conservative discretion defaults (RESEARCH A3) documented as tunable.

    Create `nearDup.ts` as a PURE module (mirror `rate.ts` header lines 1-20: state PURE / no React / no transport, state the asymmetry — suspicious-when-present, inconclusive-when-absent, NO clean/ok/safe field). `import { NEAR_DUP } from './thresholds'`. Accept `events: { id: string; content: string }[]` (a structural subset of WindowEvent so the test fixtures stay light). Export the helpers `normalizeContent(s: string): string` (NFC + toLowerCase + `replace(/\s+/g, ' ')` + trim — do NOT strip URLs/mentions/punctuation per CONTEXT), `shingles(normalized: string, k: number): Set<string>` (split on space, filter empties; if word count `< k` add the whole joined text as a single shingle, else slide a k-window), and `jaccard(a: Set<string>, b: Set<string>): number`. Implement stage 1 as a `Map<string, string[]>` bucketing event ids by `normalizeContent(content)` (treat empty-normalized content as its own bucket but do NOT cluster empties as near-duplicates of substantive posts — Pitfall 2). Implement stage 2 with a `DSU` disjoint-set class (find with path-halving, union), precomputing each event's shingle Set ONCE, comparing pairs i<j and `union(i, j)` when `jaccard >= NEAR_DUP.jaccard`; short-circuit a pair early when `|a.size - b.size| / Math.max(a.size, b.size) > (1 - NEAR_DUP.jaccard)` (size disparity makes the cutoff impossible — Pitfall 1 bound). Define `NearDupResult { analyzedCount: number; clusters: { kind: 'exact' | 'near'; memberIds: string[]; count: number }[]; duplicateCount: number }` (named like RateResult; NO clean field). A cluster requires `>= 2` members; tag a cluster 'exact' when all members share one stage-1 bucket, else 'near'. Early-return `{ analyzedCount, clusters: [], duplicateCount: 0 }` for `< 2` events (mirror rate.ts:91-99).

    Create `nearDup.test.ts` (vitest, Node env, pure — mirror `rate.test.ts:13-15` imports, importing `NEAR_DUP` from `./thresholds` rather than hard-coding numbers). Cover: exact-duplicate bucketing; case/whitespace/NFC-only differences collapse to exact; a near-duplicate pair over the Jaccard cutoff; the transitive-chain union-find case (A≈B, B≈C → one cluster); a non-duplicate pair → no cluster; `duplicateCount` equals summed members; the degenerate 0-event and 1-event inconclusive results; the short-post (< k words) whole-text-shingle match; and the MANDATORY no-clean/ok/safe-key assertion (copy `rate.test.ts:35-43` verbatim against a `NearDupResult`).
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npx vitest run src/analysis/nearDup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analysis/nearDup.test.ts` passes (all RED tests now GREEN).
    - `grep -nE 'NEAR_DUP|TAGS' src/analysis/thresholds.ts` shows both new `as const` exports AND the original `BURST` export is unchanged (`grep -c 'export const BURST' src/analysis/thresholds.ts` returns 1).
    - `grep -vE '^\s*(//|\*|/\*)' src/analysis/nearDup.ts | grep -cE "'clean'|'ok'|'safe'|clean:|ok:|safe:"` returns 0 (no clean/ok/safe field in code, comments excluded).
    - `grep -cE "from '\.\./hooks|from 'react'|transport|client" src/analysis/nearDup.ts` returns 0 (pure module, no React/transport import).
    - `grep -c 'strip' src/analysis/nearDup.ts` returns 0 (normalizeContent does not strip URLs/mentions/punctuation).
    - `nearDup.ts` exports `nearDup`, `NearDupResult`, `normalizeContent`, `shingles`, `jaccard`.
  </acceptance_criteria>
  <done>thresholds.ts carries additive NEAR_DUP + TAGS; nearDup is a pure two-stage union-find detector with no clean field; its test suite (exact/near/transitive/degenerate/asymmetry) passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the analyzeTags aggregator (DRILL-03)</name>
  <files>src/analysis/tags.ts, src/analysis/tags.test.ts</files>
  <read_first>
    - src/analysis/rate.ts (ANALOG — the pure-analyzer scaffold; `rejectedCount` (lines 40, 87) is the "count-malformed-but-don't-throw" template for `malformedTagRows`)
    - src/analysis/rate.test.ts (ANALOG — vitest imports + the no-clean-field asymmetry test pattern)
    - .planning/phases/03-remaining-spam-signals/03-RESEARCH.md (Pattern 2 lines 230-247 = the TagsResult interface already specified; Code Examples lines 346-365 = the defensive p/e/t read loop; Pitfall 3 lines 321-325 = malformed/hostile rows)
    - src/analysis/thresholds.ts (the TAGS block added in Task 1 — read TAGS.highTagCount/massMention/stuffing rather than hard-coding)
  </read_first>
  <behavior>
    - p tags increment a per-pubkey mention count; t tags increment a per-hashtag count; e tags increment a single eventRefCount.
    - topMentions / topHashtags are sorted descending by count and sliced to top-N (N = a documented constant, e.g. 10).
    - A tag row that is not an array, or whose tag[0] is not a string, is counted in malformedTagRows and skipped — never throws.
    - A p or t row whose tag[1] is not a string is counted malformed and skipped (no undefined entry in the top-N list).
    - An event whose total tag count exceeds TAGS.highTagCount appears in outlierEvents with { id, tagCount, massMention, stuffing }.
    - An event whose p-mention (fan-out) count exceeds TAGS.massMention sets that outlier entry's massMention flag true; aggregate result.massMention is true if any event trips it. (Drives the UI-SPEC amber "high mention fan-out" label, UI-SPEC line 232.)
    - An event whose t-hashtag count exceeds TAGS.stuffing sets that outlier entry's stuffing flag true; aggregate result.stuffing is true if any event trips it. (Drives the UI-SPEC amber "hashtag stuffing" label, UI-SPEC line 233.)
    - An event may be an outlier via highTagCount, massMention, OR stuffing — any of the three lands it in outlierEvents (the flags disambiguate which signal fired).
    - 0 events → all-zero/empty result (massMention/stuffing false); analyzedCount counts events whose tags were read.
    - Result object exposes NO clean/ok/safe key.
  </behavior>
  <action>
    Create `tags.ts` as a PURE module (mirror `rate.ts` header + asymmetry note; NO React/transport import). Accept `events: { id: string; tags: string[][] }[]` (structural subset of the slice-02 WindowEvent). Define `TagsResult` extending RESEARCH lines 239-247 with the mass-mention/stuffing signal flags the UI-SPEC requires: `{ analyzedCount: number; malformedTagRows: number; topMentions: { value: string; count: number }[]; topHashtags: { value: string; count: number }[]; eventRefCount: number; outlierEvents: { id: string; tagCount: number; massMention: boolean; stuffing: boolean }[]; massMention: boolean; stuffing: boolean }` — NO clean field (the aggregate massMention/stuffing booleans are signal-present flags, NOT a clean/ok verdict). Implement the defensive read loop from RESEARCH Code Examples (lines 353-364): for each event, track a per-event total tag-row count, a per-event p-mention count, and a per-event t-hashtag count. Iterate `ev.tags`; guard `Array.isArray(tag) && typeof tag[0] === 'string'` (else `malformedTagRows++`, continue); for `tag[0] === 'e'` bump `eventRefCount` and continue; require `typeof tag[1] === 'string'` before counting a value (else malformed); `tag[0] === 'p'` bumps the global mentions `Map<string, number>` AND the per-event p-mention count, `tag[0] === 't'` bumps the global hashtags Map AND the per-event t-hashtag count. After the per-event loop, compute `const massMention = pCount > TAGS.massMention` and `const stuffing = tCount > TAGS.stuffing`; if the event's total tag-row count exceeds `TAGS.highTagCount` OR massMention OR stuffing, push `{ id, tagCount, massMention, stuffing }` to outlierEvents. Set the aggregate `result.massMention` / `result.stuffing` true if ANY event tripped the respective flag (false on 0 events). Produce topMentions/topHashtags by sorting the Maps' entries descending by count and slicing the top-N (declare `const TOP_N = 10` with a comment). Import `TAGS` from `./thresholds`. No degenerate crash on 0 events.

    Create `tags.test.ts` (vitest, Node env, mirror `rate.test.ts:13-15`). Cover: p/t counting + descending top-N order; e-reference summed into eventRefCount; malformed rows (`[]`, `['p']` with no value, a non-array row) counted into malformedTagRows and excluded from counts; the per-event high-tag-count outlier firing above `TAGS.highTagCount` (build the fixture from the constant, not a literal); a mass-mention case (an event with `> TAGS.massMention` p-rows) setting the outlier entry's `massMention` flag AND aggregate `result.massMention` true (fixture built from `TAGS.massMention`, not a literal); a hashtag-stuffing case (an event with `> TAGS.stuffing` t-rows) setting the entry's `stuffing` flag AND aggregate `result.stuffing` true (fixture from `TAGS.stuffing`); a benign window leaving both aggregate flags false; the 0-event empty result (both flags false); and the MANDATORY no-clean/ok/safe-key assertion.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npx vitest run src/analysis/tags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analysis/tags.test.ts` passes.
    - `grep -vE '^\s*(//|\*|/\*)' src/analysis/tags.ts | grep -cE "'clean'|'ok'|'safe'|clean:|ok:|safe:"` returns 0.
    - `grep -cE "from 'react'|transport|client" src/analysis/tags.ts` returns 0 (pure module).
    - `grep -c 'malformedTagRows' src/analysis/tags.ts` is >= 2 (defensive counting present, not throwing).
    - `grep -c 'throw' src/analysis/tags.ts` returns 0 (never throws on hostile input).
    - `grep -cE 'massMention|stuffing' src/analysis/tags.ts` is >= 2 (the mass-mention/hashtag-stuffing signal flags are wired, satisfying the UI-SPEC amber contract).
    - `grep -cE 'massMention|stuffing' src/analysis/tags.test.ts` is >= 2 (both signal flags exercised by tests).
    - `tags.ts` exports `analyzeTags` and `TagsResult`.
  </acceptance_criteria>
  <done>analyzeTags defensively aggregates p/e/t with malformed-row counting, top-N lists, per-event high-tag-count outliers, and per-event + aggregate massMention/stuffing signal flags (feeding the UI-SPEC amber "high mention fan-out" / "hashtag stuffing" labels), no clean field; its test suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the analyzeKinds histogram + KIND_NAMES lookup (DRILL-04)</name>
  <files>src/analysis/kinds.ts, src/analysis/kinds.test.ts, src/analysis/kindNames.ts</files>
  <read_first>
    - src/analysis/rate.ts (ANALOG — REUSE its exported `isSaneTs` (lines 33-35) for createdAt; mirror the `bins: { start; count }[]` shape (line 43) so the slice-02 KindsPanel can copy RatePanel's bar JSX; the rejectedCount/out-of-range discipline lines 11-17/40/87)
    - src/analysis/rate.test.ts (ANALOG — forged/out-of-range test style lines 26-32/91-103; the no-clean-field asymmetry test)
    - .planning/phases/03-remaining-spam-signals/03-RESEARCH.md (Pattern 3 lines 250-264 = histogram + isSaneTs reuse + kind bounds-check + KIND_NAMES table source; A4 line 412 = the kind bounds rule)
  </read_first>
  <behavior>
    - Events with a sane kind (Number.isSafeInteger(kind) && kind >= 0) AND a sane createdAt (isSaneTs) are bucketed by kind into a histogram.
    - An event with a forged/out-of-range kind OR createdAt is counted in outOfRangeCount and NOT bucketed (never mis-charted).
    - bins are { kind, count } sorted (e.g. descending by count, or ascending by kind — documented), suitable for hand-rolled bars.
    - KIND_NAMES maps known kind numbers to human names; unknown kinds are not in the map (the panel renders the number alone).
    - 0 events → { analyzedCount: 0, outOfRangeCount: 0, bins: [] }; no crash.
    - Result object exposes NO clean/ok/safe key.
  </behavior>
  <action>
    Create `kindNames.ts` exporting `export const KIND_NAMES: Record<number, string>` from RESEARCH lines 258-262 (0 Metadata, 1 Short Text Note, 3 Follows, 4 Encrypted DM, 5 Deletion Request, 6 Repost, 7 Reaction, 1984 Reporting, 9735 Zap, 10002 Relay List, 30023 Long-form) with a `[CITED: nostr-protocol/nips README "Event Kinds"]` source comment. This is Claude's-discretion lookup content (CONTEXT) — known kinds labeled, unknown shown as the number.

    Create `kinds.ts` as a PURE module (mirror `rate.ts` header + asymmetry note). `import { isSaneTs } from './rate'` — do NOT re-implement the createdAt bounds-check. Accept `events: { kind: number; createdAt: number }[]`. Define `KindsResult { analyzedCount: number; outOfRangeCount: number; bins: { kind: number; count: number }[] }` — named like RateResult, NO clean field. For each event apply BOTH bounds checks: `Number.isSafeInteger(kind) && kind >= 0` (per A4) AND `isSaneTs(createdAt)`; if either fails, `outOfRangeCount++` and skip (never bucket a forged value). Bucket the rest by kind into a `Map<number, number>`, emit `bins` sorted (descending by count, ties broken by ascending kind — document the order). `analyzedCount` = events that passed both checks. No degenerate crash on 0 events.

    Create `kinds.test.ts` (vitest, Node env, mirror `rate.test.ts:13-15`). Cover: a multi-kind window producing a correct sorted histogram; a forged kind (negative, non-safe-integer) flagged into outOfRangeCount and NOT bucketed; a forged createdAt (use a value past MAX_TS, imported from `./rate`) flagged the same way; analyzedCount excludes flagged events; the 0-event empty result; and the MANDATORY no-clean/ok/safe-key assertion.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npx vitest run src/analysis/kinds.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/analysis/kinds.test.ts` passes.
    - `grep -c "import { isSaneTs } from './rate'" src/analysis/kinds.ts` returns 1 (reuse, not re-implementation).
    - `grep -vE '^\s*(//|\*|/\*)' src/analysis/kinds.ts | grep -cE "'clean'|'ok'|'safe'|clean:|ok:|safe:"` returns 0.
    - `grep -c 'outOfRangeCount' src/analysis/kinds.ts` is >= 2 (forged values flagged, not dropped/mis-bucketed).
    - `grep -cE "from 'react'|transport|client" src/analysis/kinds.ts` returns 0 (pure module).
    - `kinds.ts` exports `analyzeKinds` and `KindsResult`; `kindNames.ts` exports `KIND_NAMES`.
  </acceptance_criteria>
  <done>analyzeKinds buckets sane kinds into a sorted histogram while flagging forged kind/createdAt into outOfRangeCount via reused isSaneTs, no clean field; KIND_NAMES lookup exists; its test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lens → analyzer input | `content`, `tags`, `kind`, `createdAt` originate from author-controlled Nostr events relayed verbatim; every value is untrusted and potentially forged/hostile when it reaches a pure analyzer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | analyzeKinds — forged 64-bit kind/createdAt | mitigate | Reuse `isSaneTs` for createdAt + `Number.isSafeInteger(kind) && kind >= 0` for kind; flag into `outOfRangeCount`, never bucket/mis-compute (Task 3) |
| T-03-02 | Denial of Service | analyzeTags — malformed/hostile tag rows | mitigate | Defensive `Array.isArray`/`typeof` guards; count into `malformedTagRows`, never throw (Task 2) |
| T-03-03 | Denial of Service (self) | nearDup — O(n²) stage-2 comparison on a wide window | mitigate | Precompute shingle Sets once; size-disparity short-circuit `|a.size-b.size|/max > (1-jaccard)`; bounded for n≤500 (Task 1) |
| T-03-04 | Tampering | nearDup — over-merging distinct spam via aggressive normalization | accept | CONTEXT-mandated: NFC+lowercase+whitespace only, no URL/mention/punctuation stripping; honest under-merge is preferred to over-merge |
| T-03-SC | Tampering | npm installs | mitigate | Zero new packages this phase (RESEARCH Package Legitimacy Audit: not applicable) — no install task, no slopsquat surface |
</threat_model>

<verification>
- `npx vitest run src/analysis/` — all three new analyzer suites + the existing rate suite pass (no regression).
- `npm run build` (tsc -b) — analyzers type-check (note: panels in 03-02 are not yet present; this slice's modules compile standalone).
- Each analyzer result interface is grep-verified to carry no clean/ok/safe key.
- No `analysis/` module imports React or transport.
</verification>

<success_criteria>
- nearDup, analyzeTags, analyzeKinds are pure, tested, and asymmetric (no clean field).
- thresholds.ts carries NEAR_DUP + TAGS additively without altering BURST.
- All RED→GREEN cycles complete; `npx vitest run src/analysis/` is green.
- Zero new runtime dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-remaining-spam-signals/03-01-SUMMARY.md` when done.
</output>
