---
phase: 03-remaining-spam-signals
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/queries/events.graphql.ts
  - src/queries/rawEvent.graphql.ts
  - src/hooks/useAuthorWindow.ts
  - src/views/DuplicatePanel.tsx
  - src/views/DuplicatePanel.module.css
  - src/views/TagsPanel.tsx
  - src/views/TagsPanel.module.css
  - src/views/KindsPanel.tsx
  - src/views/KindsPanel.module.css
  - src/views/RawInspector.tsx
  - src/views/RawInspector.module.css
  - src/views/AuthorDrillDown.tsx
autonomous: true
requirements: [DRILL-02, DRILL-03, DRILL-04]

must_haves:
  truths:
    - "The author drill-down shows three stacked signal panels (Duplicate content, Tags & mentions, Event kinds) below the timeline, each re-deriving live over the accumulated useAuthorWindow window on Load more (DRILL-02, DRILL-03, DRILL-04)"
    - "The duplicate panel frames clusters against the denominator ('X of N fetched are near-duplicates'), never a bare '0 duplicates'; zero clusters reads as a neutral fact (DRILL-02)"
    - "The tags panel surfaces top-N mentions, top-N hashtags, an e-reference count, and amber per-event outlier flags (DRILL-03)"
    - "The kinds panel renders a hand-rolled CSS bar histogram with NIP/kind labels and an amber out-of-range flagged note (DRILL-04)"
    - "Each panel carries a co-located non-removable WindowIndicator, even at N=0 (DRILL-05 carried forward)"
    - "Each timeline row has a neutral 'View raw' control that lazily fetches that one event's raw bytes via events(filter:{ids:[id]},limit:1) and renders them as escaped <pre> plaintext, pretty-printed if JSON else verbatim (DRILL-04)"
    - "EventsDocument selects tags (not raw); WindowEvent gains tags: string[][]; codegen has been run so the new field is typed (DRILL-03)"
  artifacts:
    - path: "src/queries/events.graphql.ts"
      provides: "EventsDocument with tags added to the selection (raw stays out)"
      contains: "tags"
    - path: "src/queries/rawEvent.graphql.ts"
      provides: "RawEventDocument selecting raw via the ids single-event path"
      exports: ["RawEventDocument"]
    - path: "src/hooks/useAuthorWindow.ts"
      provides: "WindowEvent interface extended with tags: string[][]"
      contains: "tags"
    - path: "src/views/DuplicatePanel.tsx"
      provides: "Near-duplicate cluster panel (DRILL-02)"
    - path: "src/views/TagsPanel.tsx"
      provides: "Tag/mention fan-out panel (DRILL-03)"
    - path: "src/views/KindsPanel.tsx"
      provides: "Kind-distribution histogram panel (DRILL-04)"
    - path: "src/views/RawInspector.tsx"
      provides: "Lazy single-event raw-JSON inspector, escaped <pre> (DRILL-04)"
    - path: "src/views/AuthorDrillDown.tsx"
      provides: "Hosts the three stacked panels + per-row View raw trigger"
  key_links:
    - from: "src/views/DuplicatePanel.tsx"
      to: "src/analysis/nearDup.ts"
      via: "imports nearDup, calls it on events"
      pattern: "nearDup\\("
    - from: "src/views/TagsPanel.tsx"
      to: "src/analysis/tags.ts"
      via: "imports analyzeTags, calls it on events"
      pattern: "analyzeTags\\("
    - from: "src/views/KindsPanel.tsx"
      to: "src/analysis/kinds.ts"
      via: "imports analyzeKinds, calls it on events"
      pattern: "analyzeKinds\\("
    - from: "src/views/RawInspector.tsx"
      to: "src/queries/rawEvent.graphql.ts"
      via: "client.query(RawEventDocument, {filter:{ids:[id]},limit:1}) classified before reading data"
      pattern: "RawEventDocument"
    - from: "src/views/AuthorDrillDown.tsx"
      to: "src/views/DuplicatePanel.tsx"
      via: "mounts the three panels stacked in the loaded branch"
      pattern: "DuplicatePanel"
  prohibitions:
    - "NO dangerouslySetInnerHTML anywhere — content, tags, hashtags, pubkeys, raw bytes render only as escaped JSX text nodes"
    - "NO raw in EventsDocument — raw is selected ONLY in rawEvent.graphql.ts (lazy-only)"
    - "NO useQuery for the raw fetch — use the imperative client.query().toPromise().catch(() => 'THREW') pattern (useQuery fetches on mount = not lazy)"
    - "NO clean/ok/safe/green/teal/success treatment in any panel — neutral default, amber only on a detected signal, accent reserved for Inspect-author submit only"
    - "NO bare '0 duplicates' / verdict copy — always frame 'X of N fetched'; a zero result is a neutral fact"
    - "NO reading result.data before classify() on the lazy raw fetch (errors arrive on HTTP 200)"
    - "NO chart library — hand-rolled CSS bars only (copy RatePanel bar JSX)"
---

<objective>
Mount the full forensic picture on the existing `AuthorDrillDown` view: three stacked client-side signal panels (DuplicatePanel / TagsPanel / KindsPanel) consuming the slice-03-01 pure analyzers over the same `useAuthorWindow` window, plus a lazy per-event raw-JSON inspector. Wire the data: add `tags` to `EventsDocument`, extend `WindowEvent`, create the lazy `RawEventDocument`, and run codegen. Every panel inherits the Phase 2 honesty posture (non-removable WindowIndicator, asymmetric framing, escaped plaintext, amber-not-green).

Purpose: This is the user-visible half of the vertical slice — after this, an analyst paste→drill→sees duplicate clusters, tag fan-out, the kind histogram, and can open any event's canonical bytes, all framed against an honest denominator. It composes Phase-2 parts (RatePanel shape, WindowIndicator, the imperative transport boundary) with the slice-03-01 analyzers.

Output: three panel components (+ CSS), the RawInspector (+ CSS), the lazy query document, the additive query/interface edits, regenerated `src/gql/*`, and the updated host view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remaining-spam-signals/03-CONTEXT.md
@.planning/phases/03-remaining-spam-signals/03-RESEARCH.md
@.planning/phases/03-remaining-spam-signals/03-PATTERNS.md
@.planning/phases/03-remaining-spam-signals/03-UI-SPEC.md

# The panel + bar + amber-on-signal + co-located-indicator + persistent-caveat blueprint.
@src/views/RatePanel.tsx
@src/views/RatePanel.module.css
# The non-removable denominator (consume verbatim, one per panel).
@src/views/WindowIndicator.tsx
# The host view + the imperative-fetch + errorTreatment switch the raw inspector reuses.
@src/views/AuthorDrillDown.tsx
@src/hooks/useAuthorWindow.ts
# The additive query edit target + the analog for the new lazy document.
@src/queries/events.graphql.ts
# The slice-03-01 analyzers this slice consumes (read their exported result interfaces).
@src/analysis/nearDup.ts
@src/analysis/tags.ts
@src/analysis/kinds.ts
@src/analysis/kindNames.ts
</context>

## Artifacts this phase produces (slice 03-02)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `tags` field on `EventsDocument` | query field | additive; `raw` stays OUT |
| `tags: string[][]` on `WindowEvent` | interface field | the panels read `e.tags` typed |
| `RawEventDocument` | query document | `events(filter:{ids},limit:1){events{id raw}}` — the ONLY place `raw` is selected |
| `DuplicatePanel` (+ `.module.css`) | component | DRILL-02 |
| `TagsPanel` (+ `.module.css`) | component | DRILL-03 |
| `KindsPanel` (+ `.module.css`) | component | DRILL-04 |
| `RawInspector` (+ `.module.css`) | component | DRILL-04 lazy escaped `<pre>` |

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING codegen]: Add tags to EventsDocument, create RawEventDocument, extend WindowEvent, run codegen</name>
  <files>src/queries/events.graphql.ts, src/queries/rawEvent.graphql.ts, src/hooks/useAuthorWindow.ts</files>
  <read_first>
    - src/queries/events.graphql.ts (EDIT target — the `graphql()` document at lines 16-30; the selection set at lines 19-25 gains `tags`; the doc-comment at lines 6-10 currently says "ONLY the five fields" and must be updated; the client preset keys on the verbatim string — Pitfall 5)
    - src/hooks/useAuthorWindow.ts (EDIT target — the `WindowEvent` interface at lines 29-36 gains `tags: string[][]`; the "exactly the five fields" doc-comment at line 29 must be updated; the cast `page.events as WindowEvent[]` at line 185 relies on the field existing — Pitfall 6; NO fetch-logic change)
    - .planning/phases/03-remaining-spam-signals/03-RESEARCH.md (Code Examples lines 367-388 = the rawEvent document + the additive tags edit; Pitfall 5 lines 333-337 = codegen drift)
    - .planning/phases/03-remaining-spam-signals/03-PATTERNS.md (rawEvent.graphql.ts analog lines 218-231; events.graphql.ts edit lines 235-241)
  </read_first>
  <action>
    Edit `events.graphql.ts`: add `tags` to the `events { ... }` selection (so it reads `id pubkey kind createdAt content tags`); keep `raw` OUT (lazy-only — DRILL-04 rationale). Update the doc-comment at lines 6-10 to state the window query now selects six fields including `tags` for the Phase-3 aggregation, and that the large canonical `raw` payload remains deliberately excluded (fetched lazily by the raw inspector only).

    Create `rawEvent.graphql.ts` exporting `RawEventDocument` via `graphql()` (import `graphql` from `'../gql'`, mirroring events.graphql.ts:1): `query RawEvent($filter: EventFilterInput, $limit: Int) { events(filter: $filter, limit: $limit) { events { id raw } } }`. Add a header comment stating `raw` is selected ONLY here (the single-event `ids` path; there is no `event(id)` query — `EventFilterInput.ids` + `limit:1` is the path) and that this is the sole place the canonical bytes are fetched.

    Edit `useAuthorWindow.ts`: add `tags: string[][]` to the `WindowEvent` interface (lines 29-36) and update the doc-comment at line 29 from "exactly the five fields" to reflect the added `tags` field. Do NOT change any fetch logic — `requestPolicy: 'network-only'` and the cursor/run-token machinery stay untouched.

    Run codegen from the project directory: `cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npm run codegen`. This regenerates `src/gql/*` so `data.events.events[].tags` is typed and `RawEventDocument` is in the typed overload set. This task is BLOCKING: without codegen the new field/document are untyped (`unknown`/`any`) and a downstream `tsc -b` build would either fail or falsely pass against stale types (Pitfall 5).
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npm run codegen && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run codegen` completes without error and `npm run build` (tsc -b) passes against the regenerated types.
    - `grep -c 'tags' src/queries/events.graphql.ts` is >= 1 AND `grep -c 'raw' src/queries/events.graphql.ts` returns 0 (tags added, raw stays out of the window query).
    - `grep -c 'raw' src/queries/rawEvent.graphql.ts` is >= 1 (raw selected only here).
    - `grep -c 'tags: string\[\]\[\]' src/hooks/useAuthorWindow.ts` returns 1 (WindowEvent extended).
    - `git status --porcelain src/gql` shows regenerated files are modified (codegen actually ran).
  </acceptance_criteria>
  <done>EventsDocument selects tags (not raw); RawEventDocument selects raw via the ids path; WindowEvent has tags: string[][]; codegen + tsc -b are green.</done>
</task>

<task type="auto">
  <name>Task 2: Build the three signal panels (DuplicatePanel, TagsPanel, KindsPanel)</name>
  <files>src/views/DuplicatePanel.tsx, src/views/DuplicatePanel.module.css, src/views/TagsPanel.tsx, src/views/TagsPanel.module.css, src/views/KindsPanel.tsx, src/views/KindsPanel.module.css</files>
  <read_first>
    - src/views/RatePanel.tsx (ANALOG for all three — props `{ events, windowMeta }` line 29; re-derive-every-render line 31; `Intl.NumberFormat` formatter lines 26-27; the `<section className={styles.panel} aria-label>` shell + head + conditional amber badge lines 34-45; co-located `<WindowIndicator meta={windowMeta} />` line 48; hand-rolled bars lines 53-71; rejected/out-of-range note lines 78-84; persistent caveat lines 87-90; escaped-plaintext-only header rule lines 18-19)
    - src/views/RatePanel.module.css (ANALOG CSS — `.panel` lines 13-21; `.head`/`.title` lines 23-37; `.burstBadge` amber marker lines 40-51; `.stateDot` lines 53-60; bars `.bars`/`.barSlot`/`.bar` lines 62-92; `.caveat` lines 110-113; the color-discipline header lines 5-10 — amber `--recoverable` only, NO teal/green)
    - src/views/WindowIndicator.tsx (consume verbatim as `<WindowIndicator meta={windowMeta} />`; renders even at N=0)
    - src/analysis/nearDup.ts, src/analysis/tags.ts, src/analysis/kinds.ts, src/analysis/kindNames.ts (read the exported result interfaces — NearDupResult, TagsResult, KindsResult, KIND_NAMES — to render their fields)
    - .planning/phases/03-remaining-spam-signals/03-UI-SPEC.md (State Treatments per panel lines 163-187; Copywriting Contract VERBATIM strings lines 220-241; panel color rules lines 146-155)
    - .planning/phases/03-remaining-spam-signals/03-PATTERNS.md (DuplicatePanel lines 139-154; TagsPanel lines 158-162; KindsPanel lines 166-185)
  </read_first>
  <action>
    Each panel takes `{ events, windowMeta }: { events: WindowEvent[]; windowMeta: WindowMeta }` (import the types from `../hooks/useAuthorWindow`), re-derives its analyzer result on every render (so Load more widens the analysis live), declares a module-scope `Intl.NumberFormat` like RatePanel:26-27, co-locates `<WindowIndicator meta={windowMeta} />`, and renders ONLY escaped JSX text nodes (NEVER `dangerouslySetInnerHTML`). Copy strings VERBATIM from the UI-SPEC Copywriting Contract. Each `.module.css` copies the RatePanel module's `.panel`/`.head`/`.title`/`.burstBadge`/`.stateDot`/`.caveat` discipline; amber `--recoverable` is the ONLY non-neutral color (no teal, no green, no success token).

    DuplicatePanel (DRILL-02): call `nearDup(events.map(e => ({ id: e.id, content: e.content })))`. Title "Duplicate content". Has-clusters: a summary line "{dupCount} of {N} fetched are near-duplicates across {clusterCount} cluster(s)" (counts via the formatter, framed against `windowMeta.count` — never a bare "0 duplicates"); render each cluster as a neutral group with an amber badge (dot + the label "exact duplicate" for `kind:'exact'` or "near-duplicate" for `kind:'near'` + member count), expandable to list member events' escaped single-line content previews. Zero clusters: the neutral muted fact "No near-duplicates among the {N} fetched events." (NOT amber, NOT green). N=0: "Computed over 0 fetched events — no content to compare." Always render the persistent asymmetry note "Duplicate content is one spam signal among several — its absence does not clear this author."

    TagsPanel (DRILL-03): call `analyzeTags(events.map(e => ({ id: e.id, tags: e.tags })))`. Title "Tags & mentions". Render two top-N lists — "Most-mentioned pubkeys" (`topMentions`, each row a mono truncated pubkey first/last-8 with full value in `title` + a mono count) and "Top hashtags" (`topHashtags`, each row an escaped mono hashtag + mono count), neutral `--border` dividers; "{count} event references" from `eventRefCount`; an amber outlier badge (dot + "high tag count") per entry in `outlierEvents`. Zero tags: "No p/e/t tags among the {N} fetched events." N=0: "Computed over 0 fetched events — no tags to aggregate." Persistent asymmetry note "High mention/hashtag fan-out is suspicious; low fan-out does not clear this author." If `malformedTagRows > 0`, render a muted "{n} malformed tag rows skipped" note (parity with RatePanel's rejectedNote — counted, not hidden).

    KindsPanel (DRILL-04): call `analyzeKinds(events.map(e => ({ kind: e.kind, createdAt: e.createdAt })))`. Title "Event kinds". Render a hand-rolled CSS bar histogram by copying RatePanel's bar JSX (lines 53-71) and bar CSS (RatePanel.module.css:62-86) — bars stay NEUTRAL `--text-muted` always (kinds are neutral data; do NOT apply any amber `.barsBurst` tint to the bars). Each bar labeled with `KIND_NAMES[bin.kind]` (sans 13px/600) where known + the raw `bin.kind` number (mono); unknown kinds show the number alone + a muted "(unknown kind)" label; per-bar count in mono. When `outOfRangeCount > 0` render an amber flagged note (dot) "{count} events with out-of-range kind/timestamp" — counted and flagged, never dropped. N=0: "Computed over 0 fetched events — no kinds to show."
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc -b) passes with all three panels present.
    - `grep -c 'nearDup(' src/views/DuplicatePanel.tsx` >= 1; `grep -c 'analyzeTags(' src/views/TagsPanel.tsx` >= 1; `grep -c 'analyzeKinds(' src/views/KindsPanel.tsx` >= 1 (each panel calls its analyzer).
    - `grep -rc 'WindowIndicator' src/views/DuplicatePanel.tsx src/views/TagsPanel.tsx src/views/KindsPanel.tsx` shows each panel co-locates the indicator.
    - `grep -rL 'dangerouslySetInnerHTML' src/views/DuplicatePanel.tsx src/views/TagsPanel.tsx src/views/KindsPanel.tsx` lists all three (none use it) — i.e. `grep -rc 'dangerouslySetInnerHTML' src/views/DuplicatePanel.tsx src/views/TagsPanel.tsx src/views/KindsPanel.tsx` is 0 for each.
    - `grep -riE 'teal|--accent|#3DD6C0|green|success|clean' src/views/DuplicatePanel.module.css src/views/TagsPanel.module.css src/views/KindsPanel.module.css` returns 0 (no accent/green/clean token in any panel CSS).
    - The verbatim copy strings "Duplicate content", "Tags & mentions", "Event kinds", and "its absence does not clear this author" are each present (grep) in their panels.
  </acceptance_criteria>
  <done>Three stacked panels render their analyzer results with co-located WindowIndicators, denominator framing, neutral-default/amber-on-signal color, hand-rolled kind bars, and escaped plaintext only; tsc -b is green.</done>
</task>

<task type="auto">
  <name>Task 3: Build the lazy RawInspector and mount everything in AuthorDrillDown</name>
  <files>src/views/RawInspector.tsx, src/views/RawInspector.module.css, src/views/AuthorDrillDown.tsx</files>
  <read_first>
    - src/hooks/useAuthorWindow.ts (ANALOG for the imperative fetch — the throw-guarded `client.query(...).toPromise().catch(() => 'THREW')` at lines 128-135, the `classify(result)` BEFORE reading data at lines 150-152, the `result.data?.events?.events?.[0]` data read at line 183; imports `client` line 21, `classify` line 22)
    - src/views/AuthorDrillDown.tsx (EDIT target — the `errorTreatment` switch lines 118-144 the inspector reuses to map ApiError→tone/message; the `TimelineRow` lines 147-157 gains the neutral "View raw" trigger; the RatePanel mount line 243 is the template — stack the three new panels beside it in the loaded branch only; imports at lines 26-33; the zero-match branch 197-208 stays panel-free; `.copyButton` neutral-chrome styling is the trigger/close style)
    - src/views/RatePanel.module.css (CSS source for `.panel`/`.caveat`; the `<pre>` needs mono/13px/pre-wrap/word-break/border/space-md)
    - .planning/phases/03-remaining-spam-signals/03-UI-SPEC.md (raw inspector State Treatments lines 189-199; copy strings lines 242-249; the escaped-not-executed caption rule)
    - .planning/phases/03-remaining-spam-signals/03-PATTERNS.md (RawInspector lines 189-214; AuthorDrillDown mount lines 251-263)
    - .planning/phases/03-remaining-spam-signals/03-RESEARCH.md (Pattern 4 lines 266-283 imperative lazy fetch; Pitfall 4 lines 327-330 JSON.parse try/catch)
  </read_first>
  <action>
    Create `RawInspector.tsx`: a component taking `{ id }` (the event id) that lazily fetches on a "View raw" activation — NOT on mount. Use the imperative pattern copied from useAuthorWindow:128-152: `const result = await client.query(RawEventDocument, { filter: { ids: [id] }, limit: 1 }, { requestPolicy: 'network-only' }).toPromise().catch(() => 'THREW' as const)`; if `'THREW'` set a NETWORK error; call `classify(result)` BEFORE reading `result.data`; map any returned ApiError through the `errorTreatment` switch (export/lift it from AuthorDrillDown or import it — Claude's discretion) to the UI-SPEC retryable (amber) vs hard (red) rows; read `result.data?.events?.events?.[0]?.raw` (undefined ⇒ the zero-match note "No canonical bytes returned for this event id."). Manage local state for idle/fetching/loaded/error/zero-match. Render states per the UI-SPEC table: trigger "View raw" (neutral chrome, `.copyButton`-style), "Loading raw…" (disabled, no spinner lib), loaded → an escaped `<pre>` whose body is `JSON.stringify(JSON.parse(raw), null, 2)` inside a try/catch that falls back to `raw` verbatim on parse failure (Pitfall 4); caption "Canonical bytes — escaped, not executed." for the JSON path or "Raw bytes shown verbatim (not valid JSON)." for the verbatim path; a neutral "Close" control; amber inline note "Couldn't load raw bytes — retrying." for retryable; red "Couldn't load the raw bytes for this event." for hard. Render the `<pre>` body ONLY as a JSX text node — NEVER `dangerouslySetInnerHTML`. `RawInspector.module.css`: `.panel`/`<pre>` from RatePanel discipline; the `<pre>` is `font-family: var(--font-mono)`, 13px, `white-space: pre-wrap; word-break: break-all`, 1px `--border`, `--space-md` pad; trigger/close are neutral chrome (no accent).

    Edit `AuthorDrillDown.tsx`: import `DuplicatePanel`, `TagsPanel`, `KindsPanel`, and `RawInspector` alongside `RatePanel` (line 32). In the loaded branch (the `else` at lines 209-245, where `events.length >= 1`), stack the three new panels immediately after the existing `<RatePanel ... />` at line 243: `<DuplicatePanel events={events} windowMeta={windowMeta} />`, `<TagsPanel events={events} windowMeta={windowMeta} />`, `<KindsPanel events={events} windowMeta={windowMeta} />`. Add the neutral "View raw" trigger to `TimelineRow` (lines 147-157) wired to `RawInspector` (row-inline expand is the chosen UX per RESEARCH Open-Question 2 — collapsible under the clicked row, keeps the analyst in timeline context, no modal/focus-trap). Keep the gating order (connecting → error-shell → loaded) and the zero-match branch unchanged — panels mount ONLY where there are events to analyze.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc -b) passes and `npx vitest run` is green (no analyzer-suite regression).
    - `grep -c 'RawEventDocument' src/views/RawInspector.tsx` >= 1 AND `grep -c "requestPolicy: 'network-only'" src/views/RawInspector.tsx` >= 1 (lazy imperative fetch via the new document).
    - `grep -c 'useQuery' src/views/RawInspector.tsx` returns 0 (imperative, not declarative — stays lazy).
    - `grep -c 'classify' src/views/RawInspector.tsx` >= 1 (classified before reading data).
    - `grep -c 'dangerouslySetInnerHTML' src/views/RawInspector.tsx` returns 0; `grep -c 'JSON.parse' src/views/RawInspector.tsx` >= 1 (escaped <pre>, try/catch parse).
    - `grep -cE 'DuplicatePanel|TagsPanel|KindsPanel' src/views/AuthorDrillDown.tsx` >= 3 (all three mounted) AND each appears after the RatePanel mount line.
    - The verbatim strings "View raw", "Canonical bytes — escaped, not executed.", and "No canonical bytes returned for this event id." are present in RawInspector.
  </acceptance_criteria>
  <done>RawInspector lazily fetches one event's raw bytes via the imperative classify-gated pattern and renders escaped pretty/verbatim <pre>; the three panels + the per-row View raw trigger are mounted in the AuthorDrillDown loaded branch; build + full test run are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lens → React render | author-controlled `content`, `tags`, hashtags, pubkeys, and canonical `raw` bytes cross into the DOM; all are untrusted and must be escaped, never executed |
| client → lens (lazy raw fetch) | a new single-event query crosses to the relay; the HTTP-200-with-errors[] boundary must be classified before reading data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering / Elevation | RawInspector + panels — stored XSS via content/hashtag/raw bytes rendered as HTML | mitigate | Render ONLY as escaped JSX text nodes in `<pre>`/spans; NEVER `dangerouslySetInnerHTML` (Tasks 2 + 3; grep-gated) |
| T-03-06 | Denial of Service | RawInspector — non-JSON/huge `raw` crashing the inspector | mitigate | `try/catch` around `JSON.parse`, render verbatim on failure; `<pre>` `white-space: pre-wrap; word-break: break-all` so long lines don't break layout (Task 3) |
| T-03-07 | Information Disclosure | lazy raw-fetch error leaking server internals | mitigate | Reuse `classify()` → generic INTERNAL via the `errorTreatment` switch; hard-error copy is generic, never echoes internals (Task 3) |
| T-03-08 | Tampering | `raw` payload inflation if selected in the window query | mitigate | `raw` selected ONLY in `rawEvent.graphql.ts`; grep-gated out of `EventsDocument` (Task 1) |
| T-03-SC | Tampering | npm installs | mitigate | Zero new packages this phase (RESEARCH Package Legitimacy Audit: not applicable) — no install task |
</threat_model>

<verification>
- `npm run codegen && npm run build` — the regenerated types compile with `tags` on the window query and `RawEventDocument` typed.
- `npx vitest run` — the slice-03-01 analyzer suites still pass (panels are pure consumers; no analyzer change).
- grep gates confirm: no `dangerouslySetInnerHTML` anywhere; `raw` only in rawEvent.graphql.ts; no `useQuery` in RawInspector; no teal/green/accent in panel CSS; all three panels mounted.
- Manual UI smoke (human, optional): paste a suspect, confirm the three panels render with denominators, open a row's raw bytes as escaped `<pre>`.
</verification>

<success_criteria>
- An analyst on the drill-down sees Duplicate content, Tags & mentions, and Event kinds panels stacked below the timeline, each with a non-removable WindowIndicator, re-deriving on Load more.
- Duplicate framing is "X of N fetched", never a bare verdict; zero results read as neutral facts.
- Any timeline row's raw bytes open lazily as escaped `<pre>` (pretty if JSON, verbatim otherwise), never executed.
- `raw` is fetched only via the lazy path; `tags` rides the window query; codegen + build + tests are green.
- No XSS sink, no accent/green in any panel, zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-remaining-spam-signals/03-02-SUMMARY.md` when done.
</output>
