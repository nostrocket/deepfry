---
phase: 04-batch-triage
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/router/hashRouter.ts
  - src/router/hashRouter.test.ts
  - src/App.tsx
  - src/views/BatchTriage.module.css
  - src/views/BatchImport.tsx
  - src/views/TriageTable.tsx
autonomous: false
requirements: [BATCH-01, BATCH-02, BATCH-03, BATCH-04]
must_haves:
  truths:
    - "A neutral nav affordance in the app shell routes to #/batch; parseHash maps #/batch to { name: 'batch' } and an unknown hash still resolves to notfound"
    - "The batch view offers three import sources feeding one pipeline: a paste textarea, a .txt/.csv file upload, and an 'enumerate corpus' action with a running count + Stop"
    - "An import summary shows 'N valid · M duplicates removed · K unparseable' with the unparseable tokens listed as escaped plaintext"
    - "The triage table renders sortable per-signal columns (Author, Events, Burst, Near-dup, Tag fan-out), default event-count descending, amber-on-signal chips, neutral dash on absence, explicit '0 events' rows"
    - "Two batch WindowIndicator denominators render: 'triaged N of M authors' and 'N distinct authors as of this fetch'"
    - "Clicking a triage row navigates to the existing #/a/<hex> drill-down using the row's normalized lowercase hex"
    - "The teal accent appears ONLY on the 'Triage' submit; enumerate, Stop, file picker, textarea, sort headers, rows are neutral chrome"
  prohibitions:
    - "MUST NOT use dangerouslySetInnerHTML — all tokens/hex/content rendered as escaped plaintext"
    - "MUST NOT render a clean/ok/safe column or a single opaque spam-score column"
    - "MUST NOT render a tripped signal in green/teal — amber + label + shape only"
    - "MUST NOT apply teal accent to anything except the Triage submit"
    - "MUST NOT add a new drill-down route — reuse #/a/<hex>"
    - "MUST NOT add a table/grid or chart library — hand-rolled <table> + local .sort()"
  artifacts:
    - path: "src/views/BatchImport.tsx"
      provides: "Paste/file/enumerate import sources + import summary + Triage submit + large-set warning"
      min_lines: 80
    - path: "src/views/TriageTable.tsx"
      provides: "Sortable triage table + per-signal chips + 0-events rows + drill-in + batch window indicators"
      min_lines: 80
    - path: "src/views/BatchTriage.module.css"
      provides: "Batch view + table + signal-chip CSS (token-only, amber-on-signal, accent on submit only)"
    - path: "src/router/hashRouter.ts"
      provides: "Route union extended with { name: 'batch' } + #/batch matcher"
      contains: "name: 'batch'"
  key_links:
    - from: "src/App.tsx"
      to: "src/views/BatchImport.tsx"
      via: "route.name === 'batch' renders the batch view"
      pattern: "route.name === 'batch'"
    - from: "src/views/BatchImport.tsx"
      to: "src/analysis/batchImport.ts"
      via: "parseBatchInput(text) on paste/upload"
      pattern: "parseBatchInput\\("
    - from: "src/views/BatchImport.tsx"
      to: "src/hooks/useAuthorEnumeration.ts"
      via: "enumerate corpus source"
      pattern: "useAuthorEnumeration\\("
    - from: "src/views/TriageTable.tsx"
      to: "src/hooks/useLatestPerAuthor.ts"
      via: "rows + triagedCount/totalCount from the chunk loop"
      pattern: "useLatestPerAuthor\\("
    - from: "src/views/TriageTable.tsx"
      to: "src/analysis/triage.ts"
      via: "triageAuthor(row.events) per row → indicators"
      pattern: "triageAuthor\\("
    - from: "src/views/TriageTable.tsx"
      to: "src/router/hashRouter.ts"
      via: "row click sets window.location.hash = '#/a/' + row.author"
      pattern: "#/a/"
---

## Phase Goal

**As an** investigating analyst, **I want to** paste/upload/enumerate a suspect list and read a sortable triage table of transparent per-signal indicators, then click any author to open their full drill-down, **so that** I can screen many authors at a glance and dive into the suspicious ones — without a partial batch ever reading as a clean bill.

## Acceptance Criteria

- [ ] `npm test` passes including the extended hashRouter test
- [ ] `npm run build` typechecks and bundles
- [ ] Human verify: import → triage → sort → drill-in flow works against the live lens, accent confined to Triage, no green/clean anywhere

<objective>
Mount the two new surfaces on the 04-01 core: a BatchImport view (paste + file + enumerate) and a sortable TriageTable view, behind a new #/batch route, with the two batch window-honesty denominators and the persistent first-pass-screen framing. Rows drill into the existing #/a/<hex> view.

Purpose: Deliver the user-visible batch capability — the vertical slice top — composing the pure modules + hooks from 04-01 with the inherited UI contract (accent reservation, amber-on-signal, escaped plaintext).
Output: extended router (+test), extended App shell, two views, one CSS module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-batch-triage/04-CONTEXT.md
@.planning/phases/04-batch-triage/04-UI-SPEC.md
@.planning/phases/04-batch-triage/04-PATTERNS.md

# Reused source
@src/router/hashRouter.ts
@src/App.tsx
@src/views/SuspectEntryBar.tsx
@src/views/AuthorDrillDown.tsx
@src/views/WindowIndicator.tsx
@src/views/RatePanel.module.css
</context>

## Artifacts this phase produces (Plan 04-02)

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `{ name: 'batch' }` route variant (in `src/router/hashRouter.ts`) | extend union + matcher | `#/batch` exact match |
| `src/router/hashRouter.test.ts` | extend test | `#/batch` → batch; unknown → notfound |
| `#/batch` outlet + neutral nav (in `src/App.tsx`) | extend shell | accent stays on the two go-submits |
| `src/views/BatchImport.tsx` → `BatchImport` | new view | three sources + summary + Triage submit |
| `src/views/TriageTable.tsx` → `TriageTable` | new view | sortable table + indicators + drill-in |
| `src/views/BatchTriage.module.css` | new CSS | token-only, shared by both views |

<!-- planner-discipline-allow: dangerouslySetInnerHTML -->

<tasks>

<task type="auto">
  <name>Task 1: hashRouter #/batch route (+test) + App shell outlet & neutral nav</name>
  <files>src/router/hashRouter.ts, src/router/hashRouter.test.ts, src/App.tsx</files>
  <read_first>
    - Modifying: src/router/hashRouter.ts (existing `Route` union lines 15-18 + `parseHash` matcher lines 24-29 — extend, leave home/author/notfound intact)
    - Modifying: src/App.tsx (existing route-outlet switch lines 54-58 + shell header lines 49-53 + readiness gate lines 32-45 + ConnectingShell — preserve verbatim)
    - Analog (test): src/analysis/nearDup.test.ts (describe/it/expect, Node vitest)
  </read_first>
  <action>
In `hashRouter.ts`, add `| { name: 'batch' }` to the `Route` union (between home and author), and in `parseHash`, BEFORE the `AUTHOR_HASH` match, add `if (hash === '#/batch') return { name: 'batch' }` (exact match). Leave the home (`''`/`'#'`/`'#/'`), author, and notfound branches unchanged.

Create `hashRouter.test.ts`: assert `parseHash('#/batch')` → `{ name: 'batch' }`; `parseHash('#/')` → `{ name: 'home' }`; a valid `#/a/<64hex>` → `{ name: 'author', hex }`; an unknown hash (e.g. `#/nope`) → `{ name: 'notfound' }`.

In `App.tsx`, add a new outlet line `{route.name === 'batch' && <BatchImport />}` to the route switch (BatchImport renders the TriageTable internally once a run starts — see Task 3). Add a NEUTRAL nav affordance in the shell header that sets `window.location.hash = '#/batch'` (copy: "Batch triage" per UI-SPEC) — neutral chrome, NOT accent (accent stays on the two go-submits). Preserve the readiness gate + ConnectingShell verbatim.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npm test -- hashRouter 2>&1 | tail -15 && npx tsc -b 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- hashRouter` passes (batch + notfound + author + home cases)
    - `grep -c "name: 'batch'" src/router/hashRouter.ts` >= 2 (union variant + matcher return)
    - `grep -c "route.name === 'batch'" src/App.tsx` == 1
    - `grep -c "#/batch" src/App.tsx` >= 1 (nav affordance)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>The #/batch route resolves, is tested, the App shell renders the batch view at that route and offers a neutral nav link; home/author/notfound unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: BatchImport view — paste/file/enumerate sources + import summary + Triage submit + CSS</name>
  <files>src/views/BatchImport.tsx, src/views/BatchTriage.module.css</files>
  <read_first>
    - Analog (form + parseIdentifier + accent submit + escaped JSX + neutral input/accent-submit split): src/views/SuspectEntryBar.tsx lines 16-21 (escaped JSX), 22-24 (parseIdentifier import), 33-45 (submit handler), 63-65 (accent submit button)
    - Analog (CSS card + amber-on-signal chip + token-only): src/views/RatePanel.module.css lines 13-37 (card shape), 39-50 (`.burstBadge` amber chip + dot)
    - Reused (Task deps from 04-01): src/analysis/batchImport.ts (`parseBatchInput`, `BatchImportResult`), src/hooks/useAuthorEnumeration.ts (`useAuthorEnumeration`), src/analysis/thresholds.ts (`TRIAGE.largeSetWarn`, `TRIAGE.maxFileBytes`)
    - UI-SPEC copy strings (use VERBATIM): 04-UI-SPEC.md Copywriting Contract rows — Batch-view title "Batch triage"; paste placeholder "Paste npub / nprofile / 64-char hex pubkeys — one per line, or separated by spaces or commas"; file prompt "Upload a .txt or .csv of pubkeys"; drop hint "Drop a .txt / .csv here, or choose a file"; file error "Couldn't read that file — {reason}."; import summary "{valid} valid · {duplicates} duplicates removed · {unparseable} unparseable"; unparseable list heading "Couldn't parse these {unparseable} — fix or remove them:"; zero-valid "0 valid pubkeys — paste or upload npub / nprofile / 64-char hex."; enumerate idle "Enumerate corpus authors"; enumerate running "Enumerating… {count} distinct authors"; Stop "Stop"; complete "{N} distinct authors as of this fetch"; stopped "Snapshot — stopped early at {N} authors (incomplete)"; cursor expired "Enumeration cursor expired — restarting from the top."; warming up "Relay is warming up — retrying enumeration…"; large-set "Triaging {N} authors runs {chunks} chunked queries. Continue?"; primary CTA "Triage"
  </read_first>
  <action>
Create `BatchImport.tsx` — a `--surface` view at `#/batch`, heading "Batch triage", with a persistent body line framing it as a first-pass screen. Three import-source blocks feeding ONE collected set of deduped lowercase hexes:

1. Paste textarea (neutral): onChange runs `parseBatchInput(text)` → live import summary. Placeholder = the verbatim UI-SPEC string.
2. File upload (neutral): a .txt/.csv picker + drop zone using the browser `FileReader.readAsText` (platform, not a library). Enforce `TRIAGE.maxFileBytes` — if exceeded or unreadable, show the amber inline file-error note "Couldn't read that file — {reason}." (no hard block; paste still works). Read text → `parseBatchInput`.
3. Enumerate corpus (neutral): a "Enumerate corpus authors" button wired to `useAuthorEnumeration`; while running show "Enumerating… {count} distinct authors" (mono count, info-blue/working dot) + a neutral "Stop" control; on complete show "{N} distinct authors as of this fetch"; on stop show "Snapshot — stopped early at {N} authors (incomplete)" (amber emphasis). Its discovered authors merge into the same collected set.

Import summary block (neutral, with amber on the unparseable segment): render "{valid} valid · {duplicates} duplicates removed · {unparseable} unparseable" (counts mono via `Intl.NumberFormat`, labels sans). Below it, when unparseable > 0, the heading "Couldn't parse these {unparseable} — fix or remove them:" and the `unparseable` tokens listed as ESCAPED plaintext in mono, one per line, each truncated if long (NEVER dangerouslySetInnerHTML — JSX interpolation only). When valid === 0 show "0 valid pubkeys — paste or upload npub / nprofile / 64-char hex." and keep Triage disabled.

Large-set warning (amber, non-blocking): when the deduped valid set length > `TRIAGE.largeSetWarn`, show beside the submit "Triaging {N} authors runs {chunks} chunked queries. Continue?" — a caution, never a hard block.

The single ACCENT action: a "Triage" submit button (the ONE accent affordance this phase — accent-bordered OR accent focus, not both), disabled until valid count >= 1. On submit it surfaces the TriageTable (Task 3) with the collected deduped hexes — lift the collected hex set + a "run started" flag into BatchImport state and render `<TriageTable inputHexes={...} />` when a run is active (or call the hook here and pass rows down — Task 3 defines the exact prop boundary). Everything except the Triage submit stays NEUTRAL chrome (textarea, file picker, enumerate, Stop, retry).

Create `BatchTriage.module.css` (token-only — consume `src/styles/tokens.css`, add NO new tokens): the `--surface` card shape (RatePanel.module.css analog: `--border` 1px, 8px radius, `--space-md` padding, 13px/600 muted title), the three source blocks separated by `--space-lg`, the import-summary block, the amber-on-signal chip rule (`color: var(--recoverable)` + dot, mirroring `.burstBadge`) for the unparseable segment, and an accent treatment for `.triageSubmit` ONLY (teal appears nowhere else). The table-specific rules (Task 3) live in this same module.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npx tsc -b 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0
    - `grep -c "parseBatchInput" src/views/BatchImport.tsx` >= 1
    - `grep -c "useAuthorEnumeration" src/views/BatchImport.tsx` >= 1
    - `grep -c "dangerouslySetInnerHTML" src/views/BatchImport.tsx` == 0
    - `grep -c "FileReader" src/views/BatchImport.tsx` >= 1 (platform file read)
    - `grep -c "var(--accent)" src/views/BatchTriage.module.css` >= 1 AND the accent class name relates to the Triage submit (manual: accent only on `.triageSubmit`)
    - `grep -iE 'green|var\(--accent\)' src/views/BatchTriage.module.css | grep -ivc triage` — accent usages outside the triage-submit class are 0 (manual confirm no stray accent/green)
    - Verbatim copy present: `grep -c "Enumerate corpus authors\|distinct authors as of this fetch" src/views/BatchImport.tsx` >= 1
  </acceptance_criteria>
  <done>The batch import view offers paste/file/enumerate sources feeding one deduped set, shows the import summary with listed unparseable tokens (escaped), the non-blocking large-set warning, and a single accent Triage submit; CSS is token-only with accent confined to the submit.</done>
</task>

<task type="auto">
  <name>Task 3: TriageTable view — sortable per-signal table + 0-events rows + batch indicators + drill-in</name>
  <files>src/views/TriageTable.tsx, src/views/BatchTriage.module.css</files>
  <read_first>
    - Analog (table/rows render + WindowIndicator co-location + errorTreatment switch + row→hash nav): src/views/AuthorDrillDown.tsx lines 91-156 (errorTreatment/InlineNote/ErrorShell tone), 163-176 + 233-243 (hand-rolled header-row + events.map row render), 44-46 (`window.location.hash` nav), 218-231 (WindowIndicator co-location)
    - Analog (amber-on-signal chip, NO green/clean): src/views/RatePanel.tsx lines 38-44 (`burstBadge`); src/views/RatePanel.module.css lines 39-50
    - Reused: src/hooks/useLatestPerAuthor.ts (`useLatestPerAuthor`: rows, triagedCount, totalCount, loading, chunkErrors, run, retryChunk), src/analysis/triage.ts (`triageAuthor`, `TriageIndicators`), src/analysis/mergeByAuthor.ts (`TriageRow`), src/views/WindowIndicator.tsx (`formatInt` + amber-on-partial `<span>` pattern), src/views/ConnectingShell.tsx
    - UI-SPEC copy (VERBATIM): column captions Author / Events / Burst / Near-dup / Tag fan-out; tripped labels "burst" / "near-dup" / "high fan-out"; absent "—"; zero-match "0 events"; empty table "No authors triaged yet."; batch window indicator "Triaged {N} of {M} authors — partial batch" / "Triaged {M} of {M} authors" / "Triaged 0 of {M} authors"; first-pass framing "This table is a first-pass screen over the latest {perAuthor} events per author — quiet rows are not cleared authors. Drill in for the full picture."; chunk states "That chunk was too large — shrinking and retrying." / "Relay is warming up — retrying this chunk…" / "Couldn't load this chunk — Retry." / "Couldn't triage {n} authors in this chunk."; retry "Retry"; row a11y "Open this author's drill-down"; sort a11y "Sort by {column}"
  </read_first>
  <action>
Create `TriageTable.tsx`, prop `{ inputHexes: string[] }` (the collected deduped set from BatchImport). On mount/prop-change call `useLatestPerAuthor().run(inputHexes)`; consume `rows: TriageRow[]`, `triagedCount`, `totalCount`, `loading`, `chunkErrors`, `retryChunk`.

Compute per row: `const ind = triageAuthor(row.events)` → `{ eventCount, burst, nearDup, tagFanOut }`. Maintain local sort state (column + direction), default `events` DESCENDING; sorting is a pure local `.sort()` over the rows (NO refetch). Render a hand-rolled `<table>` (NO grid/table library):
- Header row: clickable neutral column headers (Author / Events / Burst / Near-dup / Tag fan-out) each with an inline-SVG sort caret, a11y label "Sort by {column}". Neutral chrome — never accent.
- Data rows: Author = truncated mono hex (first/last 8, full on `title` hover); Events = mono count, OR an explicit muted "0 events" cell when `eventCount === 0` (distinct from a quiet non-empty row — neither amber nor green). Burst/Near-dup/Tag fan-out cells: when the indicator is tripped render an amber chip + dot + the verbatim label ("burst" / "near-dup" / "high fan-out"); when not tripped render a neutral "—". NO clean/score column, NO green/teal anywhere.
- Whole-row click → `window.location.hash = '#/a/' + row.author` (row.author is normalized lowercase hex), a11y label "Open this author's drill-down". Reuse the EXISTING route — no new drill-down code.

Batch window indicators (reuse the WindowIndicator `formatInt` + amber-on-partial `<span>` pattern — either extend WindowIndicator with a batch variant OR build a sibling using the same treatment): render "Triaged {N} of {M} authors — partial batch" (amber when N < M) / "Triaged {M} of {M} authors" (neutral when complete) / "Triaged 0 of {M} authors". Non-removable while the table is shown. (The enumeration snapshot indicator "N distinct authors as of this fetch" is owned by BatchImport's enumerate block — Task 2; this view owns the triaged-N-of-M one.)

Persistent first-pass-screen framing line (muted, always rendered, non-dismissible): "This table is a first-pass screen over the latest {perAuthor} events per author — quiet rows are not cleared authors. Drill in for the full picture." ({perAuthor} from `TRIAGE.perAuthor`).

States: connecting first chunk → ConnectingShell or inline info-blue note; per-chunk recoverable error (from `chunkErrors`) → inline amber note + a "Retry" control calling `retryChunk(index)` (413 "That chunk was too large — shrinking and retrying.", 503 "Relay is warming up — retrying this chunk…", network/INVALID "Couldn't load this chunk — Retry.") with partial rows retained; per-chunk hard error → inline red generic "Couldn't triage {n} authors in this chunk." (no internals); empty table → "No authors triaged yet." with the indicator at "Triaged 0 of {M}". Reuse AuthorDrillDown's errorTreatment tone mapping.

Add the table + chip + row-hover + sort-caret rules to `BatchTriage.module.css` (token-only; amber-on-signal chip mirrors `.burstBadge`; row dividers `--border`; subtle `--surface` hover; NO new tokens; teal nowhere in the table).
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/GraphQLExplorer && npx tsc -b 2>&1 | tail -15 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0 and `npm run build` succeeds
    - `grep -c "useLatestPerAuthor" src/views/TriageTable.tsx` >= 1 AND `grep -c "triageAuthor" src/views/TriageTable.tsx` >= 1
    - `grep -c "dangerouslySetInnerHTML" src/views/TriageTable.tsx` == 0
    - `grep -c "#/a/" src/views/TriageTable.tsx` >= 1 (reuses existing drill-down route)
    - `grep -vE '^\s*(//|\*|/\*)' src/views/TriageTable.tsx | grep -ic "clean\|spam score\|score column"` == 0 (no verdict/score column in code)
    - `grep -c "0 events\|Triaged" src/views/TriageTable.tsx` >= 1 (explicit 0-events + batch denominator)
    - `grep -c "first-pass screen\|first-pass" src/views/TriageTable.tsx` >= 1 (persistent framing)
    - `grep -c "Tag fan-out\|Near-dup\|Burst" src/views/TriageTable.tsx` >= 1 (per-signal columns)
  </acceptance_criteria>
  <done>The sortable triage table renders transparent per-signal columns with amber-on-signal chips, explicit "0 events" rows, the persistent first-pass framing, the "triaged N of M" batch indicator, per-chunk error+retry handling, and row-click drill-in to #/a/<hex>; build is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — full batch-triage slice end-to-end</name>
  <action>Pause for the analyst to validate the full batch-triage slice in the running app per the how-to-verify steps below; do not mark the phase complete until they confirm.</action>
  <what-built>The full batch-triage slice: a #/batch route reachable from a neutral nav link; a BatchImport view with paste / file-upload / enumerate-corpus sources, an import summary (valid / duplicates / unparseable with listed unparseable tokens), a non-blocking large-set warning, and a single accent "Triage" submit; a sortable TriageTable with transparent per-signal indicators (amber-on-signal, no clean/green), explicit "0 events" rows, the persistent first-pass-screen framing, two batch window-honesty denominators, per-chunk error+retry, and row-click drill-in to the existing #/a/<hex> view.</what-built>
  <how-to-verify>
    1. `npm run dev` and open the app; confirm a neutral "Batch triage" nav affordance is in the shell header (NOT teal).
    2. Click it → URL becomes `#/batch`, the batch import view renders.
    3. Paste a mix: a couple of valid npubs, a valid 64-hex, a duplicate of one, a `note1…`, and a garbage token. Confirm the summary reads "N valid · M duplicates removed · K unparseable" with the note + garbage LISTED below (escaped mono), and the "Triage" submit is the only teal element.
    4. Upload a small .txt of pubkeys → summary updates; try an oversized/binary file → amber "Couldn't read that file" note, paste still works.
    5. Click "Enumerate corpus authors" → running count climbs with a Stop control; press Stop → "Snapshot — stopped early at N authors (incomplete)" (amber). Let another run reach completion → "N distinct authors as of this fetch".
    6. Click "Triage" → the table fills; confirm: columns Author/Events/Burst/Near-dup/Tag fan-out; default sort = event count descending; clicking a header re-sorts (no network refetch); tripped signals are amber chips with labels (no green, no "clean" column, no score column); a zero-match author shows an explicit "0 events" row; the "Triaged N of M authors" indicator and the persistent first-pass-screen line are both visible.
    7. Click any row → it opens that author's existing #/a/<hex> drill-down (full Phase 2/3 view).
    8. (If reproducible) trigger a chunk error and confirm partial rows are retained with a Retry control and no server internals are shown.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted/uploaded tokens + listed unparseable + author hex/content/tags → DOM | author-controlled untrusted strings rendered |
| uploaded file → browser | file read in-browser only, never sent to a server |
| triage row → #/a/<hex> navigation | the row's normalized hex drives the hash route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering / Info-disclosure (XSS) | BatchImport unparseable list + TriageTable cells | mitigate | all author-supplied strings rendered as escaped plaintext via JSX interpolation; never dangerouslySetInnerHTML (verified by grep == 0). |
| T-04-07 | Denial of Service (self) | file upload | mitigate | TRIAGE.maxFileBytes bound enforced before reading; oversized → amber note, no crash. |
| T-04-08 | Information disclosure | per-chunk hard error rendering | mitigate | render the generic INTERNAL copy "Couldn't triage {n} authors in this chunk." — never echo the server message. |
| T-04-09 | Spoofing/UX (false exoneration) | triage table honesty | mitigate | no clean/green/score column; explicit "0 events"; persistent first-pass-screen framing + non-removable "triaged N of M" denominator. |
| T-04-SC | Tampering | npm installs | mitigate | none — zero packages installed this phase; no install task, no checkpoint needed. |
</threat_model>

<verification>
- `npm test` green (hashRouter extension).
- `npm run build` green (full app typechecks + bundles).
- Human checkpoint confirms accent confined to Triage, no green/clean, escaped tokens, drill-in, and honesty denominators.
</verification>

<success_criteria>
A real analyst can navigate to #/batch, import suspects by paste/file/enumeration, read a sortable transparent per-signal triage table framed honestly as a first-pass screen, and click any row into the existing drill-down — with the teal accent confined to the single Triage submit and every honesty prohibition upheld.
</success_criteria>

<output>
Create `.planning/phases/04-batch-triage/04-02-SUMMARY.md` when done.
</output>
