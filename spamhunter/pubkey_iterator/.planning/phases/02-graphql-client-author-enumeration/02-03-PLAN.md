---
phase: 02-graphql-client-author-enumeration
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01-graphql-client-author-enumeration
  - 02-02-graphql-client-author-enumeration
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/enumerate.rs
  - src/main.rs
autonomous: true
requirements: [INGEST-01, INGEST-04]
user_setup:
  - service: LMDB2GraphQL adapter
    why: "The manual connectivity-proof must_have requires a reachable live adapter; unit tests do NOT need it"
    env_vars:
      - name: LMDB2GRAPHQL_URL
        source: "Operator-supplied; defaults to http://127.0.0.1:8080/graphql (contract §10 loopback)"
must_haves:
  truths:
    - "The walk terminates cleanly when endCursor is null / hasMore is false, visiting each page once (INGEST-01)"
    - "Resume continues from the stored last_cursor of the latest unfinished run, no --run-id required (D-01); if none exists --resume starts fresh (D-02)"
    - "Each enumerated pubkey is persisted via insert_pubkeys BEFORE the page's endCursor is written to last_cursor (flush-before-cursor ordering, Pitfall 2)"
    - "A 503 triggers bounded backoff-retry without advancing the cursor; on retry exhaustion the run is marked aborted with last_cursor preserved (D-06/D-07/D-08)"
    - "An in-body INVALID_CURSOR resets after=None and restarts pagination from page 1, without retry or abort (D-08)"
    - "stats.maxLevId is recorded at run start (max_lev_id_start) and clean end (max_lev_id_end); differing values do NOT abort (drift probe, D-09)"
    - "MANUAL (needs live adapter): a real walk against the live LMDB2GraphQL adapter completes, deserializing the real v1.2 response, terminating on hasMore:false — the connectivity proof"
  artifacts:
    - path: "src/enumerate.rs"
      provides: "the authors opaque-cursor walk + resume/abort/drift orchestration (pub async fn run)"
      contains: "pub async fn run"
    - path: "src/main.rs"
      provides: "minimal #[tokio::main] entry point parsing --resume (D-12, not the full Phase-5 clap surface)"
      contains: "tokio::main"
  key_links:
    - from: "src/enumerate.rs"
      to: "src/graphql/client.rs"
      via: "the walk calls client.authors(after, limit) and client.stats(); branches on ClientError"
      pattern: "client\\.authors"
    - from: "src/enumerate.rs"
      to: "src/store/mod.rs"
      via: "persists via store.insert_pubkeys then store.set_run_cursor; resume via latest_unfinished_run; abort via mark_run_aborted; drift via set_run_max_lev_start/end + mark_run_done"
      pattern: "insert_pubkeys|set_run_cursor|latest_unfinished_run"
    - from: "src/main.rs"
      to: "src/enumerate.rs"
      via: "main builds Store + GraphQlClient and calls enumerate::run(&store, &client, resume).await"
      pattern: "enumerate::run"
---

<objective>
Implement the `authors` opaque-cursor walk (`src/enumerate.rs`) and the minimal `--resume` binary entry point (`src/main.rs`) that integrate the store helpers (plan 01) and the GraphQL client (plan 02) into the working connectivity-proving vertical slice. After this plan, a real user can run the engine and watch it enumerate every distinct pubkey in the corpus into a queryable SQLite store, resumably.

Purpose: This is the phase's raison d'être — prove the contract end-to-end. It closes INGEST-01 (resumable cursor enumeration terminating cleanly) and INGEST-04 (graceful adapter-error handling: 503 backoff-no-advance, INVALID_CURSOR restart, in-body errors parsed, snapshot-drift record-and-continue) by composing the two Wave-1 foundations behind a runnable binary.

Output: `enumerate::run` (the walk loop with bounded retry + abort + drift probe + flush-before-cursor ordering), a minimal `#[tokio::main]` binary with `--resume`, and the full unit-test suite (8 behaviors + 1 property test) plus a documented manual live-adapter connectivity proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graphql-client-author-enumeration/02-RESEARCH.md
@.planning/phases/02-graphql-client-author-enumeration/02-PATTERNS.md
@.planning/phases/02-graphql-client-author-enumeration/02-VALIDATION.md

@contract.md
@src/lib.rs
@src/model.rs
@src/store/mod.rs
@.planning/phases/02-graphql-client-author-enumeration/02-01-PLAN.md
@.planning/phases/02-graphql-client-author-enumeration/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the authors opaque-cursor walk with bounded retry, abort, drift probe, and resume — RED first</name>
  <files>src/enumerate.rs, src/lib.rs</files>
  <read_first>
    - src/store/mod.rs (plan 01 helpers: `latest_unfinished_run`, `set_run_cursor`, `set_run_max_lev_start`/`_end`, `mark_run_aborted`, `mark_run_done`, `insert_pubkeys`, plus existing `begin_run`/`close`) — the exact store API the walk persists through
    - src/graphql/client.rs + queries.rs (plan 02: `GraphQlClient::authors`/`stats`, `ClientError` variants, `AuthorsPage { authors, has_more, end_cursor }`, `StatsResult { max_lev_id }`) — injectable endpoint enables pointing the walk at a stub
    - RESEARCH §"Opaque-Cursor Pagination Loop" (lines 299-326, the precise loop), §"Two-Layer Error Handling" table (lines 336-345), §"Bounded Retry + Backoff" (lines 350-373, 3 attempts / 250ms→2s / tokio::time::sleep), Pitfall 1 (sync/async boundary, lines 377-380), Pitfall 2 (flush-before-cursor, lines 382-385), Pitfall 3 (drift not an error, lines 388-390), Pitfall 4 (INVALID_CURSOR not transient, lines 392-395)
    - contract.md §6.4 (`authors` cursor rules: endCursor opaque, pass verbatim, null ⇒ done, each pubkey once per snapshot), §7 (error codes), §8 (live snapshot — corpus may change between calls), §12 (authors.limit ceiling 500, body 256 KiB)
    - 02-VALIDATION.md Per-Task Verification Map (the 8 unit behaviors + property test, with exact `cargo test enumerate::tests::...` names)
    - PATTERNS §`src/enumerate.rs` (lines 168-176, load-bearing ordering rules) + Temp-FILE test convention (lines 210-212)
  </read_first>
  <behavior>
    - terminates_on_null_cursor: mock adapter returns 3 pages then endCursor=null → loop visits each page once and terminates; all pubkeys persisted.
    - resume_from_last_cursor: seed a `run` row with a last_cursor, call run with resume=true → first `authors` call uses that cursor as `after` (no --run-id needed).
    - pubkeys_idempotent_on_resume: an overlapping resume (re-fetch a page already persisted) leaves exactly one row per pubkey (INSERT OR IGNORE).
    - retry_503_no_cursor_advance: mock returns 503 then success → bounded retry succeeds, cursor advances normally; mock returns 503 until exhaustion → run aborts, last_cursor unchanged from the prior page.
    - invalid_cursor_restarts: in-body INVALID_CURSOR once → after resets to None, pagination restarts from page 1, no abort, no retry.
    - abort_preserves_cursor: on retry exhaustion the run row has status='aborted' and last_cursor equal to the last fully-persisted page's endCursor.
    - records_drift_does_not_abort: mock stats returns maxLevId=100 at start and 150 at end → max_lev_id_start=100, max_lev_id_end=150 recorded, run completes status='done' (drift does NOT abort).
    - resume_boundary_union_complete (property): arbitrary page splits + a resume cut at any page boundary ⇒ union of persisted pubkeys == the full mocked set.
  </behavior>
  <action>
    Register `pub mod enumerate;` in src/lib.rs (mirror the lib.rs doc-comment idiom). Create `src/enumerate.rs` with `pub async fn run(store: &Store, client: &GraphQlClient, resume: bool) -> Result<(), EnumerateError>` following RESEARCH §"Opaque-Cursor Pagination Loop" exactly:

    Resume selection (D-01/D-02/D-03): if `resume`, call `store.latest_unfinished_run()`; on `Some(run)` reuse `run.run_id` and set `after = run.last_cursor`; on `None` (or non-resume) call `store.begin_run("{}")` for a fresh run_id with `after = None`. Per RESEARCH Open Q2 (lines 479-481): on resume keep the existing `max_lev_id_start` if non-null; only set it when null.

    Drift probe start (D-09): `let max_start = client.stats().await?.max_lev_id;` then `store.set_run_max_lev_start(run_id, max_start)` (only if not already set on a resume).

    The loop: call a bounded-retry helper `fetch_page_with_retry(client, after, LIMIT)` where `LIMIT = 500` (Discretion A4 — the ceiling minimizes round-trips). Branch on the result:
    - `Err(ClientError::Graphql { code: Some(c), .. }) if c == "INVALID_CURSOR"` → set `after = None`, `continue` (D-08: restart page 1, NO abort, NO retry — distinct branch BEFORE the retry helper handles it, Pitfall 4).
    - `Err(e)` (retry exhausted / non-retryable) → `store.mark_run_aborted(run_id)`, print the error to stderr, return an error (D-06/D-07 — cursor untouched).
    - `Ok(page)` → (optional defense-in-depth: validate each pubkey is 64-char lowercase hex before persisting — V5) `store.insert_pubkeys(page.authors.clone())`; advance `count`; `eprintln!` running progress (D-04 stdout observability). THEN, only after the page's pubkeys are enqueued, handle the cursor: `match page.end_cursor { Some(c) => { store.set_run_cursor(run_id, &c)?; after = Some(c); } None => break }` — set_run_cursor strictly AFTER insert_pubkeys (Pitfall 2 / flush-before-cursor). Because both route through the single writer (plan 01 enum-message design) the pubkey batch is ordered before the cursor write; the cursor never advances past unwritten pubkeys.

    Clean termination (INGEST-01): on `break` (endCursor==null), `let max_end = client.stats().await?.max_lev_id;` then `store.mark_run_done(run_id, max_end)` (D-09 end probe).

    Bounded retry helper (RESEARCH §"Bounded Retry", Discretion): `const MAX_ATTEMPTS: u32 = 3`, exponential backoff base 250ms ×2 capped at 2s via `tokio::time::sleep`. Retryable: `Unavailable` (503), `Transport` (timeout/refused), codeless `"internal error"` Graphql. Non-retryable: `PayloadTooLarge` (413), `INVALID_CURSOR` (handled by the restart branch, never reaches retry), validation messages, `TOO_MANY_AUTHORS`.

    Sync/async boundary (Pitfall 1): the walk is strictly sequential — one in-flight request, persist, repeat. `store.insert_pubkeys`/`set_run_cursor` are plain synchronous calls inside the async fn. Do NOT wrap the store in `tokio::sync::Mutex`; do NOT spawn the walk across tasks.

    Define a small `EnumerateError` (wrapping `ClientError` + `rusqlite::Error`) — `thiserror` if available. Add the `#[cfg(test)]` block with the 8 unit behaviors + property test above, driving the walk against a hand-rolled tokio stub server (or wiremock dev-dep from plan 02) and a temp-FILE store (`tempfile::TempDir`, never `:memory:`).
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/pubkey_iterator &amp;&amp; cargo test --lib enumerate::</automated>
  </verify>
  <acceptance_criteria>
    - All 8 named unit tests + the property test pass: `cargo test enumerate::tests::terminates_on_null_cursor`, `::resume_from_last_cursor`, `::pubkeys_idempotent_on_resume`, `::retry_503_no_cursor_advance`, `::invalid_cursor_restarts`, `::abort_preserves_cursor`, `::records_drift_does_not_abort`, `::resume_boundary_union_complete`.
    - The INVALID_CURSOR branch resets `after` to None and does NOT call `mark_run_aborted` and does NOT sleep/retry (asserted via mock call count).
    - On forced 503-exhaustion the run row has `status == "aborted"` and `last_cursor` equals the last successfully-persisted page's `end_cursor` (unchanged by the failed page).
    - `set_run_cursor` is only reachable after `insert_pubkeys` in the same loop iteration (source ordering assertion: in `src/enumerate.rs` the `insert_pubkeys` call precedes `set_run_cursor` within the Ok-arm).
    - `grep -n 'tokio::sync::Mutex' src/enumerate.rs` returns nothing (store is the sync boundary, Pitfall 1).
    - `cargo test --lib enumerate::` passes with no network access.
  </acceptance_criteria>
  <done>The walk terminates cleanly on null cursor, resumes from the stored cursor, persists idempotently, retries 503 without advancing the cursor, aborts with a preserved cursor on exhaustion, restarts on INVALID_CURSOR, and records the drift probe — all proven against mocks.</done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal --resume binary entry point and document the manual connectivity proof</name>
  <files>src/main.rs, Cargo.toml</files>
  <read_first>
    - src/enumerate.rs (Task 1: `pub async fn run(&store, &client, resume)`)
    - src/store/mod.rs lines 70-80 (`Store::open`), lines 117-127 (`close`)
    - src/graphql/client.rs (plan 02: `GraphQlClient::new(endpoint)`)
    - RESEARCH §"Minimal entry point" (lines 434-447, the `#[tokio::main]` shape), A2 (endpoint via `LMDB2GRAPHQL_URL` env, default loopback), D-12 (minimal — NOT the full Phase-5 clap surface); PATTERNS §`src/main.rs` (lines 179-181)
    - contract.md §10 (default bind `127.0.0.1:8080`, plain loopback HTTP), §2 (POST `/graphql` returns 503 until ready)
  </read_first>
  <action>
    Create `src/main.rs` with `#[tokio::main] async fn main() -> Result<(), Box<dyn std::error::Error>>` (D-12 minimal):
    - Parse `--resume` as a single bool from `std::env::args().any(|a| a == "--resume")` — do NOT build the full clap CLI (that is Phase 5's `run`/`export`, D-12; clap optional and unnecessary for one flag).
    - Resolve the endpoint from `std::env::var("LMDB2GRAPHQL_URL")` defaulting to `http://127.0.0.1:8080/graphql` (A2; contract §10).
    - `let store = pubkey_iterator::store::Store::open(std::path::Path::new("spamhunter.sqlite"))?;`
    - `let client = pubkey_iterator::graphql::GraphQlClient::new(endpoint);`
    - `pubkey_iterator::enumerate::run(&store, &client, resume).await?;`
    - `store.close()?;` (flushes the writer so all pubkeys + the final cursor/done update are durable before exit).

    If the crate needs an explicit `[[bin]]` (it is library-only today), add it to Cargo.toml pointing at `src/main.rs`; the binary calls only the public `lib.rs` API. Keep the on-disk DB path a simple constant for Phase 2 (config-driven path is OPS-03 / Phase 5 territory).

    In the SUMMARY, document the MANUAL connectivity proof (the phase's vertical-slice gate, 02-VALIDATION.md Manual-Only Verifications): with a live LMDB2GraphQL adapter reachable, run `LMDB2GRAPHQL_URL=<url> cargo run` (and `cargo run -- --resume` to verify resume), confirm the walk completes, terminates on `hasMore:false`, the hand-written query string + serde structs deserialize the real v1.2 response, and the `pubkey`/`run` rows are queryable in `spamhunter.sqlite`. This step cannot run in CI without the adapter — it is a manual must_have.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/spamhunter/pubkey_iterator &amp;&amp; cargo build --bin pubkey_iterator 2>/dev/null || cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.rs` exists with `#[tokio::main]` and parses `--resume` from `std::env::args()` (no clap dependency added for the flag).
    - `grep -n 'LMDB2GRAPHQL_URL' src/main.rs` matches; the default falls back to `http://127.0.0.1:8080/graphql`.
    - `main` calls `Store::open`, `GraphQlClient::new`, `enumerate::run(..).await`, then `store.close()` in that order.
    - `cargo build` produces a runnable binary (the first binary in this previously library-only crate).
    - The full suite stays green: `cargo test` passes.
    - SUMMARY documents the manual live-adapter connectivity-proof procedure and marks it a manual must_have.
  </acceptance_criteria>
  <done>A minimal `--resume` binary drives the walk against the env-configured (default loopback) adapter and flushes durably on exit; the manual connectivity proof is documented as the phase's vertical-slice gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter → walk (pubkey strings) | pubkeys from the adapter response are persisted into the DB |
| operator → binary (env / --resume) | operator supplies the endpoint URL and resume flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | Returned pubkeys persisted to DB | mitigate | Defense-in-depth: validate each returned pubkey is 64-char lowercase hex before `insert_pubkeys` (V5 input validation); the contract guarantees the shape (§5/§6.4) but validate before the DB write. Persistence binds are parameterized (plan 01). |
| T-02-09 | Tampering | Opaque cursor handling | mitigate | The cursor is passed back verbatim as `after`, never parsed or constructed (contract §6.4); stored/bound as an opaque string — no injection surface. An observed INVALID_CURSOR is treated as a client bug → restart page 1 (D-08). |
| T-02-10 | Tampering / SSRF | Operator-supplied `LMDB2GRAPHQL_URL` | accept | Endpoint is operator-supplied (default loopback), not user input — no SSRF surface in Phase 2. Forward constraint for OPS-03 / Phase 4: a config-driven URL must validate scheme/host. |
| T-02-11 | Information disclosure | stderr progress / error logging | mitigate | Log status + first error message only; do not dump full response bodies (V7). |
</threat_model>

<verification>
- `cargo test` (full suite) green: 8 enumerate unit tests + property test + plan-01 store tests + plan-02 graphql tests.
- `cargo build` produces the binary; `cargo run -- --resume` parses the flag.
- MANUAL: a live-adapter walk completes and writes queryable `pubkey`/`run` rows (connectivity proof — the phase's vertical-slice gate; requires a reachable adapter, not CI-runnable).
</verification>

<success_criteria>
The connectivity-proving vertical slice runs end-to-end: the binary enumerates every distinct pubkey resumably through the live adapter into SQLite, terminating cleanly and surviving 503 / INVALID_CURSOR / in-body errors / snapshot drift — INGEST-01 and INGEST-04 satisfied, with the manual live-adapter proof documented.
</success_criteria>

<output>
Create `.planning/phases/02-graphql-client-author-enumeration/02-03-SUMMARY.md` when done.
</output>
