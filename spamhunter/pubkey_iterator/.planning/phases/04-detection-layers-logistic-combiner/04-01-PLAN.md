---
phase: 04-detection-layers-logistic-combiner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/config.rs
  - src/detect/mod.rs
  - pubkey_iterator_config.example.toml
autonomous: true
requirements: [DETECT-05, SCORE-01, SCORE-04, SCORE-05, OPS-02, OPS-03]
must_haves:
  truths:
    - "A TOML config file at a caller-supplied path loads into typed structs (adapter_url, whitelist_url, tau, bias, per-layer enable+weight+threshold)."
    - "The Layer trait, a fixed-order ScoringStage registry, and the sigmoid(Σwᵢxᵢ+b) combiner produce a per-pubkey score from one trivial end-to-end layer."
    - "A scored pubkey persists a score row plus per-layer signal rows with non-empty evidence JSON through the existing single-writer store."
    - "Every enumerated pubkey — including a zero-event pubkey — gets a score row (match_groups wired into the production fetch closure, D-15)."
    - "Re-running the same fixture corpus with the same weights produces byte-identical score and signal rows (OPS-02)."
    - "Weights/bias/τ are read from the weight table, seeded from config on first run; no enforcement side-effect occurs (SCORE-04)."
  artifacts:
    - path: "src/config.rs"
      provides: "serde TOML Config + path-arg loader (D-09); never reads the real ~/deepfry file in tests"
      min_lines: 60
      contains: "pub fn load"
    - path: "src/detect/mod.rs"
      provides: "Layer trait, LayerOutput, ScoringStage (registry + combiner), score()"
      min_lines: 120
      contains: "pub trait Layer"
    - path: "pubkey_iterator_config.example.toml"
      provides: "committed in-repo example documenting the config shape (D-09)"
      contains: "whitelist_url"
    - path: "Cargo.toml"
      provides: "Phase-4 deps unicode-segmentation, url, toml"
      contains: "toml"
  key_links:
    - from: "src/detect/mod.rs"
      to: "src/store/mod.rs"
      via: "ScoringStage builds a Persist; store.persist(persist) → WriteMsg::Score"
      pattern: "store.*persist|Persist"
    - from: "src/detect/mod.rs"
      to: "src/model.rs"
      via: "constructs Persist + SubScore from the existing model"
      pattern: "SubScore|Persist"
    - from: "src/config.rs"
      to: "src/detect/mod.rs"
      via: "ScoringStage::from_config / weight-table seed reads Config layer entries"
      pattern: "from_config|Config"
---

<objective>
Build the Phase-4 walking slice: the thinnest real end-to-end per-pubkey verdict. A TOML config (OPS-03) seeds the `weight` table; a shared `Layer` trait (DETECT-05) feeds a fixed-order `ScoringStage` whose logistic combiner (SCORE-01) fuses sub-scores into a persisted `score` row + per-layer `signal` rows with evidence (SCORE-05); the production fetch closure wires `match_groups` so every enumerated pubkey — zero-event included — reaches the stage and gets a score (D-15); and the whole path is deterministic (OPS-02). One trivial layer proves the slice end-to-end; the four real layers land in Plans 02–03.

Purpose: Establish the integration seam every later layer plugs into, and resolve the Phase-3 `match_groups` carry-forward, before adding real detection logic.
Output: `src/config.rs`, `src/detect/mod.rs`, `pubkey_iterator_config.example.toml`, Phase-4 deps in `Cargo.toml`, lib module wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-detection-layers-logistic-combiner/04-CONTEXT.md
@.planning/phases/04-detection-layers-logistic-combiner/04-RESEARCH.md
@.planning/phases/04-detection-layers-logistic-combiner/04-VALIDATION.md
@src/model.rs
@src/store/mod.rs
@src/store/writer.rs
@src/store/queries.rs
@src/pipeline.rs
@src/fetch.rs
@src/graphql/queries.rs
@Cargo.toml
</context>

<artifacts_this_phase_produces>
This plan introduces (later plans extend):
- `Layer` trait (object-safe: `Send + Sync`, `fn name(&self) -> &'static str`, `fn score(&self, events: &[Event], whitelisted: bool) -> LayerOutput`).
- `LayerOutput { value: f64 /* xᵢ∈[0,1] */, evidence: serde_json::Value }`.
- `ScoringStage { layers: Vec<Box<dyn Layer>>, weights: Vec<f64>, bias: f64, tau: f64 }` with `from_config(...)` and `score(run_id, pubkey, events, whitelisted) -> Persist`.
- `Config` serde struct + `pub fn load(path: &Path) -> Result<Config, ConfigError>` (path-arg, D-09).
- Stable `signal.layer` keys reserved here, implemented across plans: `L0_whitelist_absence` (Plan 03), `L1_near_duplicate` (Plan 02), `L3_content_entropy` (Plan 02), `L4_link_mention` (Plan 03).
- New deps: `unicode-segmentation` 1.13, `url` 2.5, `toml` 1.1.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config loader + Phase-4 deps + lib wiring</name>
  <read_first>
    - src/store/mod.rs (the `begin_run(config_json)` path + temp-FILE `temp_db()` test idiom to mirror for temp-dir config tests)
    - .planning/phases/04-detection-layers-logistic-combiner/04-RESEARCH.md §Config TOML (the recommended shape) and §Standard Stack (Phase-4-owned additions)
    - Cargo.toml (dep-discipline header: deps land in their owning phase; add only unicode-segmentation/url/toml — NOT gaoya/linfa)
  </read_first>
  <files>Cargo.toml, src/config.rs, src/lib.rs, pubkey_iterator_config.example.toml</files>
  <behavior>
    - Test: load a temp-dir TOML with all keys → Config has adapter_url, whitelist_url, tau=0.5, bias=-4.0, and four layer entries each with enabled/weight plus layer-specific params.
    - Test: load on a missing path returns a typed `ConfigError`, never panics.
    - Test: a disabled layer entry (`enabled=false`) round-trips as enabled=false.
    - Test (D-09 repo rule): the loader takes a `&Path` argument; the test writes to a `tempfile::TempDir`, never `~/deepfry`.
  </behavior>
  <action>
    Add to `[dependencies]` in Cargo.toml exactly three Phase-4 crates with the research-verified versions: `unicode-segmentation = "1.13"`, `url = "2.5"`, `toml = "1.1"`. Do NOT add gaoya, linfa, simhash, xxhash-rust, or ahash (dep discipline; SimHash is hand-rolled in Plan 02). Create `src/config.rs` defining a serde-derived `Config` struct matching the RESEARCH §Config TOML shape: top-level `adapter_url: String`, `whitelist_url: String`, `tau: f64`, `bias: f64`, and a `layers` map/struct holding four entries keyed `L0_whitelist_absence`, `L1_near_duplicate`, `L3_content_entropy`, `L4_link_mention`, each with `enabled: bool`, `weight: f64`, and its layer-specific tunables (L0: `absence_subscore`; L1: `hamming_threshold`, `shingle_size`, `min_events`; L3: `entropy_low`, `entropy_high`, `min_len_for_low`, `emoji_density_knee`, `hashtag_density_knee`; L4: `url_ratio_knee`, `domain_concentration_knee`, `mean_ptags_knee`, `mean_ttags_knee`, `min_events`). Use the conservative D-08 defaults from RESEARCH §L7/§L1/§L3/§L4 as the literal example values. Add `pub fn load(path: &std::path::Path) -> Result<Config, ConfigError>` that reads the file and `toml::from_str`s it; define `ConfigError` via `thiserror` (a Read variant and a Parse variant). The loader MUST accept a path argument so config-loading tests use a temp dir and never touch the real `~/deepfry/pubkey_iterator_config.toml` (repo rule + D-09). Write `pubkey_iterator_config.example.toml` at repo root documenting the shape with the default values. Register `pub mod config;` in src/lib.rs. Add the four `#[test]` cases described in behavior, each writing the TOML body into a `tempfile::TempDir`-derived path.
  </action>
  <verify>
    <automated>cargo test --lib config::</automated>
  </verify>
  <done>Config loads from a temp-dir TOML into typed structs with the conservative defaults; missing-path returns a typed error; the loader is path-argument-based; the example file exists at repo root; only the three Phase-4 deps were added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layer trait + ScoringStage combiner + weight-table seed/read</name>
  <read_first>
    - src/model.rs (Persist, SubScore, Weight — the exact fields ScoringStage builds and the weight-table model)
    - src/store/mod.rs (`begin_run`, `persist`, `run_write_conn` pattern for a weight-table seed write that touches only `weight`; `reader()` for reads)
    - src/store/writer.rs (UPSERT_SCORE / UPSERT_SIGNAL: writer iterates `subscores` Vec in FIXED order — the determinism guarantee ScoringStage must preserve)
    - src/graphql/queries.rs (Event fields: content, tags, kind, created_at — the layer input)
    - .planning/phases/04-detection-layers-logistic-combiner/04-RESEARCH.md §Pattern 1, §Pattern 2, §L7 Combiner (weights L0=0.8/L1=2.0/L3=1.5/L4=1.5, bias=-4.0, τ=0.5; the multi-signal-agreement weight budget) and §Persistence (weight seed/read helpers)
  </read_first>
  <files>src/detect/mod.rs, src/lib.rs</files>
  <behavior>
    - Test: a `LayerOutput.value` outside [0,1] trips a debug_assert in `ScoringStage::score`.
    - Test: with one trivial layer emitting value=v under weight w and bias b, `ScoringStage::score` returns a Persist whose `score == 1/(1+e^-(b + w*v))` and whose subscores carry that layer's name + value + non-empty evidence.
    - Test (multi-signal structural property, seeded with the conservative weights): a single layer at value=1.0 (others 0.0) yields score < τ (not suspected); two-plus strong layers yield score > τ (suspected). (Full four-layer assertion lands in Plan 03; here assert the property with synthetic layers.)
    - Test: weight-table seed-on-empty writes the six rows (four layer weights + `_bias` + `_threshold`) from config; a second call reads the stored values rather than re-seeding.
    - Test (OPS-02): `ScoringStage::score` over the same events twice returns equal Persist (no RNG, no HashMap iteration in the sum).
  </behavior>
  <action>
    Create `src/detect/mod.rs`. Define `pub struct LayerOutput { pub value: f64, pub evidence: serde_json::Value }` and the object-safe `pub trait Layer: Send + Sync { fn name(&self) -> &'static str; fn score(&self, events: &[crate::graphql::queries::Event], whitelisted: bool) -> LayerOutput; }` per RESEARCH §Pattern 1. Define `pub struct ScoringStage { layers: Vec<Box<dyn Layer>>, weights: Vec<f64>, bias: f64, tau: f64 }`. Implement `pub fn score(&self, run_id: i64, pubkey: &str, events: &[Event], whitelisted: bool) -> crate::model::Persist`: start `z = self.bias`, iterate `self.layers` in index order (FIXED order — never a HashMap; this is the OPS-02 fixed-sum-order guarantee per RESEARCH §Pattern 2 and the writer.rs precedent), for each layer call `score(...)`, `debug_assert!((0.0..=1.0).contains(&out.value))`, accumulate `z += self.weights[i] * out.value`, and push a `SubScore { layer: layer.name().to_string(), value: out.value, evidence: Some(out.evidence.to_string()) }`. Compute `score = 1.0/(1.0 + (-z).exp())` and `suspected = score > self.tau`. Return `Persist { run_id, pubkey: pubkey.to_string(), score, whitelisted, suspected, subscores }`. A disabled layer is OMITTED from `layers` at build time (writes no signal row) — do not evaluate-then-zero. Add weight-table helpers per RESEARCH §Persistence: `read_weights(conn) -> rusqlite::Result<Vec<crate::model::Weight>>` (parameterized SELECT over the `weight` table, ORDER BY layer for stable read order) and `seed_weights_if_empty(store, &config)` that, when the weight table has no rows for these keys, INSERTs the six rows (`L0_whitelist_absence`→0.8, `L1_near_duplicate`→2.0, `L3_content_entropy`→1.5, `L4_link_mention`→1.5, `_bias`→-4.0, `_threshold`→0.5 using the `threshold` column) via the `run_write_conn`-style short-lived connection that touches ONLY the `weight` table (does not race the writer actor). Add `ScoringStage::from_config(&Config, weights: &[Weight])` that builds the fixed-order `layers` Vec (only the enabled layers, in declared order L0,L1,L3,L4) and the positional `weights` Vec by name lookup, plus bias and τ. For THIS plan the four real layers do not exist yet — provide an internal test-only trivial layer (or a `from_layers` constructor) so Task 2's combiner tests run; the real layers register in Plans 02–03. Register `pub mod detect;` in src/lib.rs. Use `params![]` for every weight write (T-04-01). Add the five tests from behavior. Do not write any enforcement side-effect — the only outputs are weight rows and the returned Persist (SCORE-04).
  </action>
  <verify>
    <automated>cargo test --lib detect::</automated>
  </verify>
  <done>Layer trait + ScoringStage compile and are object-safe; the combiner computes sigmoid over a fixed positional sum; the multi-signal structural property holds (single layer < τ, multi-signal > τ); weight seed-on-empty writes six rows and subsequent reads return stored values; score is deterministic across re-runs; all weight writes are parameterized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire match_groups into a production scoring pipeline (D-15 zero-event end-to-end)</name>
  <read_first>
    - src/pipeline.rs (`run_pipeline<F,Fut,C>` seam: the injected `consumer: Fn(&AuthorGroup)` and the `fetch` closure; the `injected_consumer_seam` test as the analog)
    - src/fetch.rs (`match_groups(requested, groups) -> Vec<(&str, Vec<Event>)>` — built but UNWIRED; the `match_groups_no_shift` test proves omitted authors become empty vecs)
    - src/store/mod.rs (`Store::open`, `begin_run`, `persist`, `close`)
    - .planning/phases/04-detection-layers-logistic-combiner/04-RESEARCH.md §Architecture diagram (note A: resolve whitelist in the fetch stage), §Wiring match_groups in the fetch stage (D-15), §Common Pitfalls Pitfall 3
  </read_first>
  <files>src/pipeline.rs, src/detect/mod.rs</files>
  <behavior>
    - Test (D-15, integration over a temp store): drive `run_pipeline` over a requested pubkey list where a mock fetch OMITS one pubkey (zero events). After the run, the omitted pubkey HAS a `score` row — proving match_groups wiring scores every enumerated pubkey.
    - Test (OPS-02 end-to-end): run the same fixture corpus twice into two fresh temp DBs; `read_scores` and `read_signals` (ordered) are byte-identical across runs (mirror `store::tests::rerun_is_deterministic`).
    - Test: the trivial-layer ScoringStage consumer persists a score row + signal row(s) with non-empty evidence for a normal (non-zero-event) pubkey (SCORE-05).
  </behavior>
  <action>
    Add a production fetch+score wiring that closes the Phase-3 seam. In a fetch closure (the `F: Fn(Vec<String>) -> Fut` passed to `run_pipeline`), after `fetch_batch(&client, 1, per_author, &batch).await?`, call `crate::fetch::match_groups(&batch, groups)` and rebuild one `AuthorGroup { author: pk.to_string(), events }` per REQUESTED pubkey so omitted (zero-event) authors become empty-`events` groups that still cross the channel (RESEARCH §Wiring match_groups; D-15). The consumer closure owns a `ScoringStage` and a `&Store` (via `Arc`/clone as the seam requires `Send + Sync + 'static`); for each `&AuthorGroup` it calls `stage.score(run_id, &group.author, &group.events, whitelisted)` and `store.persist(persist)`. For THIS plan pass `whitelisted=false` (the real L0 async lookup lands in Plan 03) and use the trivial/test layer registry — the slice must produce a real persisted score for every pubkey including zero-event ones. Keep the whitelist lookup OUT of the consumer (Pitfall 5 — never block the consumer on HTTP; resolved in the fetch stage in Plan 03). Add the three behavior tests using the `mock_fetch`/`synthetic_authors`/`block_on` idiom from pipeline.rs and the temp-store idiom from store/mod.rs. The zero-event test MUST assert `count(score)` includes the omitted pubkey. Do not introduce RNG and do not iterate any HashMap in the score path (OPS-02).
  </action>
  <verify>
    <automated>cargo test --lib detect:: pipeline::</automated>
  </verify>
  <done>A zero-event (adapter-omitted) pubkey gets a `score` row via match_groups wiring; re-running the same fixture corpus twice yields byte-identical score/signal rows; a normal pubkey persists a score + signal row with non-empty evidence; no enforcement side-effect, no RNG, no HashMap in the score path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event content/tags → layers | `Event.content` and `Event.tags` are UNTRUSTED adapter-relayed text; layers treat them as opaque (this plan only passes them through). |
| config file → loader | `pubkey_iterator_config.toml` is operator-supplied; URLs are operator-trusted, not end-user input. |
| score/signal/weight → SQLite | all writes funnel through the single-writer store (or the weight short-lived conn) using `params![]`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | weight-table seed write + score/signal persist | mitigate | every value bound with `params![]` / `?N`; never `format!` a pubkey, layer name, weight, or value into SQL (existing store invariant). |
| T-04-02 | Repudiation | non-deterministic scoring | mitigate | zero RNG; fixed positional layer-sum order; UPSERT on `(run_id,pubkey)`; OPS-02 re-run test asserts byte-identical rows. |
| T-04-03 | DoS | adversarial event content | accept | events already capped (`per_author` ≤100, 256 KiB body, contract); this plan's trivial layer is O(events). Real-layer bounds land in Plans 02–03. |
| T-04-04 | Elevation/Integrity | enforcement side-effect | mitigate | SCORE-04 — the only outputs are SQLite rows; no whitelist/quarantine write, no event mutation. Verified by the persistence tests asserting only score/signal/weight rows change. |
| T-04-SC | Tampering | crates.io installs (unicode-segmentation/url/toml) | mitigate | RESEARCH §Package Legitimacy Audit verdict OK for all three (downloads/repo/age); crates.io has no postinstall vector. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `cargo test --lib config:: detect:: pipeline::` green.
- Config loader takes a path argument; no test references `~/deepfry`.
- ScoringStage iterates a positional Vec (no HashMap) in the score path.
- Zero-event pubkey gets a score row; re-run determinism holds.
- Only `unicode-segmentation`, `url`, `toml` added to Cargo.toml.
</verification>

<success_criteria>
The walking slice runs end-to-end: config → weight seed → Layer trait → ScoringStage combiner → persisted score + signal-with-evidence for every enumerated pubkey (zero-event included), deterministically, with no enforcement side-effect. The four real layers plug into this `ScoringStage` registry in Plans 02–03.
</success_criteria>

<output>
Create `.planning/phases/04-detection-layers-logistic-combiner/04-01-SUMMARY.md` when done.
</output>
