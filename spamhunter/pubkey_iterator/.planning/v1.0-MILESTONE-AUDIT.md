---
milestone: v1.0
audited: 2026-06-26
status: passed   # OPS-01 text/impl drift reconciled 2026-06-26 (REQUIREMENTS.md OPS-01 now matches D-02)
status_was: tech_debt
scores:
  requirements: 22/22   # OPS-01 reconciled — wording now matches D-02 (no `label` subcommand)
  phases: 6/6
  integration: 6/6 seams wired
  flows: 2/2 E2E flows complete
gaps:
  requirements: []   # no unsatisfied requirements — no FAIL-gate blocker
  integration: []
  flows: []
tech_debt:
  - phase: 05-cli-run-export
    items:
      - "RESOLVED 2026-06-26: OPS-01 text/impl drift. REQUIREMENTS.md OPS-01 reconciled to match decision D-02 — wording now states the CLI is `run, export, tune` and that labeling is intentionally SQL-direct into the `backpropagation` table (no `label` subcommand), satisfying TUNE-01. No code change; the decision was honored rather than reversed."
  - phase: 02-graphql-client-author-enumeration
    items:
      - "VERIFICATION status `human_needed`: the live-adapter full-walk connectivity proof is not CI-re-runnable. It WAS satisfied during execution against http://192.168.149.21:8080/graphql (3500→5500 pubkeys, maxLevId 47928105 matched). Tracked, not a blocker."
  - phase: cross-cutting
    items:
      - "SUMMARY.md `requirements_completed` frontmatter is sparsely populated (only 02-02/02-03/03-02/05-02/06-02 carry it). Per-phase VERIFICATION.md requirement tables carry the authoritative evidence; this is a documentation-completeness gap only."
verification:
  note: "Phase 6 VERIFICATION and this audit's source checks were performed inline. The org monthly spend limit was noted (Phase 6) as having blocked some subagent spawns; the gsd-integration-checker subagent ran successfully for this audit."
nyquist:
  overall: n/a   # validate-phase step hook not active for this project
---

# Milestone v1.0 — Audit: Spamhunter Pubkey Spam Classifier

**Status: TECH DEBT** — all 22 v1 requirements substantively met, all 6 phases verified, all cross-phase seams wired and E2E flows complete. One documentation/scope reconciliation item (OPS-01) and two tracked non-blockers.

## Requirements Coverage (3-source cross-reference)

All 22 v1 requirements are `[x]` in REQUIREMENTS.md, each maps to a phase whose VERIFICATION.md is `passed` (Phase 02 = `human_needed`, live proof satisfied during execution).

| Req | Phase | VERIFICATION | Integration | Final |
|-----|-------|--------------|-------------|-------|
| INGEST-01 | 02 | passed (human_needed)* | WIRED | satisfied |
| INGEST-02 | 03 | passed | WIRED | satisfied |
| INGEST-03 | 03 | passed | WIRED | satisfied |
| INGEST-04 | 02→03 | passed | WIRED (match_groups by author, not index) | satisfied |
| DETECT-01..05 | 04 | passed | WIRED | satisfied |
| SCORE-01 | 04 | passed | WIRED (one shared `detect::combine`) | satisfied |
| SCORE-02 | 01 | passed | WIRED | satisfied |
| SCORE-03 | 05 | passed | WIRED | satisfied |
| SCORE-04 | 04 | passed | WIRED (pubkey-level only, no enforcement) | satisfied |
| SCORE-05 | 04 | passed | WIRED (per-layer evidence persisted+exported) | satisfied |
| TUNE-01 | 06 | passed | WIRED (backpropagation table; labels via SQL) | satisfied |
| TUNE-02 | 06 | passed | WIRED (linfa-logistic fit) | satisfied |
| TUNE-03 | 06 | passed | WIRED (latest weights snapshot into next run) | satisfied |
| TUNE-04 | 06 | passed | WIRED (deterministic negative sampling) | satisfied |
| TUNE-05 | 06 | passed | WIRED (STRICT no-regression backtest gate) | satisfied |
| OPS-01 | 05 | passed | **PARTIAL** (no `label` subcommand — see tech debt) | satisfied (capability) |
| OPS-02 | 04 | passed | WIRED (deterministic) | satisfied |
| OPS-03 | 04 | passed | WIRED (config-file weights/thresholds) | satisfied |

\* Phase 02 live-adapter proof satisfied during execution, not CI-re-runnable.

**Orphans:** none — every traceability-table requirement appears in a phase VERIFICATION.

## Cross-Phase Integration (gsd-integration-checker)

6/6 seams **WIRED**, 0 orphaned, 0 broken. Build clean; 110 lib + 2 bin tests pass (1 ignored = live integration test, self-skipping by design).

1. enumerate (P2) → `store.insert_pubkeys` single-writer (flush-before-cursor) — WIRED
2. enumerate output → pipeline `read_pubkeys` source (decoupled via DB) — WIRED
3. `fetch_batch` match-by-author (P3) → detect consumes groups (P4); INGEST-04 empty-group omission honored — WIRED
4. `detect::combine()` is genuinely ONE shared function — used by both `ScoringStage::score` (run-time) and `tune::backtest` (re-scoring). Highest-risk seam, verified byte-identical math. — WIRED
5. `run` → `run_batch` → score+signal persist (P1) → `export` reads back (P5) — WIRED
6. `tune`: signal × backpropagation join → linfa fit → backtest gate → provenance write → next run reads latest weights (TUNE-03 loop closes) — WIRED

**E2E flows:**
- Primary (enumerate → fetch → pipeline → detect → combine → score → export): COMPLETE
- Correctability loop (label → tune → backtest → re-score): COMPLETE (label entry is by SQL, not CLI — D-02)

## Tech Debt (no blockers)

**Phase 05 — OPS-01 text/impl drift (the one item to resolve):** REQUIREMENTS.md OPS-01 lists a `label` subcommand; the shipped CLI has `run`/`export`/`tune` only. This is intentional (D-02, documented in `src/store/schema.rs:71-73`) — labels are inserted directly into the `backpropagation` table. The TUNE-01 capability is satisfied. **Action:** reconcile OPS-01 wording to match D-02, or add a thin `label` subcommand.

**Phase 02 —** live-adapter proof not CI-re-runnable (satisfied during execution).

**Cross-cutting —** `requirements_completed` SUMMARY frontmatter sparsely populated (evidence lives in VERIFICATION tables instead).
