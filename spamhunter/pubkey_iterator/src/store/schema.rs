//! Embedded schema DDL.
//!
//! Task 1 stub — the full `SCHEMA_DDL` const (7 tables + 2 indexes) lands in
//! Task 2. Declared now so the crate compiles and the test harness can be
//! written against the real `Store` signatures (RED).

/// The 7 `CREATE TABLE IF NOT EXISTS` statements + indexes (filled in Task 2).
pub const SCHEMA_DDL: &str = "";
