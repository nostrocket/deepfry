---
phase: 01.1-go-binary-streaming-bridge-perf-01
plan: 03
type: execute
wave: 3
depends_on: ["01.1-02"]
files_modified:
  - src/ui/loader.ts
  - src/ui/verdict.ts
  - src/main.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PERF-01, DATA-01, DATA-03]

must_haves:
  truths:
    - "The loader shows binary-path stages Fetching → Receiving bytes (MB/s) → Building layout with no 'Parsing JSON' stage (D-09)"
    - "The verdict records server fetch+compute ms / stream ms / client decode ms / node+edge counts / peak JS heap for the bridge path"
    - "A fresh PERF-01 feasibility verdict is recorded confirming PASS (usable on the real dev DB, no swap, 60fps holds after load) vs the 01-03 JSON FAIL"
    - "ROADMAP Phase 2/3 success criteria + REQUIREMENTS traceability are updated to reflect degree/community/timestamps now computed server-side (D-06)"
  artifacts:
    - path: "src/ui/loader.ts"
      provides: "Binary-path stage labels + bytes/MB-s counter (D-09)"
      contains: "receive"
    - path: "src/ui/verdict.ts"
      provides: "Bridge feasibility verdict metrics (server/stream/decode ms, counts, peak heap)"
      contains: "peakHeap"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 01.1 fleshed out + Phase 2/3 criteria adjusted for server-side prep (D-06)"
      contains: "01.1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PERF-01 PASS + DATA-01/DATA-03 re-validated; OVER-01/OVER-02/FILT-01 notes that inputs are now server-side"
      contains: "PERF-01"
  key_links:
    - from: "src/main.ts"
      to: "src/ui/loader.ts"
      via: "bridge path mounts the loader and drives it with 'receive' stage progress"
      pattern: "mountLoader"
    - from: "src/main.ts"
      to: "src/ui/verdict.ts"
      via: "on bridge load complete, renders the bridge verdict with measured metrics"
      pattern: "renderVerdict|Verdict"
---

<objective>
Close PERF-01: adapt the loader/verdict instrument to the binary path (D-09), record a FRESH
feasibility verdict against the real dev Dgraph confirming the Go bridge PASSES where the 01-03
JSON wire FAILED, and propagate the D-06 cross-phase consequence — degree, community, and
timestamps now arrive precomputed from the bridge, so Phase 2 (OVER-01/OVER-02) and Phase 3
(FILT-01) no longer compute their inputs client-side — into ROADMAP.md and REQUIREMENTS.md.

Purpose: The loader has been a measurement instrument since Phase 1; this turns it onto the binary
path and produces the recorded artifact that resolves the 01-03 FAIL and unblocks Phases 2/3 with
an updated, accurate plan of record.
Output: Binary-path loader stages, a bridge verdict readout, a recorded PERF-01 PASS verdict (human
-verified on the real DB), and updated ROADMAP/REQUIREMENTS reflecting the server-side data-prep move.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-go-binary-streaming-bridge-perf-01/01.1-CONTEXT.md
@.planning/phases/01.1-go-binary-streaming-bridge-perf-01/01.1-RESEARCH.md

# The instruments to adapt + the wiring point:
@src/ui/loader.ts
@src/ui/verdict.ts
@src/main.ts
@src/transport/GoBridgeTransport.ts
</context>

<artifacts_produced>
This plan creates / extends:
- `src/ui/loader.ts`: STAGE_LABELS extended for the 'receive' stage (Fetching → Receiving bytes (MB/s) → Building layout); a bytes/MB-s readout
- `src/ui/verdict.ts`: a bridge-path verdict (e.g. `BridgeVerdictMetrics` + `renderBridgeVerdict`, or an extended VerdictMetrics) carrying serverComputeMs / streamMs / decodeMs / counts / peakHeapBytes — and a PASS criterion of "usable, no swap, 60fps holds" rather than the JSON 30s extrapolation
- `src/main.ts`: bridge-path branch that mounts the loader, times the load, measures peak heap, and renders the bridge verdict
- `.planning/ROADMAP.md`: Phase 01.1 fleshed out (goal/requirements/plans/verdict); Phase 2/3 success criteria adjusted per D-06
- `.planning/REQUIREMENTS.md`: PERF-01 → PASS; DATA-01/DATA-03 re-validated; OVER-01/OVER-02/FILT-01 traceability notes that inputs are bridge-provided
- A recorded PERF-01 feasibility verdict (in the SUMMARY + STATE Accumulated Context)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Adapt loader + verdict to the binary path and wire the bridge verdict in main.ts</name>
  <files>src/ui/loader.ts, src/ui/verdict.ts, src/main.ts</files>
  <read_first>
    - src/ui/loader.ts (STAGE_LABELS fetch/parse/layout; formatEdges; the no-percentage discipline; LoaderHandle)
    - src/ui/verdict.ts (VerdictMetrics shape; measurePeakHeap; extrapolateVerdict + the 30M/30s JSON extrapolation; renderVerdict console.table + panel)
    - src/main.ts (isDgraph verdict wiring: loadStart/firstParseAt timing, measurePeakHeap, renderVerdict; selectTransport already returns the bridge transport from Plan 02)
    - src/transport/GoBridgeTransport.ts (emits stage 'receive' then 'layout'; carries no encodingNs — the server compute time is server-side)
    - .planning/phases/01.1-go-binary-streaming-bridge-perf-01/01.1-CONTEXT.md (D-09 stages; Claude's-discretion PASS bar: usable on real dev DB, no swap, 60fps holds after load)
    - .planning/phases/01.1-go-binary-streaming-bridge-perf-01/01.1-RESEARCH.md (Loader/verdict adaptation section)
  </read_first>
  <action>
    loader.ts: add a 'receive' entry to STAGE_LABELS so the binary path reads Fetching… → Receiving
    bytes (MB/s)… → Building layout… (D-09). Keep the existing fetch/parse/layout entries (DgraphTransport
    still uses 'parse'). The 'receive' readout shows bytes received and a MB/s rate (derive rate from
    bytes / elapsed); keep the no-percentage discipline (cursor/stream has no honest upfront total).
    Avoid embedding any acceptance-criterion literal that a negative grep would later scan for.

    verdict.ts: add a bridge-path verdict path. The JSON path's linear-extrapolate-to-30M / ≤30s
    PASS_THRESHOLD does NOT apply to the bridge (the bridge loads the real graph directly, no
    extrapolation). Add a BridgeVerdictMetrics (serverComputeMs — time the server-side fetch+remap+
    degree+Louvain pass took, surfaced via a response timing header or measured as fetch-start→first-
    byte; streamMs — first-byte→last-byte; decodeMs — concat+view+buildGraphBuffers; nodeCount; edgeCount;
    peakHeapBytes; peakHeapSource) and a renderBridgeVerdict that console.tables + panels them with a
    qualitative PASS verdict per the Claude's-discretion bar (usable on the real dev DB, no swap, 60fps
    holds after load) — NOT a numeric 30s gate. Reuse measurePeakHeap unchanged.

    main.ts: add an isBridge branch parallel to isDgraph. Mount the loader, time loadStart, capture the
    'receive' transition and last-byte/layout transition for the stream/decode split, await
    measurePeakHeap after render, and call renderBridgeVerdict. Leave the dgraph and synthetic branches
    intact. crossOriginIsolated must still be true (COOP/COEP retained in Plan 02) so peak-heap
    measurement stays accurate.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && grep -q "receive" src/ui/loader.ts && grep -qiE "bridge.*verdict|renderBridgeVerdict|BridgeVerdict" src/ui/verdict.ts</automated>
  </verify>
  <done>tsc --noEmit + build pass; loader has the 'receive' stage + MB/s readout without removing 'parse'; verdict.ts has a bridge verdict that does NOT use the 30M/30s extrapolation; main.ts renders the bridge verdict on the ?transport=bridge path; dgraph/synthetic paths unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Record the PERF-01 feasibility verdict against the real dev Dgraph</name>
  <action>
    Run the full bridge pipeline end-to-end against the real dev Dgraph and record the measured
    feasibility verdict (server compute ms / stream ms / client decode ms / node+edge counts / peak
    JS heap), confirming PERF-01 PASS (usable, no swap, 60fps holds after load) vs the 01-03 JSON FAIL.
    Follow the how-to-verify steps below; this is a blocking human-verify checkpoint, not automated.
  </action>
  <what-built>
    The full bridge pipeline end-to-end: the Go bridge (Plan 01) reads the real dev Dgraph,
    computes degree + community + timestamps, and streams the binary frame; GoBridgeTransport
    (Plan 02) decodes it with zero JSON.parse; the loader (Task 1) shows the binary stages and
    the verdict panel records server/stream/decode ms + node/edge counts + peak heap.
  </what-built>
  <how-to-verify>
    1. Start Dgraph from the deepfry repo root: `docker-compose -f docker-compose.dgraph.yml up -d`
       (wait for gRPC :9080 to be ready).
    2. Start the bridge: `cd web-of-trust-explorer/bridge && make run` (binds 127.0.0.1:8081).
       Confirm it logs the NodeCount/EdgeCount after reading (expect ~365k follow-source nodes /
       ~1.5M profiles / tens of millions of edges per STATE.md).
    3. Sanity-check the wire: `curl -s --max-time 180 http://127.0.0.1:8081/graph.bin | head -c 4 | xxd`
       shows the WOTB magic.
    4. Start the app: `cd web-of-trust-explorer && npm run dev`, open
       http://localhost:5173/?transport=bridge in Chrome (the D-07 reference machine, M3 Pro).
    5. Watch the loader: it must show Fetching → Receiving bytes (MB/s) → Building layout (NO
       "Parsing JSON" stage) with the byte/MB-s counter ticking.
    6. Confirm the graph renders and that pan/zoom/hover still hold ~60fps AFTER load (the core value).
    7. Read the verdict panel + console.table: note server compute ms, stream ms, client decode ms,
       node/edge counts, peak JS heap. CRITICAL: the machine must NOT go into swap and the tool must
       be usable on the real dev DB (the PASS bar; contrast the 01-03 JSON FAIL where the machine
       swapped and was unusable).
    8. If Louvain dominates server compute or peak heap is alarming, note it — Plan 01 Task 2
       documented the fallback ladder (undirected Louvain → defer community); record which path was used.
    Report the measured numbers (server/stream/decode ms, counts, peak heap) and whether it PASSED
    (usable, no swap, 60fps holds) or surfaced a problem.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded numbers, or describe the failure (e.g. swap, slow Louvain, decode error, COEP block).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the PERF-01 verdict and propagate the D-06 server-side move to ROADMAP + REQUIREMENTS</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 01.1 stub at lines ~65-74 to flesh out; Phase 2 criteria ~82-88; Phase 3 criteria ~98-104; the Progress table ~114-118)
    - .planning/REQUIREMENTS.md (PERF-01 line ~58; DATA-01 ~12 / DATA-03 ~14; OVER-01/OVER-02 ~25-26; FILT-01 ~36; the Traceability table ~81-95)
    - .planning/phases/01.1-go-binary-streaming-bridge-perf-01/01.1-CONTEXT.md (D-06 cross-phase impact — the authoritative instruction for these edits)
    - The approved verdict numbers from the checkpoint above (record them verbatim)
  </read_first>
  <action>
    Use Edit (scoped replacements), NOT Write, on both planning files.

    ROADMAP.md — flesh out the Phase 01.1 entry: replace the "[Urgent work - to be planned]" goal with
    the real goal (Go binary-streaming bridge: server-side read+remap+degree+community+timestamps →
    binary HTTP stream → zero browser JSON.parse, resolving the 01-03 JSON FAIL); set Requirements:
    PERF-01 (DATA-01, DATA-03 re-validated); set Plans: 3 plans with the wave list (01.1-01 Go bridge,
    01.1-02 GoBridgeTransport decode, 01.1-03 loader/verdict + re-verdict); add a feasibility-verdict
    line recording the measured PASS numbers from the checkpoint. Then adjust the Phase 2 success
    criteria so degree (OVER-01) and community (OVER-02) are described as ENCODING bridge-provided
    arrays into style buffers (NOT computing them in a client Worker — that path is superseded by D-06);
    adjust Phase 3 criterion 4 (FILT-01) so the time filter reads bridge-provided kind3CreatedAt/
    last_db_update. Update the Progress table to add Phase 01.1 (3/3 once complete).

    REQUIREMENTS.md — set PERF-01 to PASS/Complete with the recorded verdict (replace the
    "TRIGGERED & PULLED FORWARD" framing with the resolution). Mark DATA-01 and DATA-03 as
    re-validated at scale via the bridge (no longer "carried by PERF-01" — PERF-01 delivered it).
    Add notes to OVER-01, OVER-02, FILT-01 that their inputs (degree, community, timestamps) are now
    produced server-side by the bridge (D-06). Update the Traceability table: PERF-01 → Phase 01.1
    Complete; DATA-01/DATA-03 → re-validated in Phase 01.1; OVER-01/OVER-02/FILT-01 keep their phases
    but note the server-side input source. Update the footer date/summary line.

    These are the D-06 propagation edits the CONTEXT marks MUST-propagate at planning/transition.
  </action>
  <verify>
    <automated>grep -q "PERF-01" .planning/ROADMAP.md && grep -qiE "bridge|server-side" .planning/ROADMAP.md && grep -qiE "PASS|Complete|re-validated" .planning/REQUIREMENTS.md && grep -qi "01.1" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP Phase 01.1 is fleshed out with goal/PERF-01/3-plan wave list/recorded verdict; Phase 2/3 criteria reflect bridge-provided (server-side) degree/community/timestamps per D-06; REQUIREMENTS PERF-01 is PASS with the verdict, DATA-01/DATA-03 re-validated, OVER-01/OVER-02/FILT-01 noted as server-side-sourced; all edits made with scoped Edit (no whole-file Write).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → running bridge/app | The human-verify checkpoint runs the full stack against the real read-only DB; no new trust boundary beyond Plans 01/02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-07 | Information Disclosure | verdict panel / console.table leaking pubkeys | accept | Local single-user dev tool; verdict reports counts + timings + heap, not pubkey contents |
| T-01.1-04 | Tampering | corrupt frame surfaced at the checkpoint | mitigate | decodeFrame MAGIC/VERSION guard (Plan 02) fails loudly before render; the checkpoint catches it |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run build` pass after the loader/verdict edits.
- `grep -q "receive" src/ui/loader.ts` (binary stage present); verdict.ts has a bridge verdict not gated on the 30M/30s extrapolation.
- The human-verify checkpoint is APPROVED with recorded numbers: server compute ms / stream ms / decode ms / node+edge counts / peak heap, and confirmation of usable / no swap / 60fps-holds (PERF-01 PASS).
- ROADMAP Phase 01.1 fleshed out; Phase 2/3 criteria reflect server-side prep; REQUIREMENTS PERF-01 PASS + D-06 notes — all via scoped Edit.
</verification>

<success_criteria>
- The loader shows the binary-path stages (Fetching → Receiving bytes (MB/s) → Building layout) with no JSON-parse stage, and the verdict records the bridge metrics.
- A fresh PERF-01 feasibility verdict is recorded confirming PASS (usable on the real dev DB, no swap, 60fps holds after load) vs the 01-03 JSON FAIL.
- ROADMAP + REQUIREMENTS are updated so Phases 2/3 consume bridge-provided degree/community/timestamps (D-06), with PERF-01 marked resolved.
</success_criteria>

<output>
Create `.planning/phases/01.1-go-binary-streaming-bridge-perf-01/01.1-03-SUMMARY.md` when done.
</output>
