---
phase: 03-write-path-correctness-regression-coverage
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - pkg/dgraph/dgraph_chunks_test.go
  - pkg/dgraph/dgraph_writepath_test.go
autonomous: true
requirements: [TEST-03, TEST-04]
security_enforcement: true

must_haves:
  truths:
    - "A unit test (no live Dgraph, runs under make test / -short) asserts chunkSlice count + membership at the 0/200/201/500/501/10000 boundaries (TEST-04, D-10)"
    - "An integration test (//go:build integration, make test-integration) asserts a >chunk-size follow-list persists its full follow set (TEST-03, D-11)"
    - "The integration test selects the highest-count fixture via testdata/largest-kind3-*.json glob and t.Skips with a harvest message when none exists (D-11)"
    - "The integration test cleans up its inserted nodes via t.Cleanup so the live graph is left untouched"
  artifacts:
    - path: "pkg/dgraph/dgraph_chunks_test.go"
      provides: "Table-driven unit test for chunkSlice boundary logic (no build tag, no Dgraph)"
      contains: "func TestChunkSlice"
    - path: "pkg/dgraph/dgraph_writepath_test.go"
      provides: "Integration test asserting full follow-set persistence for a large kind-3 fixture"
      contains: "//go:build integration"
  key_links:
    - from: "pkg/dgraph/dgraph_chunks_test.go"
      to: "chunkSlice (Plan 01)"
      via: "white-box package dgraph call"
      pattern: "chunkSlice\\("
    - from: "pkg/dgraph/dgraph_writepath_test.go"
      to: "AddFollowers (Plan 01)"
      via: "full-set call + count assertion"
      pattern: "AddFollowers\\("
---

<objective>
Add the regression coverage that proves the write-path fix: a unit test for the pure `chunkSlice` boundary logic (TEST-04, no live Dgraph) and an integration test that asserts a follow-list larger than the chunk size persists its FULL follow set to Dgraph (TEST-03, gated behind `//go:build integration`).

Purpose: TEST-04 catches a regression in chunk count / chunk membership without any infrastructure (runs in CI under `make test` / `-short`). TEST-03 reproduces the original data-drop against a real largest-kind-3 fixture harvested from the wild; it fails against pre-fix code and passes post-fix, and `t.Skip`s cleanly when no fixture has been harvested yet (acceptable per D-11, since harvesting needs a manual live crawl).

Output: `pkg/dgraph/dgraph_chunks_test.go` (unit, no build tag) and `pkg/dgraph/dgraph_writepath_test.go` (integration build tag). Both white-box `package dgraph`, plain `if`/`t.Fatalf`, no testify — mirroring `dgraph_stale_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-write-path-correctness-regression-coverage/03-CONTEXT.md
@.planning/phases/03-write-path-correctness-regression-coverage/03-PATTERNS.md
@.planning/phases/03-write-path-correctness-regression-coverage/03-01-SUMMARY.md
@pkg/dgraph/dgraph_stale_test.go
@pkg/dgraph/dgraph.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit test for chunkSlice boundary logic (TEST-04, D-10)</name>
  <files>pkg/dgraph/dgraph_chunks_test.go</files>
  <read_first>
    - pkg/dgraph/dgraph.go (the chunkSlice signature delivered by Plan 01)
    - pkg/dgraph/dgraph_stale_test.go (white-box package, plain if/t.Fatalf style, no testify)
    - .planning/phases/03-write-path-correctness-regression-coverage/03-PATTERNS.md (§ dgraph_chunks_test.go pattern)
    - .planning/phases/03-write-path-correctness-regression-coverage/03-01-SUMMARY.md (confirm chunkSlice final signature)
  </read_first>
  <action>Create `pkg/dgraph/dgraph_chunks_test.go` in `package dgraph` with NO `//go:build` tag so it runs under `make test` / `-short` (D-10, TEST-04). Add a table-driven `TestChunkSlice(t *testing.T)` covering the boundaries: 0→0 chunks; 200→1 chunk; 201→2 chunks [200,1]; 500→3 chunks; 501→3 chunks; 10000→50 chunks. For each case generate an input slice of N unique strings (a small `makeStrings(n int) []string` helper producing e.g. `fmt.Sprintf("%d", i)`), call `chunkSlice(in, 200)`, and assert: (a) `len(chunks)` equals the expected chunk count via `t.Fatalf`, and (b) the UNION of all chunk lengths equals N (membership — no items dropped or duplicated). Optionally assert no chunk exceeds size 200 and no empty trailing chunk. Use plain `if`/`t.Fatalf`, no external deps, matching `dgraph_stale_test.go` style. Match the final `chunkSlice` signature exactly as shipped by Plan 01 (confirm in 03-01-SUMMARY.md).</action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust && go test -short -run TestChunkSlice ./pkg/dgraph/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/dgraph/dgraph_chunks_test.go` exists, `package dgraph`, with NO `//go:build` tag (`grep -c 'go:build' pkg/dgraph/dgraph_chunks_test.go` == 0).
    - `go test -short -run TestChunkSlice ./pkg/dgraph/` passes with subtests for the 0/200/201/500/501/10000 cases.
    - The test asserts both chunk COUNT and membership UNION (`grep -c 'chunkSlice(' pkg/dgraph/dgraph_chunks_test.go` >= 1).
    - No testify / external test deps imported.
  </acceptance_criteria>
  <done>A no-infrastructure unit test pins chunkSlice's count + membership at every boundary and would fail on a regression in the chunk-splitting logic.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test — full follow-set persistence for a large kind-3 fixture (TEST-03, D-11)</name>
  <files>pkg/dgraph/dgraph_writepath_test.go</files>
  <read_first>
    - pkg/dgraph/dgraph_stale_test.go (//go:build integration tag, NewClient/EnsureSchema lifecycle, mustMutate helper, t.Cleanup teardown — DO NOT redeclare mustMutate)
    - pkg/dgraph/dgraph.go (AddFollowers signature + DeleteNodes for cleanup)
    - .planning/phases/03-write-path-correctness-regression-coverage/03-PATTERNS.md (§ dgraph_writepath_test.go pattern: fixture glob + t.Skip, fake-pubkey generation, assertion style)
    - .planning/phases/03-write-path-correctness-regression-coverage/03-CONTEXT.md (D-11 fixture mechanism)
  </read_first>
  <action>Create `pkg/dgraph/dgraph_writepath_test.go` with `//go:build integration` on line 1 and `package dgraph` (TEST-03, D-11), mirroring `dgraph_stale_test.go`. Write `TestAddFollowersLargeKind3(t *testing.T)` that: (1) globs `testdata/largest-kind3-*.json` and selects the file with the highest `<count>` parsed from its name; if none match, `t.Skip("no large kind-3 fixture found; run the crawler with the harvest flag/env to generate one")` — this skip is the expected state during this phase since harvesting needs a manual live crawl. (2) Decodes the fixture JSON into a Nostr event, extracts the p-tag followee pubkeys into a `map[string]struct{}` and a unique signer pubkey (use the fixture's pubkey, or a time-seeded `fmt.Sprintf("%064x", time.Now().UnixNano())` signer to avoid colliding with live graph data). (3) Calls `NewClient("localhost:9080")` + `EnsureSchema(ctx)` (copy the analog lifecycle), registers a `t.Cleanup` that resolves the inserted signer + followee UIDs and calls `c.DeleteNodes(ctx, uids)` so the live graph is left untouched. (4) Calls `c.AddFollowers(ctx, signer, createdAt, follows, true)` ONCE, then queries the signer's persisted `follows` edge count and asserts it equals `len(follows)` with `t.Fatalf("expected %d follow edges, got %d — chunked write dropped %d entries", want, got, want-got)`. Add a comment documenting the red/green proof: this test FAILS against pre-fix code (chunks 2…N dropped) and PASSES post-fix; to reproduce the failure, temporarily revert Plan 01's AddFollowers rewrite (e.g. `git stash`). Reuse the existing `mustMutate` helper from `dgraph_stale_test.go` — do NOT redeclare it. Use only stdlib imports (`encoding/json`, `os`, `path/filepath`, `sort`/`strconv`, `strings`, `testing`, `time`, `context`, `fmt`).</action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust && go vet -tags=integration ./pkg/dgraph/ && go test -tags=integration -run TestAddFollowersLargeKind3 ./pkg/dgraph/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/dgraph/dgraph_writepath_test.go` line 1 is `//go:build integration` (`head -1` matches), `package dgraph`.
    - `go vet -tags=integration ./pkg/dgraph/` compiles the file (no redeclaration of `mustMutate`).
    - `go test -tags=integration -run TestAddFollowersLargeKind3 ./pkg/dgraph/` either PASSES (fixture present + live Dgraph) or SKIPS with the harvest message (fixture absent) — it does not error/panic. Without a live Dgraph it may also skip/fail on connect; the compile + skip-on-missing-fixture path is the gate for this phase.
    - The assertion compares persisted edge count to `len(follows)` (`grep -c 'AddFollowers(' pkg/dgraph/dgraph_writepath_test.go` >= 1).
    - `t.Cleanup` + `DeleteNodes` teardown present (`grep -c 't.Cleanup' pkg/dgraph/dgraph_writepath_test.go` >= 1).
    - No `func mustMutate` redeclaration in this file.
  </acceptance_criteria>
  <done>An integration test compiles under `-tags=integration`, selects the largest harvested fixture (or skips cleanly), asserts the full follow set persists, and cleans up its own nodes — proving the data-drop regression is caught.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture file → test | `testdata/largest-kind3-*.json` is a real Nostr event committed to the repo, parsed by the integration test |
| test → Dgraph | test inserts + deletes nodes against the local `localhost:9080` graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03T-01 | Tampering (test pollution) | integration test mutates the live local Dgraph | mitigate | `t.Cleanup` resolves inserted UIDs and calls `DeleteNodes`; signer uses a time-seeded unique pubkey so test data never collides with real graph nodes. |
| T-03T-02 | Information disclosure | committing a real kind-3 event as a fixture | accept | kind-3 events are public Nostr data (pubkey + p-tag pubkeys only, ID-only — no event payload/content stored), consistent with the no-payloads-outside-StrFry rule; harmless to commit per D-11. |
| T-03T-03 | Denial of Service | CI running an integration test with no Dgraph | mitigate | `//go:build integration` keeps it out of `make test`/`-short`; absent-fixture path `t.Skip`s rather than hanging. |
| T-03T-SC | Tampering | npm/pip/cargo installs | accept | No package installs — tests use only stdlib + the existing module deps. No supply-chain surface added. |
</threat_model>

<verification>
- `go test -short ./pkg/dgraph/` runs the unit test (TestChunkSlice) and passes; the integration test is excluded by build tag.
- `go vet -tags=integration ./pkg/dgraph/` compiles the integration test (mustMutate reused, not redeclared).
- `go test -tags=integration -run TestAddFollowersLargeKind3 ./pkg/dgraph/` skips cleanly when no fixture is present (expected this phase) or passes against a live Dgraph + fixture.
- Both test files are white-box `package dgraph`, plain `if`/`t.Fatalf`, no testify.
</verification>

<success_criteria>
- TEST-04: `TestChunkSlice` asserts chunk count + membership at 0/200/201/500/501/10000, runs under `make test` / `-short`, no Dgraph.
- TEST-03: `TestAddFollowersLargeKind3` asserts full follow-set persistence under `//go:build integration` / `make test-integration`, selects the highest-count fixture, skips cleanly when absent, and cleans up after itself.
- Both tests mirror `dgraph_stale_test.go` conventions and reuse its `mustMutate` helper.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)

New symbols / files (exclude from drift verification — created here):
- `pkg/dgraph/dgraph_chunks_test.go` (new file)
- `TestChunkSlice` (new test func)
- `makeStrings(n int) []string` (new test helper, if used)
- `pkg/dgraph/dgraph_writepath_test.go` (new file)
- `TestAddFollowersLargeKind3` (new test func)
- `pkg/dgraph/testdata/largest-kind3-<count>.json` (fixture; absent during this phase — harvested via a later manual live crawl, test t.Skips until then)

Reused (NOT created here):
- `mustMutate` (already in `dgraph_stale_test.go` — reused, not redeclared)
- `chunkSlice`, `AddFollowers`, `DeleteNodes` (from Plan 01 / existing)
</artifacts>

<output>
Create `.planning/phases/03-write-path-correctness-regression-coverage/03-02-SUMMARY.md` when done
</output>
