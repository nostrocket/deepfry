---
phase: 05-pubkey-validation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/crawler/crawler.go
  - pkg/dgraph/dgraph.go
  - pkg/dgraph/validate_test.go
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03]
must_haves:
  truths:
    - "A p-tag with uppercase hex, a relay-URL blob, or a truncated value is rejected at updateFollowsFromEvent and nothing is written to Dgraph for that value"
    - "FetchAndUpdateFollows no longer calls nostr.GetPublicKey to validate batch pubkeys; it uses the hex regex validator"
    - "MarkAttempted recovers 64-char uppercase-hex nodes to lowercase and purges unrecoverable garbage nodes (short hex, relay blobs) so they age out of the stale frontier"
    - "ValidatePubkey and isValidHexPubkey have unit tests covering uppercase hex, short hex, relay-URL blob, and valid lowercase hex"
  artifacts:
    - path: "pkg/crawler/crawler.go"
      provides: "Hex-regex validation at both follow-list call sites"
      contains: "dgraph.ValidatePubkey"
    - path: "pkg/dgraph/dgraph.go"
      provides: "MarkAttempted recover-or-purge logic"
      contains: "ResolvePubkeysToUIDs"
    - path: "pkg/dgraph/validate_test.go"
      provides: "Unit tests for ValidatePubkey and isValidHexPubkey"
      contains: "func TestValidatePubkey"
  key_links:
    - from: "pkg/crawler/crawler.go"
      to: "pkg/dgraph/validate.go"
      via: "dgraph.ValidatePubkey call at both p-tag/batch validation sites"
      pattern: "dgraph\\.ValidatePubkey"
    - from: "pkg/dgraph/dgraph.go MarkAttempted"
      to: "pkg/dgraph/clusterscan.go ResolvePubkeysToUIDs + DeleteNodes"
      via: "UID resolution then in-place pubkey update or node delete"
      pattern: "ResolvePubkeysToUIDs|DeleteNodes"
---

<objective>
Fix the pubkey validator bug at its source and make existing garbage nodes self-clean.

Replace the misused `nostr.GetPublicKey` validator (a private→public key derivation function that silently accepts garbage) with the canonical `dgraph.ValidatePubkey` hex-regex validator at both crawler call sites (VALID-01). Add inline recover-or-purge logic to `MarkAttempted` so garbage nodes already in Dgraph are either case-corrected (recoverable uppercase hex) or deleted (unrecoverable short hex / relay blobs) the first time the crawler encounters them in the stale batch — this satisfies VALID-03 and subsumes the dropped explicit-purge step VALID-02. Add `make test`-runnable unit tests for the validators (D-06).

Purpose: Garbage pubkeys (19 known stubs) re-enter the stale frontier on every batch because the validator never rejected them and they were never aged out. This closes the source bug and ages out the existing garbage without a separate migration step.
Output: Hardened validation at the crawler layer, self-cleaning MarkAttempted, and validator unit tests.

Note on VALID-02: The explicit startup-migration / `healthcheck -purge` deliverable (VALID-02) is intentionally dropped per CONTEXT.md — VALID-03's inline recover/purge in MarkAttempted handles all existing garbage nodes the first time they surface. `healthcheck -purge` remains available as a manual tool but is not built here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-pubkey-validation-hardening/05-CONTEXT.md
@pkg/dgraph/validate.go
@pkg/dgraph/clusterscan.go
</context>

## Artifacts this phase produces

This plan creates or modifies the following symbols (exclude from drift verification):
- `pkg/dgraph/validate_test.go` (new file) — `TestValidatePubkey`, `TestIsValidHexPubkey` test functions
- A recover-or-purge code path inside the existing `func (c *Client) MarkAttempted(ctx context.Context, pubkeys []string, ts int64) error` in `pkg/dgraph/dgraph.go` (no signature change; new internal branching). The exact name/signature of any extracted helper is at the executor's discretion (CONTEXT.md "Claude's Discretion").
- Modified validation lines in `pkg/crawler/crawler.go` at `FetchAndUpdateFollows` (~line 266) and `updateFollowsFromEvent` (~line 507) — now calling `dgraph.ValidatePubkey`.

Reused, NOT created (already exist): `dgraph.ValidatePubkey`, `dgraph.isValidHexPubkey`, `Client.ResolvePubkeysToUIDs`, `Client.DeleteNodes`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap nostr.GetPublicKey for dgraph.ValidatePubkey at both crawler call sites (VALID-01)</name>
  <files>pkg/crawler/crawler.go</files>
  <read_first>
    - pkg/crawler/crawler.go (lines 258-296 FetchAndUpdateFollows, lines 497-517 updateFollowsFromEvent — see current nostr.GetPublicKey usage and the skip-and-log convention)
    - pkg/dgraph/validate.go (ValidatePubkey signature: returns error, nil on valid)
  </read_first>
  <behavior>
    - Given an uppercase 64-char hex string, the validator rejects it (it is skipped, not queried/written).
    - Given a short hex string ("f1", "cbdc"), the validator rejects it.
    - Given a relay-URL blob ("0115wss://relay.mostr.pub..."), the validator rejects it.
    - Given a valid 64-char lowercase hex pubkey, the validator accepts it (it is appended to authors / rawFollows).
  </behavior>
  <action>
    At `FetchAndUpdateFollows` (~line 266) replace the `if _, err := nostr.GetPublicKey(pubkey); err != nil` validation guard with `if err := dgraph.ValidatePubkey(pubkey); err != nil`. Preserve the existing skip-and-log behavior exactly (D-02): keep the `if c.debug { log.Printf("Skipping invalid pubkey: %s, error: %v", pubkey, err) }` then `continue`. A single bad pubkey must not abort the loop.
    At `updateFollowsFromEvent` (~line 507) replace `if _, err := nostr.GetPublicKey(pubkey); err != nil` with `if err := dgraph.ValidatePubkey(pubkey); err != nil`, preserving the existing `if c.debug { log.Printf("Invalid pubkey in follow list: %s, error: %v", pubkey, err) }` then `continue`.
    Do NOT remove the `github.com/nbd-wtf/go-nostr` import — it is used 14 times elsewhere in this file (e.g. nostr.Filter, nostr.Event). Confirm the `pkg/dgraph` import is present (it already is, since the crawler uses the dgraph client); do not add a duplicate.
    This implements D-01 per user decision (research/prior code used nostr.GetPublicKey, which is a key-derivation function, not a validator).
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust && go build ./... && grep -v '^[[:space:]]*//' pkg/crawler/crawler.go | grep -c 'nostr.GetPublicKey' | grep -qx 0 && grep -c 'dgraph.ValidatePubkey' pkg/crawler/crawler.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `pkg/crawler/crawler.go` contains no non-comment occurrence of `nostr.GetPublicKey`
    - `pkg/crawler/crawler.go` contains at least 2 occurrences of `dgraph.ValidatePubkey`
    - The `github.com/nbd-wtf/go-nostr` import line is still present in `pkg/crawler/crawler.go`
    - Both validation sites still `continue` (skip) on error rather than returning/aborting
  </acceptance_criteria>
  <done>Both crawler validation sites use the hex-regex validator; build passes; skip-and-log behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add recover-or-purge logic to MarkAttempted (VALID-03, subsumes VALID-02)</name>
  <files>pkg/dgraph/dgraph.go</files>
  <read_first>
    - pkg/dgraph/dgraph.go (lines 565-609 MarkAttempted — current skip+log gate at 577-587, UID resolution at 589, nquad stamp loop at 594-597)
    - pkg/dgraph/clusterscan.go (lines 43-88 ResolvePubkeysToUIDs — queries by exact value via eq(pubkey, [...]); returns map[pubkey]uid; missing pubkeys omitted)
    - pkg/dgraph/dgraph.go (lines 927-949 DeleteNodes — accepts []uid, deletes via DelNquads)
    - pkg/dgraph/validate.go (isValidHexPubkey — unexported bool fast-path, same package)
  </read_first>
  <action>
    Modify `MarkAttempted` so the existing skip-and-log gate (currently `if !isValidHexPubkey(pk) { log.Printf("WARN: skipping..."); continue }`) instead routes invalid pubkeys into inline recover-or-purge BEFORE the valid-pubkey UID-stamp path (D-03, D-04). Implement two branches for each invalid pubkey `pk`:
    1. RECOVERABLE — `isValidHexPubkey(strings.ToLower(pk))` is true (i.e. 64-char uppercase/mixed hex). Compute `lower := strings.ToLower(pk)`. Resolve the garbage node's UID by calling `c.ResolvePubkeysToUIDs(ctx, []string{pk})` (exact garbage string; D-05). If the UID is not found in the result map, silently skip (nothing in Dgraph to fix — Claude's Discretion item). Otherwise, also resolve whether the lowercase form already exists via `c.ResolvePubkeysToUIDs(ctx, []string{lower})`:
       - If `lower` already exists as a separate node: purge the uppercase node via `c.DeleteNodes(ctx, []string{garbageUID})`.
       - Else: update the pubkey field in place with a UID-based nquad mutation `<garbageUID> <pubkey> "<lower>" .` (same UID-nquad pattern as the existing last_attempt stamp at line 596, using strconv.Quote or %q for the value). Do NOT set last_attempt on the recovered node — the 9 uppercase garbage nodes have last_attempt=NONE and must re-enter the fresh frontier to be crawled with the corrected pubkey.
    2. UNRECOVERABLE — `isValidHexPubkey(strings.ToLower(pk))` is false (short hex like "f1"/"cbdc"/"de", or relay-URL blobs). Resolve UID via `c.ResolvePubkeysToUIDs(ctx, []string{pk})`; if found, delete via `c.DeleteNodes(ctx, []string{uid})`; if not found, silently skip.
    Log each action (recover-to-lowercase, purge-duplicate, purge-unrecoverable) at WARN/INFO so operators see cleanup happen. Valid pubkeys continue to flow into the existing `valid` slice → ResolvePubkeysToUIDs → last_attempt stamp path unchanged. Keep CommitNow semantics consistent with the existing mutation. The exact helper extraction (inline vs. private function) is at your discretion (CONTEXT.md). This satisfies VALID-03 and makes VALID-02's separate purge step redundant.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust && go build ./... && go vet ./pkg/dgraph/ && grep -q 'strings.ToLower' pkg/dgraph/dgraph.go && grep -q 'DeleteNodes' pkg/dgraph/dgraph.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 and `go vet ./pkg/dgraph/` exits 0
    - `MarkAttempted` body references `strings.ToLower` (recoverable-case detection)
    - `MarkAttempted` body references `ResolvePubkeysToUIDs` for garbage-node UID lookup
    - `MarkAttempted` body references `DeleteNodes` for the unrecoverable/duplicate purge path
    - Recovered-node mutation writes `<uid> <pubkey> "<lowercase>" .` and does NOT stamp last_attempt on that node
    - Valid pubkeys still receive the existing `<uid> <last_attempt> "ts" .` stamp (existing path unchanged)
  </acceptance_criteria>
  <done>MarkAttempted recovers uppercase-hex nodes to lowercase, purges duplicates and unrecoverable garbage, and still stamps valid pubkeys; build and vet pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for ValidatePubkey and isValidHexPubkey (D-06)</name>
  <files>pkg/dgraph/validate_test.go</files>
  <read_first>
    - pkg/dgraph/validate.go (ValidatePubkey returns error; isValidHexPubkey returns bool; both enforce ^[0-9a-f]{64}$; both in package dgraph)
    - pkg/dgraph/dgraph_chunks_test.go (existing same-package unit test style for table-driven tests; note these have NO build tag so they run under make test)
  </read_first>
  <behavior>
    - Valid lowercase 64-char hex (e.g. "e88a691e..." lowered): ValidatePubkey returns nil; isValidHexPubkey returns true.
    - Uppercase 64-char hex (e.g. "83E818DF..."): ValidatePubkey returns non-nil error; isValidHexPubkey returns false.
    - Short hex "f1": rejected. Short hex "cbdc": rejected. Short hex "de": rejected.
    - Relay-URL blob "0115wss://relay.mostr.pub..." (114-char): rejected.
    - Empty string: rejected.
  </behavior>
  <action>
    Create `pkg/dgraph/validate_test.go` in `package dgraph` with NO build tag (so it runs under `make test` / `go test -short`). Write table-driven tests `TestValidatePubkey` and `TestIsValidHexPubkey` covering every garbage type confirmed in the live DB (CONTEXT.md §"Real garbage nodes"): a 64-char uppercase hex, short hex "f1"/"cbdc"/"de", a relay-URL blob string, the empty string, and at least one valid 64-char lowercase hex pubkey. Assert ValidatePubkey returns nil only for the valid lowercase case and a non-nil error for all others; assert isValidHexPubkey returns the matching bool. Use a real lowercase 64-char hex literal for the valid case (e.g. derive by lowercasing a 64-char hex string). Follow the existing table-driven style in dgraph_chunks_test.go.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust && go test -short ./pkg/dgraph/ -run 'TestValidatePubkey|TestIsValidHexPubkey' -v</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/dgraph/validate_test.go` exists, is in `package dgraph`, and has no `//go:build` tag line
    - `pkg/dgraph/validate_test.go` contains `func TestValidatePubkey` and `func TestIsValidHexPubkey`
    - `go test -short ./pkg/dgraph/ -run 'TestValidatePubkey|TestIsValidHexPubkey'` exits 0
    - Test cases include: 64-char uppercase hex, "f1", "cbdc", relay-URL blob, empty string, and a valid lowercase 64-char hex
  </acceptance_criteria>
  <done>Validator unit tests pass under make test, covering all live-DB garbage types plus a valid pubkey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nostr relay → crawler | Untrusted p-tag values (arbitrary attacker-controlled strings) cross into the write path at updateFollowsFromEvent |
| Garbage string → Dgraph query | Garbage pubkey strings are interpolated into DQL via ResolvePubkeysToUIDs (eq(pubkey, [...])) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | updateFollowsFromEvent p-tag intake | mitigate | Validate every p-tag pubkey against `^[0-9a-f]{64}$` via dgraph.ValidatePubkey before any Dgraph write (Task 1); malformed values logged and skipped, never written |
| T-05-02 | Tampering / Injection | ResolvePubkeysToUIDs DQL interpolation of garbage strings | accept | ResolvePubkeysToUIDs already uses strconv.Quote on every value (clusterscan.go:54-56), neutralizing DQL injection; garbage strings (relay blobs, uppercase hex) are quoted before reaching the query. No new sink added by this plan. |
| T-05-03 | Denial of Service | Stale-frontier re-queue of garbage nodes every batch | mitigate | MarkAttempted recover/purge removes or corrects garbage nodes so they age out instead of consuming batch capacity indefinitely (Task 2) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies introduced; all symbols reused from existing pkg/dgraph and go-nostr already in go.mod |
</threat_model>

<verification>
- `go build ./...` passes (no broken imports after validator swap).
- `go test -short ./pkg/dgraph/` passes (new validator unit tests green; no Dgraph required).
- `go vet ./...` passes.
- No non-comment occurrence of `nostr.GetPublicKey` remains in `pkg/crawler/crawler.go`.
- Integration behavior (MarkAttempted recover/purge end-to-end, no-garbage write) is verified in Plan 02 against live Dgraph.
</verification>

<success_criteria>
- VALID-01: A p-tag containing uppercase hex, a relay-URL blob, or a truncated value is rejected at updateFollowsFromEvent via the `^[0-9a-f]{64}$` regex, logged, and not written to Dgraph.
- VALID-03: When MarkAttempted encounters a pubkey that fails the hex validator, it recovers (lowercase update) or purges (delete) the node via UID-based mutation so it stops re-entering the stale frontier.
- VALID-02 (subsumed): No separate purge step exists; the inline recover/purge in MarkAttempted handles existing garbage nodes the first time they surface (per CONTEXT.md decision to drop the explicit migration).
- Validator unit tests pass under `make test`.
</success_criteria>

<output>
Create `.planning/phases/05-pubkey-validation-hardening/05-01-SUMMARY.md` when done
</output>
