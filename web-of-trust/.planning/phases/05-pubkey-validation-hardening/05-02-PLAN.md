---
phase: 05-pubkey-validation-hardening
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - pkg/dgraph/dgraph_validation_test.go
autonomous: true
requirements: [VALID-01, VALID-03]
must_haves:
  truths:
    - "An integration test inserts an uppercase-hex node, a short-hex node, and a relay-blob node, calls MarkAttempted, and asserts the uppercase node's pubkey was lowercased (last_attempt still unset) while short-hex and relay-blob nodes were deleted"
    - "An integration test synthesizes a kind-3 event with garbage p-tags, processes it through the write path, and asserts zero invalid-pubkey nodes exist in Dgraph afterward"
  artifacts:
    - path: "pkg/dgraph/dgraph_validation_test.go"
      provides: "Integration tests for MarkAttempted recover/purge (D-07) and no-garbage write (D-08)"
      contains: "//go:build integration"
  key_links:
    - from: "pkg/dgraph/dgraph_validation_test.go"
      to: "pkg/dgraph MarkAttempted + ResolvePubkeysToUIDs"
      via: "white-box same-package test calling production methods against live Dgraph"
      pattern: "MarkAttempted|ResolvePubkeysToUIDs"
---

<objective>
Prove the Plan 01 validation behavior end-to-end against live Dgraph with integration tests gated behind `//go:build integration`.

D-07: Verify MarkAttempted's recover-or-purge — insert an uppercase-hex node, a short-hex node, and a relay-blob node; call MarkAttempted; assert the uppercase node was case-corrected (pubkey lowercased, last_attempt still unset) and the short-hex and relay-blob nodes were deleted.
D-08: Verify the crawler write path produces no garbage — exercise the validation gate with garbage pubkeys (uppercase hex, short hex, relay blob) and assert zero invalid-pubkey nodes exist in Dgraph after the write.

Purpose: VALID-01 and VALID-03 have no unit-testable path for the Dgraph mutations; these integration tests are the real gates that the recover/purge and write-path hardening work against the live schema.
Output: `pkg/dgraph/dgraph_validation_test.go` with two integration tests, runnable via `make test-integration`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pubkey-validation-hardening/05-CONTEXT.md
@.planning/codebase/TESTING.md
@pkg/dgraph/dgraph_stale_test.go
@pkg/dgraph/clusterscan.go
@pkg/dgraph/validate.go
</context>

## Artifacts this phase produces

This plan creates the following symbols (exclude from drift verification):
- `pkg/dgraph/dgraph_validation_test.go` (new file) — `TestMarkAttemptedRecoverOrPurge` (D-07) and `TestWritePathRejectsGarbage` (D-08) integration test functions. Whether these live in one file or two is at the executor's discretion (CONTEXT.md).

Reused, NOT created: `mustMutate`, `countFrontier` (test helpers in dgraph_stale_test.go, same package), `Client.MarkAttempted`, `Client.ResolvePubkeysToUIDs`, `Client.NewClient`, `Client.EnsureSchema`, `dgraph.ValidatePubkey`.

<tasks>

<task type="auto">
  <name>Task 1: Integration test for MarkAttempted recover-or-purge (D-07)</name>
  <files>pkg/dgraph/dgraph_validation_test.go</files>
  <read_first>
    - pkg/dgraph/dgraph_stale_test.go (build tag `//go:build integration`, NewClient("localhost:9080"), EnsureSchema, mustMutate helper, countFrontier helper, the `fmt.Sprintf("%064x", time.Now().UnixNano())` unique-fixture pattern)
    - pkg/dgraph/dgraph.go (MarkAttempted signature and the recover/purge behavior added in Plan 01: uppercase → lowercase in-place pubkey update with last_attempt left unset; short-hex/blob → DeleteNodes)
    - pkg/dgraph/clusterscan.go (ResolvePubkeysToUIDs — to look up nodes by exact value for assertions)
  </read_first>
  <action>
    Create `pkg/dgraph/dgraph_validation_test.go` with the first line `//go:build integration` (matching dgraph_stale_test.go), in `package dgraph`. Write `TestMarkAttemptedRecoverOrPurge`:
    1. Connect via `NewClient("localhost:9080")`, `defer c.Close()`, call `EnsureSchema(ctx)`.
    2. Build unique fixtures so the test is rerun-safe on the live graph: derive a unique valid lowercase 64-char hex via `fmt.Sprintf("%064x", time.Now().UnixNano())`, then create its UPPERCASE form via `strings.ToUpper(lower)` for the recoverable node. Build a unique short-hex garbage string and a unique relay-blob garbage string (prefix-tag them with the test name so they cannot collide with real data, e.g. `"zz" + shortFixture` — but keep short-hex genuinely non-64-hex). Insert all three nodes via `mustMutate` with `<dgraph.type> "Profile"` and pubkey set to the garbage value; leave last_attempt unset on the uppercase node.
    3. Call `c.MarkAttempted(ctx, []string{upperPubkey, shortHexGarbage, relayBlobGarbage}, time.Now().Unix())`.
    4. Assert via `ResolvePubkeysToUIDs`: the lowercase form of the uppercase node now EXISTS (recovered), the original uppercase string no longer resolves to a node OR resolves to the same recovered node with lowercase pubkey, and that recovered node has last_attempt unset (query its last_attempt; expect absent/zero). Assert the short-hex and relay-blob garbage strings no longer resolve to any node (deleted).
    5. Clean up the recovered lowercase node at the end (DeleteNodes on its UID) so the test does not pollute the live frontier.
    Use a `requireDgraph`-style fatal skip only if the existing test files do — otherwise mirror dgraph_stale_test.go exactly (it calls t.Fatal on connect failure). If dgraph_validation_test.go and a D-08 test share this file, define both `func TestMarkAttemptedRecoverOrPurge` and the D-08 test here; otherwise keep D-08 in a separate file (Task 2). Splitting is at your discretion (CONTEXT.md).
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust && go vet -tags=integration ./pkg/dgraph/ && go test -tags=integration ./pkg/dgraph/ -run TestMarkAttemptedRecoverOrPurge -v</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/dgraph/dgraph_validation_test.go` first line is `//go:build integration`
    - File is in `package dgraph` and contains `func TestMarkAttemptedRecoverOrPurge`
    - Test inserts an uppercase-hex node, a short-hex node, and a relay-blob node, then calls `MarkAttempted`
    - Test asserts the lowercase form of the uppercase node exists, that node's last_attempt is unset, and the short-hex + relay-blob nodes no longer resolve
    - `go test -tags=integration ./pkg/dgraph/ -run TestMarkAttemptedRecoverOrPurge` exits 0 against a live Dgraph
    - Test cleans up the recovered node it created
  </acceptance_criteria>
  <done>Integration test proves MarkAttempted recovers uppercase nodes to lowercase (last_attempt unset) and purges unrecoverable garbage, passing against live Dgraph.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test for end-to-end no-garbage write (D-08)</name>
  <files>pkg/dgraph/dgraph_validation_test.go</files>
  <read_first>
    - pkg/dgraph/dgraph_stale_test.go (test scaffolding, build tag, unique-fixture pattern, mustMutate)
    - pkg/dgraph/validate.go (ValidatePubkey / isValidHexPubkey — the gate the write path now enforces)
    - pkg/dgraph/dgraph.go (the pubkey-add write path: AddFollowers / the validation gate used on writes)
  </read_first>
  <action>
    Add `TestWritePathRejectsGarbage` (in the same file as Task 1 or a sibling integration file — executor's discretion). Goal: prove that processing a follow-list containing garbage p-tags writes ZERO invalid-pubkey nodes (VALID-01 at the Dgraph layer). Because `updateFollowsFromEvent` lives in package `crawler` (not directly callable white-box from package dgraph), exercise the equivalent dgraph-layer write path that the crawler funnels into: take a signer pubkey plus a follow-list slice that mixes valid lowercase hex with garbage (a 64-char uppercase hex, a short hex like "f1", and a relay-URL blob), and pass it through the same validated write entry point the crawler uses (e.g. the AddFollowers / pubkey-add path that already calls the ValidatePubkey gate at dgraph.go:128/:265). Then query Dgraph for any node whose pubkey does not match `^[0-9a-f]{64}$` among the just-written set (resolve each garbage string via ResolvePubkeysToUIDs and assert none resolve), and assert the valid lowercase follow was written. Use unique fixtures (`%064x` of time.Now().UnixNano()) and clean up created nodes at the end. If the crawler-layer gate is the only place garbage is filtered (and the dgraph-layer write does not itself reject), structure the test to call through the validation gate first (mirror the crawler's pre-write filter using `dgraph.ValidatePubkey`) so the test asserts the gate + write contract VALID-01 specifies. Document in a comment which layer the assertion covers.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust && go test -tags=integration ./pkg/dgraph/ -run TestWritePathRejectsGarbage -v</automated>
  </verify>
  <acceptance_criteria>
    - The integration file contains `func TestWritePathRejectsGarbage` under the `//go:build integration` tag
    - Test feeds a follow-list mixing valid lowercase hex with uppercase hex, short hex, and a relay-URL blob through the validated write path
    - Test asserts none of the garbage strings resolve to a node afterward (zero invalid-pubkey nodes written) and the valid follow was written
    - `go test -tags=integration ./pkg/dgraph/ -run TestWritePathRejectsGarbage` exits 0 against live Dgraph
    - Test cleans up created fixtures
  </acceptance_criteria>
  <done>Integration test proves the validated write path writes no invalid-pubkey nodes when given garbage p-tags, passing against live Dgraph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → live Dgraph | Test inserts and deletes nodes in the live graph; must not pollute production frontier |
| Garbage string → DQL | Garbage pubkey fixtures are interpolated into queries via ResolvePubkeysToUIDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Test pollution of live Dgraph frontier | mitigate | Tests use time-derived unique fixtures and DeleteNodes cleanup at the end so no orphan nodes persist in the live stale frontier |
| T-05-05 | Injection | Garbage-string fixtures in ResolvePubkeysToUIDs queries | accept | ResolvePubkeysToUIDs quotes every value with strconv.Quote (clusterscan.go); no raw interpolation sink added by tests |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; tests reuse existing dgo/v210 api package and standard library only |
</threat_model>

<verification>
- `go vet -tags=integration ./pkg/dgraph/` passes (test compiles under the integration tag).
- `make test-integration` (or `go test -tags=integration ./pkg/dgraph/`) passes against a live Dgraph at localhost:9080 — this is the manual verification step per spec §6.
- No leftover test fixture nodes remain in Dgraph after the run (cleanup asserted).
</verification>

<success_criteria>
- D-07: MarkAttempted recover/purge proven against live Dgraph — uppercase node recovered to lowercase (last_attempt unset), short-hex and relay-blob nodes deleted.
- D-08: End-to-end no-garbage write proven — zero invalid-pubkey nodes after processing a garbage-laden follow-list (VALID-01 at the Dgraph layer).
- Both tests gated behind `//go:build integration` and run via `make test-integration`; `make test` (short) is unaffected.
</success_criteria>

<output>
Create `.planning/phases/05-pubkey-validation-hardening/05-02-SUMMARY.md` when done
</output>
