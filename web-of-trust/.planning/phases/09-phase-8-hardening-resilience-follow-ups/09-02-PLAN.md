---
phase: 09-phase-8-hardening-resilience-follow-ups
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - pkg/crawler/crawler.go
  - cmd/crawler/main.go
autonomous: false
requirements: [HARD-03, RESIL-01, HARD-01]
must_haves:
  truths:
    - "forwardEvent publishes within a short bounded context derived from c.timeout, so a hung forward relay cannot stall the single-threaded drain loop or delay MarkAttempted / the next batch (HARD-03/WR-04)."
    - "forwardEvent's existing backoff bookkeeping on publish failure is unchanged — only the publish call is wrapped in the bounded context."
    - "The BackfillNextAttempt caller in crawler.go passes int64(cfg.MissBackoff.HitRefreshCadence.Seconds()) to match the new signature (HARD-01 caller update + IN-03)."
    - "A transient Dgraph gRPC error (codes.Unavailable, DeadlineExceeded, ResourceExhausted; the observed 'EOF' surfaces as Unavailable) in the main crawl loop logs a WARN and retries with bounded backoff rather than terminating the process (RESIL-01)."
    - "Genuinely fatal/unrecoverable Dgraph errors (non-transient gRPC codes, non-gRPC errors like a misconfigured endpoint) still exit the loop loudly (RESIL-01)."
    - "Retry respects ctx cancellation — a shutdown signal during a retry backoff breaks the main loop instead of blocking (RESIL-01)."
  artifacts:
    - path: "pkg/crawler/crawler.go"
      provides: "Bounded-context forwardEvent + updated BackfillNextAttempt caller"
      contains: "context.WithTimeout(ctx, c.timeout)"
    - path: "cmd/crawler/main.go"
      provides: "Transient-vs-fatal Dgraph error classification + retry-with-backoff at the per-batch call sites"
      contains: "status.FromError"
  key_links:
    - from: "forwardEvent"
      to: "forwardRelay.conn.Publish"
      via: "context.WithTimeout(ctx, c.timeout) child context"
      pattern: "WithTimeout\\(ctx, c\\.timeout\\)"
    - from: "main loop GetStalePubkeys/CountPubkeys/CountStalePubkeys"
      to: "grpc/status code inspection"
      via: "isDgraphTransient classification + backoff retry loop"
      pattern: "status\\.FromError"
    - from: "cmd/crawler/main.go BackfillNextAttempt caller"
      to: "pkg/dgraph BackfillNextAttempt(ctx, hitRefreshCadence)"
      via: "matches new signature from plan 09-01"
      pattern: "BackfillNextAttempt\\(ctx"
---

<objective>
Bound the forward-publish so it cannot stall the drain loop, wire the new BackfillNextAttempt signature, and make the main crawl loop survive transient Dgraph blips instead of exiting.

- HARD-03 (WR-04): wrap `forwardEvent`'s publish in `context.WithTimeout(ctx, c.timeout)` so a hung forward relay times out instead of blocking the single-threaded drain.
- HARD-01 caller (IN-03): update the `BackfillNextAttempt` call in crawler.go to pass `cfg.MissBackoff.HitRefreshCadence` seconds, matching the new signature from plan 09-01.
- RESIL-01: classify main-loop Dgraph errors via `google.golang.org/grpc/status`; transient codes (Unavailable/DeadlineExceeded/ResourceExhausted) → WARN + bounded backoff retry; fatal → exit loudly.

Purpose: RESIL-01 was observed live — the crawler exited on `code = Unavailable desc = error reading from server: EOF`. HARD-03/WR-04 is a latent drain-loop stall.
Output: A bounded-context forwardEvent, a correct BackfillNextAttempt call, and a resilient main loop with a transient-error classifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-phase-8-hardening-resilience-follow-ups/09-CONTEXT.md
@.planning/phases/09-phase-8-hardening-resilience-follow-ups/09-PATTERNS.md
@.planning/todos/pending/crawler-retry-transient-stale-count-errors.md

# Depends on plan 09-01 (new BackfillNextAttempt signature)
@.planning/phases/09-phase-8-hardening-resilience-follow-ups/09-01-PLAN.md

# Code to modify (read the cited line ranges)
@pkg/crawler/crawler.go
@cmd/crawler/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound forwardEvent publish + update BackfillNextAttempt caller (HARD-03, HARD-01)</name>
  <files>pkg/crawler/crawler.go</files>
  <read_first>
    - pkg/crawler/crawler.go:235-255 — current forwardEvent: Publish(ctx, ...) with no per-publish timeout; backoff bookkeeping on failure.
    - pkg/crawler/crawler.go:456-459 — FetchAndUpdateFollows bounded-context pattern (context.WithTimeout(relayContext, c.timeout); defer cancel).
    - pkg/crawler/crawler.go:99,192 — c.timeout field (set from cfg.Timeout); :587 — forwardEvent is called with the long-lived relayContext.
    - pkg/crawler/crawler.go:137-144 — current BackfillNextAttempt(ctx) call (one-time startup backfill); needs the new signature from plan 09-01.
    - 09-PATTERNS.md HARD-01 (caller update) and HARD-03 (forwardEvent fix pattern) sections.
  </read_first>
  <behavior>
    - forwardEvent wraps the Publish call in context.WithTimeout(ctx, c.timeout) with defer cancel; a publish that exceeds c.timeout returns a deadline error and runs the existing failure bookkeeping rather than blocking indefinitely.
    - On publish failure the existing backoff/teardown (close conn, alive=false, retryAt, backoff*=2 capped at maxBackoff) is unchanged.
    - The early-return guard (forwardRelay nil or not alive) is unchanged.
  </behavior>
  <action>
    forwardEvent (crawler.go:235-255): after the nil/alive guard, create `pubCtx, cancel := context.WithTimeout(ctx, c.timeout)` with `defer cancel()`, then call `c.forwardRelay.conn.Publish(pubCtx, *event)`. Leave every other line (failure bookkeeping, debug log) byte-for-byte as is. This guards only the publish; the caller still passes the long-lived relayContext at :587, so the bounded child protects the drain loop without shortening the outer context.

    BackfillNextAttempt caller (crawler.go:137-144): compute `cadenceSec := int64(cfg.MissBackoff.HitRefreshCadence.Seconds())` and pass it as the second argument: `dgClient.BackfillNextAttempt(ctx, cadenceSec)`. (HARD-01 caller half + IN-03.) Verify `cfg` is the loaded config value in scope at that point; if the constructor uses a differently-named local for the config, use that name.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust &amp;&amp; go build ./... &amp;&amp; go vet ./pkg/crawler/ &amp;&amp; go test -short ./pkg/crawler/</automated>
  </verify>
  <done>forwardEvent publishes via a context.WithTimeout(ctx, c.timeout) child with defer cancel; failure bookkeeping unchanged. BackfillNextAttempt is called with the cadence-seconds argument. Package compiles, vets, and unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transient-vs-fatal Dgraph error classification + retry in main loop (RESIL-01)</name>
  <files>cmd/crawler/main.go</files>
  <read_first>
    - cmd/crawler/main.go:1-18 — imports (grpc/status + grpc/codes not yet imported).
    - cmd/crawler/main.go:100-144 — mainLoop break-on-error sites: GetStalePubkeys (113-117), CountPubkeys (119-123), CountStalePubkeys (140-144).
    - cmd/crawler/main.go:160-185 — MarkAttempted call (176-178) currently only log.Printf on error.
    - pkg/crawler/crawler.go:47-50 (initialBackoff/maxBackoff constants), :344-353 (backoff doubling+cap pattern).
    - 09-PATTERNS.md RESIL-01 section — isDgraphTransient classifier, retry-wrapper pattern, fatal-vs-transient code table.
  </read_first>
  <behavior>
    - isDgraphTransient(err) returns true for gRPC status codes Unavailable, DeadlineExceeded, ResourceExhausted (the observed "error reading from server: EOF" surfaces as Unavailable); false for nil, non-gRPC errors, and all other codes (InvalidArgument, NotFound, PermissionDenied, Internal, Unimplemented).
    - A transient error at a wrapped call site logs a WARN with attempt count and retries after a doubling backoff (start dgraphRetryInitial, cap dgraphRetryMax, max dgraphRetryAttempts).
    - A fatal error logs and breaks the main loop (preserving loud exit for misconfigured endpoints).
    - ctx cancellation during a retry backoff breaks the main loop promptly (select on time.After vs ctx.Done()).
    - After exhausting retries on persistent transient errors, the loop exits loudly.
  </behavior>
  <action>
    Add imports `google.golang.org/grpc/codes` and `google.golang.org/grpc/status` to cmd/crawler/main.go.

    Add a package-level `isDgraphTransient(err error) bool` helper using `status.FromError(err)` and a switch on `st.Code()` returning true for codes.Unavailable, codes.DeadlineExceeded, codes.ResourceExhausted (per 09-PATTERNS.md RESIL-01). Add retry constants consistent with the relay backoff constants: `dgraphRetryInitial = 5 * time.Second`, `dgraphRetryMax = 2 * time.Minute`, `dgraphRetryAttempts = 5` (Claude's discretion per CONTEXT.md — keep consistent with crawler.go:47-50).

    Wrap the three per-batch call sites — GetStalePubkeys (113-117), CountPubkeys (119-123), CountStalePubkeys (140-144) — each in a retry loop following the 09-PATTERNS.md RESIL-01 pattern: attempt the call; on success break; if `!isDgraphTransient(err)` log "Fatal Dgraph error ..." and `break mainLoop`; else log a WARN with attempt n/max and retry after a `select` on `time.After(retryDelay)` vs `ctx.Done()` (ctx.Done → `break mainLoop`), doubling retryDelay capped at dgraphRetryMax. After the loop, if err is still non-nil log "Dgraph unavailable after N attempts, exiting" and `break mainLoop`. Keep the existing "Database is empty" seed handling and the empty-pubkeys "work complete" break unchanged.

    Also wrap the MarkAttempted call (176-178) in the same retry loop so a transient failure does not silently skip PERF-02 stamping for the batch (per CONTEXT.md "consider MarkAttempted"); on persistent failure keep the existing WARN-and-continue behavior (do NOT exit — MarkAttempted is best-effort aging, not loop-critical) by logging a WARN after exhausting attempts and proceeding to the next batch rather than breaking. Refactor the repeated retry logic into a small local helper if it reduces duplication, but inline is acceptable.

    Avoid `goto`; use labeled `break mainLoop` / `continue mainLoop` for control flow. Do not change the staleRemaining clamp (180-184) or the final report.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/web-of-trust &amp;&amp; go build ./... &amp;&amp; go vet ./cmd/crawler/ &amp;&amp; grep -niE "isDgraphTransient|status.FromError|codes.Unavailable" cmd/crawler/main.go | grep -c .</automated>
  </verify>
  <done>main.go imports grpc/status+codes, defines isDgraphTransient, and wraps GetStalePubkeys/CountPubkeys/CountStalePubkeys (exit on persistent transient/fatal) and MarkAttempted (WARN-and-continue) in backoff-retry loops that honor ctx cancellation. Package compiles and vets clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-host re-verification of resilience + drain hardening</name>
  <what-built>
    - HARD-03: forwardEvent now publishes within a c.timeout-bounded context (drain loop cannot be stalled by a hung forward relay).
    - HARD-01 caller + RESIL-01: BackfillNextAttempt called with cadence param; main loop classifies transient Dgraph errors and retries with backoff instead of exiting.
    These touch the live drain/main loop, which CLAUDE.md §6 requires verifying against live Dgraph + relays on the strfry host.
  </what-built>
  <how-to-verify>
    On the strfry host (live Dgraph localhost:9080 + relays):
    1. Build: `cd web-of-trust &amp;&amp; make build-crawler`. Confirm it compiles.
    2. Run the crawler against live Dgraph for at least one batch; confirm it processes a batch and logs `Batch complete: ...` (HARD-03 did not break the drain — events still forward, MarkAttempted still runs).
    3. RESIL-01 transient retry: while the crawler is mid-run, bounce the Dgraph container (`docker-compose -f docker-compose.dgraph.yml restart dgraph-alpha` or equivalent). Expected: the crawler logs a WARN like "Transient Dgraph error ... (attempt 1/5) ... retrying in 5s" and RESUMES once Dgraph is back — WITHOUT the process exiting.
    4. Fatal path sanity: point the crawler at a bad Dgraph address (or stop Dgraph entirely past the retry budget) and confirm it eventually exits loudly with "Dgraph unavailable after 5 attempts" rather than hanging.
    5. Confirm no "BackfillNextAttempt failed" WARN on startup against the already-migrated production graph (it should log "seeded 0" or a real count).
  </how-to-verify>
  <resume-signal>Type "approved" if the crawler survives a Dgraph bounce, exits loudly on a persistent/fatal failure, and drains batches normally; otherwise describe the observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- `go build ./...`, `go vet ./pkg/crawler/ ./cmd/crawler/` pass.
- Unit tests pass: `cd web-of-trust && go test -short ./pkg/crawler/ ./cmd/crawler/`.
- forwardEvent wraps Publish in context.WithTimeout(ctx, c.timeout); failure bookkeeping unchanged.
- main.go has isDgraphTransient using status.FromError; transient → retry, fatal → loud exit; retries honor ctx.Done().
- BackfillNextAttempt caller passes the cadence-seconds argument (matches plan 09-01 signature).
- Live-host checkpoint (Task 3) confirms drain resilience and transient-retry behavior per CLAUDE.md §6.
</verification>

<success_criteria>
- HARD-03: forwardEvent publishes within a c.timeout-bounded context so a hung forward relay cannot stall the drain loop or delay MarkAttempted/next batch (ROADMAP Phase 9 criterion 3).
- RESIL-01: a transient Dgraph gRPC error in the main loop logs a WARN and retries with backoff; genuinely fatal errors still exit loudly (ROADMAP criterion 5).
- HARD-01 caller: BackfillNextAttempt is invoked with the cadence parameter, completing the IN-03 signature change started in plan 09-01.
</success_criteria>

<output>
Create `.planning/phases/09-phase-8-hardening-resilience-follow-ups/09-02-SUMMARY.md` when done. Record the chosen retry parameters and the outcome of the live-host checkpoint (Task 3), or note it as deferred to a live-host run with the exact verification steps still pending.
</output>
