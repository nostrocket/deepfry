---
phase: 13
plan: 13-01
type: implementation
wave: 1
depends_on: []
files_modified:
  - pkg/config/config.go
  - pkg/config/config_test.go
  - pkg/crawler/crawler.go
  - cmd/crawler/main.go
  - cmd/crawler/main_test.go
  - cmd/crawler/metrics.go
  - cmd/crawler/metrics_test.go
  - pkg/crawler/crawler_filter_test.go
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-03
  - LOOP-04
  - COUNT-01
  - COUNT-02
  - COUNT-03
  - MEASURE-01
  - MEASURE-02
  - MEASURE-03
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "LOOP-01: Operator can set frontier_batch_size separately from relay_filter_batch_size in ~/deepfry/web-of-trust.yaml."
    - "LOOP-02/LOOP-04: The crawler may select more pubkeys from Dgraph than a relay can accept in one REQ, while relay requests remain chunked by learned relay filter caps."
    - "LOOP-03: Batch logs and BATCH_METRICS report selected, queried, hits, skipped_attempts, and marked_attempted using exact post-decoupling batch accounting, with queried sourced from the validated author count actually submitted to relay filters."
    - "COUNT-01/COUNT-02/COUNT-03: CountPubkeys and CountStalePubkeys run only on configured sample intervals, metrics explicitly mark cached count fields between samples, and sampled count calls still use retryDgraph."
    - "MEASURE-01/MEASURE-02/MEASURE-03: BATCH_METRICS and crawler-metrics.jsonl records contain enough config and count-sampling context for baseline-vs-optimized operator comparison."
    - "TEST-01/TEST-02: Unit tests cover config defaults/guards, loop accounting with skipped attempts, count throttling, and relay chunk safety without touching ~/deepfry/."
  prohibitions:
    - "No changes to StrFry or StrFry plugin protocol."
    - "No event payload storage outside StrFry; Dgraph remains an ID-only pubkey graph."
    - "No blind relay filter-size increase; frontier_batch_size must never become the per-relay chunk cap."
    - "No tests may read, write, remove, or depend on the real ~/deepfry/ directory."
  artifacts:
    - path: "pkg/config/config.go"
      provides: "Config.FrontierBatchSize and Config.CountSampleInterval with mapstructure keys frontier_batch_size and count_sample_interval."
    - path: "cmd/crawler/main.go"
      provides: "Main-loop frontier selection, count sampling, actual batch accounting, and retryDgraph-preserved sampled count calls."
    - path: "pkg/crawler/crawler.go"
      provides: "FetchResult.Queried exact count of valid authors submitted to relay querying after invalid pubkey filtering."
    - path: "cmd/crawler/metrics.go"
      provides: "BATCH_METRICS and runRecord fields for frontier/relay batch sizes, count interval, sampled/cached count state, selected, queried, skipped, and marked-attempted counts."
    - path: "pkg/crawler/crawler_filter_test.go"
      provides: "Regression coverage that a 250+ author frontier is still chunked as 100/100/50 at the relay boundary."
  key_links:
    - from: "cmd/crawler/main.go"
      to: "pkg/dgraph/dgraph.go:GetStalePubkeys"
      via: "GetStalePubkeys limit must use cfg.FrontierBatchSize."
      pattern: "GetStalePubkeys\\([^\\n]+cfg\\.FrontierBatchSize"
    - from: "cmd/crawler/main.go"
      to: "pkg/crawler/crawler.go:queryRelay"
      via: "crawler.Config.FilterBatchSize remains cfg.RelayFilterBatchSize, so relay request chunking is independent of frontier selection size."
      pattern: "FilterBatchSize:\\s*cfg\\.RelayFilterBatchSize"
    - from: "cmd/crawler/main.go"
      to: "cmd/crawler/metrics.go"
      via: "Loop passes actual selected/query/hit/skip/marked/count-sampled values into logBatchMetrics and buildRunRecord."
      pattern: "logBatchMetrics\\("
---

<objective>
Implement Phase 13 Main-Loop Throughput Controls for the web-of-trust crawler.

Purpose: reduce avoidable per-loop Dgraph/bookkeeping overhead and safely increase useful crawl work per loop without repeating the Phase 6 relay oversized-filter failure mode.

Output: one implementation pass that adds independent Dgraph frontier sizing, count-query sampling, updated batch/run metrics, relay chunking regression coverage, and an operator baseline/optimized verification procedure inside this plan.
</objective>

## Artifacts this phase produces

- `pkg/config.Config.FrontierBatchSize int` mapped from YAML key `frontier_batch_size` for Dgraph frontier selection. Default: `100`, matching current `relay_filter_batch_size` behavior. Guard: non-positive values reset to `RelayFilterBatchSize` when positive, otherwise `100`. Covers LOOP-01 and TEST-01.
- `pkg/config.Config.CountSampleInterval int` mapped from YAML key `count_sample_interval`. Default: `1`, preserving current count-every-loop behavior. Guard: non-positive values reset to `1`. Covers COUNT-01 and TEST-01.
- Main-loop use of `cfg.FrontierBatchSize` as the `GetStalePubkeys` limit while `crawler.Config.FilterBatchSize` remains `cfg.RelayFilterBatchSize`. Covers LOOP-01, LOOP-02, and LOOP-04.
- A count-sampling helper in `cmd/crawler/main.go`, named `countSampleState` with methods or functions for sample-due decisions, cached `totalPubkeys`, cached `totalStale`, `countsSampled`, `countsCached`, and `countSampleAgeBatches` reporting. Covers COUNT-01, COUNT-02, COUNT-03, and TEST-02.
- Batch accounting symbols in `cmd/crawler/main.go`: `selectedCount`, `queriedCount`, `hitCount`, `skippedAttempts`, `markedAttempted`, `countsSampled`, `countsCached`, `countSampleAgeBatches`, and cached count values used for logs and metrics. Covers LOOP-03 and COUNT-02.
- `cmd/crawler/metrics.go` `runStats` counters extended with selected, queried, hits, skipped attempts, marked-attempted, count-sample, and cached-count batch totals. Covers LOOP-03, COUNT-02, and MEASURE-01.
- `pkg/crawler/crawler.go` `FetchResult.Queried int` populated from the validated `authors` slice after invalid pubkey filtering and before relay filter construction. Covers LOOP-03 and TEST-02.
- `cmd/crawler/metrics.go` `logBatchMetrics` JSON fields: `frontier_batch_size`, `relay_filter_batch_size`, `count_sample_interval`, `counts_sampled`, `counts_cached`, `count_sample_age_batches`, `selected`, `queried`, `hits`, `skipped_attempts`, `marked_attempted`, `stale_remaining`, `total_pubkeys`, and `new_pubkeys`. On unsampled batches, `total_pubkeys` and `stale_remaining` are last sampled cached values, `counts_sampled=false`, `counts_cached=true`, `count_sample_age_batches>0`, and `new_pubkeys` is JSON `null` because it is not freshly derivable from `CountPubkeys`. Covers LOOP-03, COUNT-02, and MEASURE-01.
- `cmd/crawler/metrics.go` `runRecord` JSON fields: `frontier_batch_size`, `relay_filter_batch_size`, `count_sample_interval`, `total_selected`, `total_queried`, `total_skipped_attempts`, `total_marked_attempted`, `count_samples`, and `count_cached_batches`. Keep existing `batch_size` only if needed for backward compatibility, but it must no longer be the only batch-size field. Covers LOOP-03, COUNT-02, and MEASURE-02.
- Unit tests in `pkg/config/config_test.go`, `cmd/crawler/main_test.go`, `cmd/crawler/metrics_test.go`, and `pkg/crawler/crawler_filter_test.go`. Covers TEST-01 and TEST-02.
- Operator verification procedure in this PLAN.md under `<verification>`, using `WOT_ROUND` labels and `~/deepfry/crawler-metrics.jsonl`. Covers MEASURE-03.

<execution_context>
@/Users/g/.codex/gsd-core/workflows/execute-plan.md
@/Users/g/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@cmd/crawler/main.go
@cmd/crawler/metrics.go
@cmd/crawler/main_test.go
@cmd/crawler/metrics_test.go
@pkg/config/config.go
@pkg/config/config_test.go
@pkg/dgraph/dgraph.go
@pkg/dgraph/dgraph_stale_test.go
@pkg/crawler/crawler.go
@pkg/crawler/crawler_filter_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add independent frontier and count-sampling config</name>
  <files>pkg/config/config.go, pkg/config/config_test.go</files>
  <read_first>
    - pkg/config/config.go: Config struct, LoadConfig defaults, non-positive guard pattern for RelayEjectionThresholds and MissBackoff.
    - pkg/config/config_test.go: viper.Reset, t.Setenv("HOME", t.TempDir()), and temp ~/deepfry/web-of-trust.yaml patterns.
  </read_first>
  <action>
    Add `FrontierBatchSize int` with `mapstructure:"frontier_batch_size"` and `CountSampleInterval int` with `mapstructure:"count_sample_interval"` to `pkg/config.Config` per LOOP-01 and COUNT-01. Set viper defaults `frontier_batch_size=100` and `count_sample_interval=1` so current behavior is preserved. After unmarshal, guard non-positive values: `FrontierBatchSize` resets to `RelayFilterBatchSize` when that is positive, otherwise `100`; `CountSampleInterval` resets to `1`. Add unit tests that reset viper, set `HOME` to `t.TempDir()`, and verify defaults, explicit YAML values, and non-positive guards without touching the real home directory per TEST-01. Do not introduce external packages.
  </action>
  <acceptance_criteria>
    - Default LoadConfig returns `RelayFilterBatchSize=100`, `FrontierBatchSize=100`, and `CountSampleInterval=1`.
    - YAML containing `relay_filter_batch_size: 100`, `frontier_batch_size: 500`, and `count_sample_interval: 5` loads those exact values.
    - YAML containing non-positive `frontier_batch_size` and `count_sample_interval` is corrected to safe values.
    - Tests only use temp HOME directories and do not access real `~/deepfry/`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/config -run 'TestLoadConfig_.*(FrontierBatchSize|CountSampleInterval|ThroughputControls)' -count=1</automated>
    <automated>go test ./pkg/config -count=1</automated>
  </verify>
  <done>Config exposes and safely defaults `frontier_batch_size` and `count_sample_interval`; tests prove defaults, explicit YAML values, non-positive guards, and temp-HOME isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor main-loop accounting, count sampling, and metrics</name>
  <files>pkg/crawler/crawler.go, pkg/crawler/crawler_filter_test.go, cmd/crawler/main.go, cmd/crawler/main_test.go, cmd/crawler/metrics.go, cmd/crawler/metrics_test.go</files>
  <read_first>
    - pkg/crawler/crawler.go: FetchResult, FetchAndUpdateFollows invalid pubkey filtering into `authors`, and relay filter construction.
    - pkg/crawler/crawler_filter_test.go: existing filter construction/chunking tests and validation helpers.
    - cmd/crawler/main.go: retryDgraph, main loop GetStalePubkeys, CountPubkeys, CountStalePubkeys, FetchAndUpdateFollows, attemptableBatchKeys, batch logs, BATCH_METRICS call, buildRunRecord call.
    - cmd/crawler/metrics.go: runStats, recordBatch, logBatchMetrics, runRecord, buildRunRecord.
    - cmd/crawler/main_test.go: fakeSleep, retryDgraph, attemptableBatchKeys tests.
    - cmd/crawler/metrics_test.go: buildRunRecord and writeRunRecord tests with temp HOME.
  </read_first>
  <action>
    Update the main loop per LOOP-01, LOOP-03, COUNT-01, COUNT-02, COUNT-03, MEASURE-01, MEASURE-02, and TEST-02. Change `GetStalePubkeys` to use `cfg.FrontierBatchSize`. Keep `crawler.Config.FilterBatchSize` wired to `cfg.RelayFilterBatchSize`.

    Add exact queried-count accounting rather than inferring it from the selected map. Extend `pkg/crawler.FetchResult` with `Queried int`, set it in `FetchAndUpdateFollows` to `len(authors)` after `dgraph.ValidatePubkey` filtering and before relay query/filter construction, and wire `result.Queried` into the main-loop batch log, `runStats.recordBatch`, `logBatchMetrics`, and `buildRunRecord`. Add a unit test in `pkg/crawler/crawler_filter_test.go` or another existing crawler test file that passes a selected map containing at least one invalid pubkey plus valid 64-hex authors, runs `FetchAndUpdateFollows` through the existing injectable query seam or filter helper, and proves `selectedCount > result.Queried` while the relay filter authors contain only valid keys. The test must also prove invalid selected keys are not counted as queried. Do not use `selectedCount` as a fallback for `queriedCount`.

    Introduce `countSampleState` in `cmd/crawler/main.go` to decide whether counts are due on a batch, cache `totalPubkeys` and `totalStale`, expose `countsSampled`, expose `countsCached`, and expose `countSampleAgeBatches` for logging/metrics. Batch 1 must sample counts; later batches sample on the configured interval; with default interval `1`, every batch samples as today. When a sample is due, both `CountPubkeys` and `CountStalePubkeys` must be called through `retryDgraph`; if either sampled count fails, preserve the current fatal-break behavior with clean shutdown distinction. When a sample is not due, reuse the last sampled cached counts and continue crawling.

    Make skipped count-sample semantics explicit in logs and JSON. On sampled batches, `counts_sampled=true`, `counts_cached=false`, `count_sample_age_batches=0`, `total_pubkeys` and `stale_remaining` are fresh sampled values, and `new_pubkeys` is derived from fresh `CountPubkeys` minus the previous fresh sampled total. On unsampled batches, `counts_sampled=false`, `counts_cached=true`, `count_sample_age_batches` is the number of completed batches since the last successful count sample, `total_pubkeys` and `stale_remaining` are last sampled cached values, and `new_pubkeys` must be JSON `null` because it is not freshly known. The `Batch complete` log must include selected, queried, hits, skipped attempts, marked attempted, stale remaining, total pubkeys, counts sampled/cached state, and count sample age. Compute `staleRemaining` from the cached or sampled `totalStale` with the same clamp-to-zero behavior, and ensure log text makes clear cached values are cached when `countsSampled` is false.

    Extend `runStats.recordBatch` and `buildRunRecord` to carry totals for selected, queried, hits, skipped attempts, marked attempted, count samples, and cached-count batches. Extend `logBatchMetrics` to emit JSON keys `frontier_batch_size`, `relay_filter_batch_size`, `count_sample_interval`, `counts_sampled`, `counts_cached`, `count_sample_age_batches`, `selected`, `queried`, `hits`, `skipped_attempts`, `marked_attempted`, `stale_remaining`, `total_pubkeys`, and `new_pubkeys`. Keep existing comparable keys such as `round_id`, `batch_ms`, `fetch_ms`, `overhead_ms`, and `pubkeys_per_sec`, with `pubkeys_per_sec` based on exact queried count. Extend `runRecord` with JSON keys `frontier_batch_size`, `relay_filter_batch_size`, `count_sample_interval`, `total_selected`, `total_queried`, `total_skipped_attempts`, `total_marked_attempted`, `count_samples`, and `count_cached_batches`.

    Add focused unit tests before implementation where practical: count-sampling helper samples first batch and every Nth configured interval; skipped intervals reuse cached count values and report `counts_sampled=false`, `counts_cached=true`, and positive `count_sample_age_batches`; sampled count failures still return through `retryDgraph` classification; run record includes new config values, exact totals, and count sample/cache totals; BATCH_METRICS JSON contains the new exact keys and values for both sampled and unsampled batches, with sampled JSON deriving `new_pubkeys` from fresh CountPubkeys and unsampled JSON encoding `new_pubkeys` as null.
  </action>
  <acceptance_criteria>
    - `cmd/crawler/main.go` selects Dgraph frontier rows with `cfg.FrontierBatchSize`, not `cfg.RelayFilterBatchSize`.
    - `crawler.Config.FilterBatchSize` still receives `cfg.RelayFilterBatchSize`.
    - With `count_sample_interval=1`, the count calls occur every batch.
    - With `count_sample_interval=3`, batch 1 samples, batches 2 and 3 can reuse cached counts, and the next due batch samples again according to the helper tests.
    - Sampled `CountPubkeys` and `CountStalePubkeys` failures still pass through `retryDgraph`, preserving transient retry behavior.
    - `FetchResult.Queried` is populated from validated authors sent to relay filters, and a unit test proves selected count can exceed queried count when selected data contains an invalid pubkey.
    - BATCH_METRICS and final run records include self-describing frontier, relay filter, and count sampling settings.
    - Unsampled BATCH_METRICS lines include `counts_sampled=false`, `counts_cached=true`, `count_sample_age_batches>0`, cached `total_pubkeys`/`stale_remaining`, and `new_pubkeys:null`.
    - Sampled BATCH_METRICS lines include `counts_sampled=true`, `counts_cached=false`, `count_sample_age_batches=0`, fresh `total_pubkeys`/`stale_remaining`, and numeric `new_pubkeys`.
    - Skipped attempts are excluded from `MarkAttempted` and counted in logs and metrics.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/crawler -run 'Test(CountSample|AttemptableBatchKeys|BuildRunRecord|LogBatchMetrics|Round3|ResolveRoundID)' -count=1</automated>
    <automated>go test ./pkg/crawler -run 'Test.*(FetchAndUpdateFollows.*Queried|Invalid.*Queried|Queried.*Invalid)' -count=1</automated>
    <automated>go test ./cmd/crawler -count=1</automated>
  </verify>
  <done>Main loop uses `FrontierBatchSize` for selection, keeps relay filter sizing separate, uses exact validated-author queried counts, samples count queries on schedule through `retryDgraph`, and emits logs, BATCH_METRICS, and run records whose sampled versus cached count semantics are explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove relay chunk safety and document operator measurement procedure</name>
  <files>pkg/crawler/crawler_filter_test.go, .planning/phases/13-main-loop-throughput-controls/13-01-PLAN.md</files>
  <read_first>
    - pkg/crawler/crawler.go: FetchAndUpdateFollows filter construction and queryRelay chunking loop.
    - pkg/crawler/crawler_filter_test.go: TestSplitAuthorsChunks and filter rejection tests.
    - .planning/REQUIREMENTS.md: MEASURE-01, MEASURE-02, MEASURE-03, LOOP-02, LOOP-04, and out-of-scope prohibitions.
  </read_first>
  <action>
    Extend relay chunking coverage per LOOP-02 and LOOP-04 without changing production relay chunking behavior. Add a test in `pkg/crawler/crawler_filter_test.go` that names the frontier/relay split explicitly, constructs at least 250 selected authors, sets a relay `filterCap` of 100, and proves chunks are exactly `[100,100,50]`. The test must make clear that `frontier_batch_size` is a Dgraph selection limit and never the relay chunk cap. If the existing `TestSplitAuthorsChunks` is reused, rename or expand it so its failure message mentions frontier-vs-relay cap separation.

    Keep the operator verification procedure in this PLAN.md, not a separate file, so the phase produces exactly one planning artifact. The procedure must tell the operator to run a baseline round with current defaults, then an optimized round with a larger `frontier_batch_size` and `count_sample_interval`, both labeled with `WOT_ROUND`, and compare `BATCH_METRICS` plus appended `~/deepfry/crawler-metrics.jsonl` records. It must also remind the operator not to edit StrFry and not to store event payloads outside StrFry.
  </action>
  <acceptance_criteria>
    - Relay chunking regression test demonstrates a selected frontier larger than relay cap is split into safe relay requests.
    - No production code increases `RelayFilterBatchSize` as a side effect of adding `FrontierBatchSize`.
    - PLAN.md contains operator baseline and optimized run steps with `WOT_ROUND` labels and JSONL comparison guidance.
    - The operator procedure keeps StrFry stock and respects Dgraph ID-only storage.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/crawler -run 'Test.*(SplitAuthorsChunks|Frontier.*Relay|Relay.*Chunk)' -count=1</automated>
    <automated>grep -n 'WOT_ROUND' .planning/phases/13-main-loop-throughput-controls/13-01-PLAN.md</automated>
    <automated>grep -n 'frontier_batch_size' .planning/phases/13-main-loop-throughput-controls/13-01-PLAN.md</automated>
  </verify>
  <done>Relay chunking safety is covered for selected frontiers larger than relay caps, and this plan contains a concrete WOT_ROUND baseline/optimized operator verification procedure.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Reduce avoidable per-batch Dgraph/bookkeeping overhead and safely increase useful work per loop | Tasks 1-3 |
| REQ | LOOP-01 independent Dgraph frontier batch size | Tasks 1-2 |
| REQ | LOOP-02 larger frontier with relay chunking | Tasks 2-3 |
| REQ | LOOP-03 selected/queried/hit/skipped/marked metrics | Task 2 |
| REQ | LOOP-04 no Phase 6 oversized-filter regression | Task 3 |
| REQ | COUNT-01 throttle count query frequency | Tasks 1-2 |
| REQ | COUNT-02 accurate logs/run records when skipped | Task 2 |
| REQ | COUNT-03 sampled count failures use retryDgraph | Task 2 |
| REQ | MEASURE-01 compare rounds with BATCH_METRICS and JSONL | Tasks 2-3 |
| REQ | MEASURE-02 run records include new settings | Task 2 |
| REQ | MEASURE-03 operator verification procedure | Task 3 and verification section |
| REQ | TEST-01 config tests without real home directory | Task 1 |
| REQ | TEST-02 loop accounting tests | Task 2 |
| CONTEXT | No Phase 13 CONTEXT.md exists | No locked decisions to implement or deferred ideas to exclude |
| RESEARCH | Research skipped by project config | Plan uses ROADMAP, REQUIREMENTS, STATE, codebase docs, and code graph evidence |

<verification>
Automated implementation verification:
- `go test ./pkg/config -count=1`
- `go test ./cmd/crawler -count=1`
- `go test ./pkg/crawler -run 'Test.*(SplitAuthorsChunks|Frontier.*Relay|Relay.*Chunk)' -count=1`
- `go test ./... -short -cover`

Operator verification procedure for MEASURE-03:
1. On the strfry host, preserve the stock StrFry deployment and do not change event storage. Dgraph remains ID-only graph storage.
2. Build or deploy the crawler binary for this commit.
3. Run a baseline round with current default controls and an explicit label, for example `WOT_ROUND=phase13-baseline ./bin/crawler`, long enough to produce multiple `BATCH_METRICS` lines and one appended `~/deepfry/crawler-metrics.jsonl` run record.
4. Update only `~/deepfry/web-of-trust.yaml` crawler controls, for example `frontier_batch_size: 500` and `count_sample_interval: 5`, while leaving `relay_filter_batch_size: 100` unless the operator is intentionally running a separate relay-filter experiment.
5. Run the optimized round with a distinct label, for example `WOT_ROUND=phase13-frontier500-count5 ./bin/crawler`.
6. Compare console `BATCH_METRICS` lines and the two JSONL records for `pubkeys_per_sec`, `new_pubkeys_per_sec`, `batch_ms`, `fetch_ms`, `overhead_ms`, `frontier_batch_size`, `relay_filter_batch_size`, `count_sample_interval`, `counts_sampled`, `counts_cached`, `count_sample_age_batches`, `total_pubkeys`, `stale_remaining`, `new_pubkeys`, `selected`, `queried`, `hits`, `skipped_attempts`, and `marked_attempted`.
7. Confirm no relay oversized-filter warnings or filter-rejection spikes appear when `frontier_batch_size` is larger than `relay_filter_batch_size`; if they do, stop and inspect whether relay chunking was bypassed.
</verification>

<success_criteria>
- All Phase 13 requirement IDs in frontmatter are implemented and covered by tests or the operator procedure: LOOP-01, LOOP-02, LOOP-03, LOOP-04, COUNT-01, COUNT-02, COUNT-03, MEASURE-01, MEASURE-02, MEASURE-03, TEST-01, TEST-02.
- Operators can increase Dgraph frontier selection independently from relay request chunk size.
- Count queries can be sampled less frequently without halting crawl progress or corrupting logs/run records.
- Metrics and run records are self-describing enough to compare baseline and optimized rounds.
- Relay request chunking remains governed by learned relay filter caps, with regression coverage for selected author sets larger than the cap.
- Unit tests do not touch real `~/deepfry/`.
</success_criteria>

<output>
Create `.planning/phases/13-main-loop-throughput-controls/13-01-SUMMARY.md` when done.
</output>
