---
milestone: v1.4
audited: 2026-06-16
status: passed
scores:
  requirements: 4/4
  phases: 1/1
  integration: N/A (single-phase, no cross-phase wiring surface)
  flows: N/A (no user-facing flow; internal crawler liveness)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []  # both items below RESOLVED before completion (see Tech Debt section)
tech_debt_resolved:
  - phase: 11-relay-query-liveness
    items:
      - "WR-01 (RESOLVED, commit 60ee269): replaced the completedThisBatch boolean with a per-batch generation token (Crawler.batchSeq + relayState.completedGen), eliminating the cross-batch marker race entirely — no reset to race."
      - "IN-02 (RESOLVED, commit 29ca3e7): added TestFetchAndUpdateFollows_ClosesStuckRelayOnQuorumExit covering the quorum-cancel close branch (conn closed + alive=false, no failTransport penalty)."
nyquist:
  overall: disabled (workflow.nyquist_validation=false)
---

# Milestone v1.4 — Crawler Hang Fix (Relay-Query Liveness) — Audit

**Audited:** 2026-06-16
**Status:** tech_debt (all requirements satisfied; 2 minor non-blocking deferred items)

## Definition of Done

A stuck or half-open relay can never wedge `FetchAndUpdateFollows` — the dispatcher always returns within a bounded multiple of its relay-query timeout, and stuck-relay goroutines do not leak unboundedly.

**Met.** Confirmed by Phase 11 verification (5/5 must-haves) and an adversarial code-review + fix loop (2 critical concurrency defects found and fixed, re-reviewed clean of blockers). Build, `go vet`, and the full unit suite are green; the three liveness tests pass under `-race -count=5`.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-----------------|---------------------|-----------------|-------|
| HANG-01 | passed | listed | Complete | **satisfied** |
| HANG-02 | passed | listed | Complete | **satisfied** |
| HANG-03 | passed | listed | Complete | **satisfied** |
| TEST-02 | passed | listed | Complete | **satisfied** |

4/4 satisfied. No unsatisfied, partial, or orphaned requirements. FAIL gate not triggered.

## Phases

| Phase | Status | Verification |
|-------|--------|--------------|
| 11 Relay-Query Liveness | Complete | passed (5/5) |

## Integration

Single-phase milestone with no cross-phase wiring surface. The change is internal to `pkg/crawler/crawler.go` (`FetchAndUpdateFollows` dispatcher + `queryRelay`), consumed by the unchanged `cmd/crawler` main loop (verified live in v1.2/v1.3). No new API routes, exports, or consumers. Integration check N/A.

## Tech Debt — RESOLVED before completion

Both items surfaced by the adversarial review's third iteration were closed before completing the milestone (user elected to address debt first):

1. **WR-01 — cross-batch marker race → RESOLVED (commit `60ee269`).** Replaced the `completedThisBatch` boolean with a monotonic per-batch generation token (`Crawler.batchSeq` + `relayState.completedGen`). A stale goroutine stamps its own older generation, which can never equal the current batch's generation, so it is always correctly treated as outstanding. The reset loop (the thing that raced) is gone entirely.
2. **IN-02 — quorum-path test coverage → RESOLVED (commit `29ca3e7`).** Added `TestFetchAndUpdateFollows_ClosesStuckRelayOnQuorumExit`: 4 relays, quorum 0.70, three fast EOSEs reach quorum while one blocks; asserts the quorum-cancel branch closes the outstanding relay's connection and marks it not-alive WITHOUT a failure penalty.

Verification after debt closure: `make build`, `go vet`, full unit suite, and all four liveness tests pass under `-race -count=3`. Crawler package coverage rose to 26.7%.

## Verdict

All requirements satisfied, no blockers, no broken flows, no remaining tech debt. The milestone's definition of done is met and the review-surfaced refinements are closed. Ready to complete and archive.
