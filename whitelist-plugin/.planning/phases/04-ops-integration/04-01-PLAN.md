---
phase: 04-ops-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/g/git/deepfry/whitelist-plugin/Makefile
  - /Users/g/git/deepfry/Dockerfile.strfry
  - /Users/g/git/deepfry/config/strfry/strfry.conf
  - /Users/g/git/deepfry/docker-compose.strfry.yml
autonomous: true
requirements: [OPS-01, OPS-02]
must_haves:
  truths:
    - "make build-bloom produces bin/bloom from ./cmd/bloom"
    - "make build-bloom-alpine and make build-bloom-linux each produce a static binary and exit 0"
    - "The Docker image bakes /app/plugins/bloom alongside /app/plugins/whitelist and /app/plugins/router"
    - "An operator can select the bloom plugin by changing one line in strfry.conf"
    - "The bloom persisted filter under /root/deepfry/ survives a container restart"
    - "The whitelist and router binaries, cmd/whitelist, cmd/router, and their selection paths are byte-identical"
  artifacts:
    - path: /Users/g/git/deepfry/whitelist-plugin/Makefile
      provides: "build-bloom, build-bloom-alpine, build-bloom-linux targets + APP_BLOOM var"
      contains: "build-bloom"
    - path: /Users/g/git/deepfry/Dockerfile.strfry
      provides: "third plugin-builder RUN building /bloom + COPY into /app/plugins/bloom"
      contains: "COPY --from=plugin-builder /bloom /app/plugins/bloom"
    - path: /Users/g/git/deepfry/config/strfry/strfry.conf
      provides: "writePolicy plugin selection documenting /app/plugins/bloom as a third option"
      contains: "/app/plugins/bloom"
    - path: /Users/g/git/deepfry/docker-compose.strfry.yml
      provides: "persistence for /root/deepfry/ on the strfry service so the bloom filter survives restarts"
      contains: "/root/deepfry"
  key_links:
    - from: /Users/g/git/deepfry/Dockerfile.strfry
      to: /Users/g/git/deepfry/whitelist-plugin/cmd/bloom
      via: "go build -o /bloom ./cmd/bloom in the plugin-builder stage"
      pattern: "-o /bloom ./cmd/bloom"
    - from: /Users/g/git/deepfry/config/strfry/strfry.conf
      to: /Users/g/git/deepfry/Dockerfile.strfry
      via: "writePolicy plugin path resolves to the baked /app/plugins/bloom binary"
      pattern: "/app/plugins/bloom"
---

<objective>
Make the completed `cmd/bloom` gate plugin buildable (OPS-01) and deployable (OPS-02) the same way `whitelist`/`router` already are: add bloom Makefile targets, bake the bloom binary into the strfry Docker image, document it as a third selectable writePolicy plugin in `strfry.conf`, and ensure the bloom persisted filter survives container restarts.

Purpose: An operator can build the bloom plugin natively and as a static Alpine/Linux binary, the Docker image ships all three plugins, and selecting bloom is a single-line config change — with on-disk filter persistence (GATE-05) holding in the Docker deployment, not just locally.
Output: Extended `Makefile` (APP_BLOOM + 3 targets + .PHONY/all/help), extended `Dockerfile.strfry` (build + COPY), updated `config/strfry/strfry.conf` (comment block + selectable line), and a persistence mount for `/root/deepfry/` in `docker-compose.strfry.yml`.

This phase adds NO runtime behavior. The plugin code from Phases 1–3 is complete and stays unchanged. Per D-04, `whitelist`/`router` binaries, `cmd/whitelist`, `cmd/router`, and their selection paths remain byte-identical.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ops-integration/04-CONTEXT.md
@.planning/phases/04-ops-integration/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bloom Makefile build targets (OPS-01, D-02)</name>
  <files>/Users/g/git/deepfry/whitelist-plugin/Makefile</files>
  <read_first>
    - /Users/g/git/deepfry/whitelist-plugin/Makefile (the file being modified — study the existing `router` targets: `APP_ROUTER=router` at line 3, `build-router` at lines 41-42, `build-router-alpine` at lines 81-87, `build-router-linux` at lines 89-96, the `.PHONY` line 28, the `all:` line 30, and the help block lines 131-148)
    - /Users/g/git/deepfry/whitelist-plugin/.planning/phases/04-ops-integration/04-PATTERNS.md (section "whitelist-plugin/Makefile — add bloom build targets" + "Shared Patterns" for LDFLAGS/static flags)
  </read_first>
  <action>
    Mirror the existing `router` build targets exactly for bloom (per D-02). Add an `APP_BLOOM=bloom` variable alongside `APP_ROUTER=router`. Add three targets built from `./cmd/$(APP_BLOOM)`:
    - `build-bloom` — native: `go build $(BUILD_FLAGS) -o bin/$(APP_BLOOM)$(BINARY_EXT) ./cmd/$(APP_BLOOM)`, same shape as `build-router`.
    - `build-bloom-alpine` — static musl: same `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 ... -a -installsuffix cgo -ldflags "$(LDFLAGS) -w -s -extldflags '-static'" -tags netgo` flags as `build-router-alpine`, output `bin/$(APP_BLOOM)-alpine`, with the trailing `@echo` confirmation line.
    - `build-bloom-linux` — generic static: same flags as `build-router-linux`, output `bin/$(APP_BLOOM)-linux`, with the trailing `@echo` line.
    Register `build-bloom build-bloom-alpine build-bloom-linux` in the `.PHONY` list. Add `build-bloom` to the `all:` target (so `all: build build-server build-router build-bloom`). Add three `@echo` help rows in the same column-aligned style as the `build-router*` help rows. Use `$(BUILD_FLAGS)` for the native target and inline `$(LDFLAGS)` for the static targets, identical to router. Do NOT modify any existing whitelist/router/server target.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/whitelist-plugin && grep -q 'APP_BLOOM=bloom' Makefile && grep -q 'build-bloom:' Makefile && grep -q 'build-bloom-alpine:' Makefile && grep -q 'build-bloom-linux:' Makefile && make build-bloom && test -f bin/bloom && make build-bloom-alpine && test -f bin/bloom-alpine && make build-bloom-linux && test -f bin/bloom-linux && grep -q 'all: build build-server build-router build-bloom' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `make build-bloom` exits 0 and produces `bin/bloom`.
    - `make build-bloom-alpine` exits 0 and produces `bin/bloom-alpine`.
    - `make build-bloom-linux` exits 0 and produces `bin/bloom-linux`.
    - `Makefile` contains `APP_BLOOM=bloom`, and `build-bloom`/`build-bloom-alpine`/`build-bloom-linux` appear in `.PHONY`.
    - `build-bloom` is present in the `all:` target.
    - `git diff Makefile` shows additions only for bloom — no whitelist/router/server target lines changed.
  </acceptance_criteria>
  <done>Native + static Alpine + static Linux bloom build targets exist, mirror the router targets exactly, build successfully, and are registered in .PHONY/all/help; existing targets untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Bake bloom into the Docker image and make it selectable in strfry.conf (OPS-02, D-03, D-04)</name>
  <files>/Users/g/git/deepfry/Dockerfile.strfry, /Users/g/git/deepfry/config/strfry/strfry.conf</files>
  <read_first>
    - /Users/g/git/deepfry/Dockerfile.strfry (the file being modified — the two existing `RUN ... go build ... -o /whitelist ./cmd/whitelist` / `-o /router ./cmd/router` blocks at lines 4-13, and the two `COPY --from=plugin-builder ... /app/plugins/...` lines 16-17)
    - /Users/g/git/deepfry/config/strfry/strfry.conf (the file being modified — the `writePolicy { ... }` block lines 96-119, comment options for whitelist/router, and the active `plugin = "/app/plugins/router"` line 117)
    - /Users/g/git/deepfry/whitelist-plugin/.planning/phases/04-ops-integration/04-PATTERNS.md (sections "Dockerfile.strfry — add bloom build step and COPY" and "config/strfry/strfry.conf — writePolicy plugin line", plus "Dockerfile static build flags")
  </read_first>
  <action>
    In `/Users/g/git/deepfry/Dockerfile.strfry` (D-03): add a third `RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -a -installsuffix cgo -ldflags "-w -s -extldflags '-static'" -tags netgo -o /bloom ./cmd/bloom` block immediately after the `/router` build block, mirroring it exactly. Do NOT add version LDFLAGS — the existing whitelist/router builds in this file omit them; stay consistent. Add a `COPY --from=plugin-builder /bloom /app/plugins/bloom` line immediately after the `/router` COPY. The existing `/whitelist` and `/router` RUN and COPY lines stay byte-identical.

    In `/Users/g/git/deepfry/config/strfry/strfry.conf` (D-04): bloom is opt-in. Extend the `writePolicy` comment block (lines 99-116) to describe a third option `/app/plugins/bloom` in the same prose style as the existing `/app/plugins/whitelist` and `/app/plugins/router` entries — describe it as making every accept/reject decision from a locally-held bloom filter with zero per-event HTTP, fetched periodically from the server `/bloom` endpoint and persisted to disk for resilience. Update the "Two plugin binaries ship" wording to reflect three. Per SC2 the selection is a single-line change to `plugin = "/app/plugins/bloom"`; keep the existing `plugin = "/app/plugins/router"` line as the active default (D-04: bloom stays opt-in, default selection unchanged) and document that switching to bloom is the one-line edit. Leave `config/strfry/strfry-quarantine.conf` unchanged (discretion: leave quarantine on its current plugin).
  </action>
  <verify>
    <automated>grep -q '\-o /bloom ./cmd/bloom' /Users/g/git/deepfry/Dockerfile.strfry && grep -qF 'COPY --from=plugin-builder /bloom /app/plugins/bloom' /Users/g/git/deepfry/Dockerfile.strfry && grep -qF '/app/plugins/bloom' /Users/g/git/deepfry/config/strfry/strfry.conf && grep -qF 'plugin = "/app/plugins/router"' /Users/g/git/deepfry/config/strfry/strfry.conf && grep -qF 'COPY --from=plugin-builder /whitelist /app/plugins/whitelist' /Users/g/git/deepfry/Dockerfile.strfry && grep -qF 'COPY --from=plugin-builder /router /app/plugins/router' /Users/g/git/deepfry/Dockerfile.strfry</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile.strfry` contains a `go build ... -o /bloom ./cmd/bloom` RUN block and `COPY --from=plugin-builder /bloom /app/plugins/bloom`.
    - The existing `/whitelist` and `/router` RUN+COPY lines are byte-identical (unchanged by `git diff`).
    - `config/strfry/strfry.conf` mentions `/app/plugins/bloom` in the writePolicy comment block, and the active `plugin = "/app/plugins/router"` line is preserved (bloom opt-in).
    - `config/strfry/strfry-quarantine.conf` is unchanged.
    - If Docker is available: `docker build -f /Users/g/git/deepfry/Dockerfile.strfry /Users/g/git/deepfry` succeeds and `/app/plugins/bloom` exists in the resulting image. (If Docker is unavailable in the execution environment, the grep checks above are authoritative for plan completion.)
  </acceptance_criteria>
  <done>The Docker image builds all three plugin binaries and bakes /app/plugins/bloom; strfry.conf documents bloom as a third selectable writePolicy plugin while keeping router as the active default; whitelist/router build+COPY+selection unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Persist the bloom filter directory across container restarts (OPS-02, D-05)</name>
  <files>/Users/g/git/deepfry/docker-compose.strfry.yml</files>
  <read_first>
    - /Users/g/git/deepfry/docker-compose.strfry.yml (the file being modified — the `strfry` service volume block lines 20-24 with the per-file `/root/deepfry/whitelist.yaml` and `/root/deepfry/router.yaml` bind-mounts and the `${STRFRY_DB_PATH:-./data/strfry-db}` host-path pattern at line 21; note the separate `strfry-quarantine` service lines 33-56 which must stay unchanged)
    - /Users/g/git/deepfry/whitelist-plugin/.planning/phases/04-ops-integration/04-PATTERNS.md (section "docker-compose.strfry.yml — volume mounts and filter persistence" — explains that the current `/root/deepfry/` mounts are per-file, so the bloom-written filter would not survive a restart; documents both the named-volume and host-path-bind options)
  </read_first>
  <action>
    The bloom plugin persists its fetched filter to `bloom_path` (default `/root/deepfry/bloom.dfbf`) so it can serve decisions when the server is unreachable (GATE-05/D-05). The current `strfry` service mounts only individual files into `/root/deepfry/` (whitelist.yaml, router.yaml), so a filter the plugin writes there is lost on container restart. Add a host-path bind-mount for the bloom filter directory to the `strfry` service volume block ONLY, mirroring the existing `${STRFRY_DB_PATH:-./data/strfry-db}` env-overridable host-path pattern: add `- ${BLOOM_DATA_PATH:-./data/bloom}:/root/deepfry/bloom-data` and document (via a comment in the compose file and the README task in plan 02) that an operator selecting bloom must set `bloom_path` to a file under `/root/deepfry/bloom-data/` (e.g. `bloom_path: /root/deepfry/bloom-data/bloom.dfbf`) so persistence survives restarts. Mounting a dedicated subdirectory (`/root/deepfry/bloom-data`) avoids colliding with the existing per-file `whitelist.yaml`/`router.yaml` bind-mounts under `/root/deepfry/`. Do NOT add this mount to the `strfry-quarantine` service (bloom is mainline-relay opt-in; quarantine stays unchanged). Keep the existing whitelist.yaml/router.yaml/strfry.conf/strfry-db mounts byte-identical.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry && grep -qF '/root/deepfry/bloom-data' docker-compose.strfry.yml && grep -qF 'BLOOM_DATA_PATH' docker-compose.strfry.yml && grep -qF './config/whitelist/whitelist.yaml:/root/deepfry/whitelist.yaml' docker-compose.strfry.yml && grep -qF './config/whitelist/router.yaml:/root/deepfry/router.yaml' docker-compose.strfry.yml && if command -v docker >/dev/null 2>&1; then docker compose -f docker-compose.strfry.yml config >/dev/null; else echo "docker absent — grep checks authoritative"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.strfry.yml` adds a `/root/deepfry/bloom-data` mount (env-overridable via `BLOOM_DATA_PATH`, default `./data/bloom`) to the `strfry` service.
    - The existing `whitelist.yaml`, `router.yaml`, `strfry.conf`, and `strfry-db` mounts on the `strfry` service are byte-identical (unchanged by `git diff`).
    - The `strfry-quarantine` service is unchanged.
    - If Docker is available, `docker compose -f docker-compose.strfry.yml config` validates the file (exit 0). If Docker is absent, the grep checks are authoritative.
  </acceptance_criteria>
  <done>The strfry service mounts a writable host-path directory at /root/deepfry/bloom-data so the bloom plugin's persisted filter survives container restarts (GATE-05 in Docker); quarantine and all existing mounts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → build/deploy config | Operator-authored Makefile / Dockerfile / compose / strfry.conf changes; no untrusted runtime input crosses any boundary in this phase |

## STRIDE Threat Register

This phase adds NO runtime code path, no new network listener, and no new package-manager installs (the bloom binary is built from existing first-party `./cmd/bloom` Go source already in the repo, mirroring the existing whitelist/router build). No new trust boundary is introduced.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | strfry.conf writePolicy selection | accept | Config is operator-controlled and version-controlled; bloom stays opt-in (router remains active default per D-04). No runtime change. |
| T-04-02 | Tampering | docker-compose `/root/deepfry/bloom-data` bind-mount | mitigate | Mount a dedicated subdirectory (`/root/deepfry/bloom-data`) on the `strfry` service only, not over the existing per-file config mounts; quarantine service untouched, preventing cross-relay data leak. |
| T-04-SC | Tampering | go build of ./cmd/bloom | accept | No new external dependencies pulled; binary built from first-party source already audited in Phases 1–3. No npm/pip/cargo install in this phase. |
</threat_model>

<verification>
- `make build-bloom && make build-bloom-alpine && make build-bloom-linux` all exit 0 and produce `bin/bloom`, `bin/bloom-alpine`, `bin/bloom-linux`.
- `Dockerfile.strfry` builds `/bloom` and copies it to `/app/plugins/bloom`; `/whitelist` and `/router` lines unchanged.
- `strfry.conf` documents `/app/plugins/bloom` as a third option; `plugin = "/app/plugins/router"` remains the active default.
- `docker-compose.strfry.yml` adds the `/root/deepfry/bloom-data` persistence mount on the `strfry` service only; `strfry-quarantine` unchanged.
- `git diff` confirms no whitelist/router/server target, RUN, COPY, or selection line was modified (byte-identical, D-04).
</verification>

<success_criteria>
- OPS-01: bloom plugin builds natively and as a static Alpine/Linux binary, matching the existing plugins' build conventions.
- OPS-02: the Docker image bakes the bloom binary alongside whitelist/router, strfry.conf can select it with a single-line change, and the persisted filter survives container restarts.
- The whitelist/router binaries and their selection paths remain byte-identical.
</success_criteria>

<output>
Create `.planning/phases/04-ops-integration/04-01-SUMMARY.md` when done.
</output>