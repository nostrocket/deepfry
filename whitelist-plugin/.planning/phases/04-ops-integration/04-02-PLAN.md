---
phase: 04-ops-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/g/git/deepfry/whitelist-plugin/README.md
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "The README documents the bloom gate plugin: config keys, accept/reject behavior, periodic fetch, and persistence/resilience"
    - "The README documents the server's GET /bloom endpoint with conditional GET / ETag"
    - "The README documents how to build the bloom plugin (make build-bloom / build-bloom-alpine)"
    - "The README documents the bloom filter persistence requirement under /root/deepfry/ in Docker"
  artifacts:
    - path: /Users/g/git/deepfry/whitelist-plugin/README.md
      provides: "Bloom Gate Plugin section + /bloom HTTP API row + bloom build/file-structure/docker-config entries"
      contains: "Bloom Gate Plugin"
  key_links:
    - from: /Users/g/git/deepfry/whitelist-plugin/README.md
      to: /Users/g/git/deepfry/whitelist-plugin/pkg/config/config.go
      via: "documented bloom_-prefixed config keys match BloomConfig mapstructure tags"
      pattern: "bloom_refresh_interval"
    - from: /Users/g/git/deepfry/whitelist-plugin/README.md
      to: /Users/g/git/deepfry/whitelist-plugin/pkg/server/server.go
      via: "documented GET /bloom endpoint matches the server's bloom handler"
      pattern: "/bloom"
---

<objective>
Document the bloom gate plugin and the server's `/bloom` endpoint in `whitelist-plugin/README.md` (OPS-03, D-06), consistent in structure with the existing "Router Plugin (optional)" section and HTTP API table.

Purpose: An operator reading the README understands how the bloom plugin makes accept/reject decisions from a local filter with zero per-event HTTP, how it stays fresh and survives server outages, how to configure it via the shared `whitelist.yaml`, how to build it, and how the `GET /bloom` conditional-GET endpoint works.
Output: New "Bloom Gate Plugin (optional)" README section, a `/bloom` row in the HTTP API table, bloom build-command + file-structure + Docker-config entries, mirroring the established documentation patterns.

This is a docs-only plan. It touches no code and no deploy file (those are plan 04-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ops-integration/04-CONTEXT.md
@.planning/phases/04-ops-integration/04-PATTERNS.md
@pkg/config/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the bloom plugin and GET /bloom endpoint in README (OPS-03, D-06)</name>
  <files>/Users/g/git/deepfry/whitelist-plugin/README.md</files>
  <read_first>
    - /Users/g/git/deepfry/whitelist-plugin/README.md (the file being modified — study these anchor sections: the HTTP API table lines 90-96; the "Router Plugin (optional)" section lines 214-261 (heading, opt-in description, "How It Works" numbered flow, "Configuration" with yaml block + field table); the "Docker Deployment" + "Config Files in Docker" table lines 275-298; the "File Structure" tree lines 300-341; the "Build Commands" block lines 347-361)
    - /Users/g/git/deepfry/whitelist-plugin/pkg/config/config.go (lines 93-127 — the authoritative `BloomConfig` struct + `LoadBloomConfig` defaults: `server_url` default `http://localhost:8081`, `bloom_refresh_interval` `6h`, `bloom_path` `~/deepfry/bloom.dfbf`, `bloom_fetch_timeout` `30s`, `refresh_retry_count` `3`. Document these exact keys/defaults.)
    - /Users/g/git/deepfry/whitelist-plugin/.planning/phases/04-ops-integration/04-PATTERNS.md (section "whitelist-plugin/README.md — bloom plugin section" — gives the exact patterns and line ranges to mirror for every README edit)
    - /Users/g/git/deepfry/whitelist-plugin/.planning/phases/04-ops-integration/04-01-PLAN.md (Task 3 — the `/root/deepfry/bloom-data` persistence mount + `BLOOM_DATA_PATH` env var + `bloom_path: /root/deepfry/bloom-data/bloom.dfbf` recommendation that the Docker-config docs must reflect)
  </read_first>
  <action>
    Extend `README.md` consistent with existing docs (D-06). Make these additions:

    1. HTTP API table (after line 96): add a row `| `/bloom` | GET | Fetch the current serialized bloom filter; supports conditional GET via `If-None-Match` / ETag | `200` binary filter body or `304 Not Modified` |`. Note that membership reflects the whitelist as of the last server refresh (SRV-01/02/03).

    2. New section `## Bloom Gate Plugin (optional)` placed immediately after the Router Plugin section (after line 261), mirroring the Router section's structure:
       - Opening paragraph: a drop-in alternative writePolicy plugin that makes every accept/reject decision from a locally-held bloom filter with ZERO per-event HTTP. Not-in-set → reject; maybe-in-set → accept (a deliberately bounded ~1e-6 false-positive accept rate is tolerated for zero per-event network cost). Opt-in: activate by changing the `strfry.conf` writePolicy line to `plugin = "/app/plugins/bloom"` and restarting StrFry; whitelist/router are unaffected.
       - `### How It Works` numbered flow: (1) at startup fetches the filter from the server `GET /bloom` (conditional GET); (2) refreshes on the configured interval (~6h) and atomically swaps in each new filter without dropping events; (3) each successfully fetched filter is persisted to `bloom_path` under `~/deepfry/`; (4) per-event decisions use only the local filter — no per-event HTTP; (5) when the server is unreachable, decisions are served from the last persisted on-disk filter (GATE-05); (6) cold start blocks only when there is neither a reachable server nor a persisted filter on disk (GATE-06).
       - `### Configuration` sub-section: state that bloom reads `bloom_`-prefixed keys from the shared `~/deepfry/whitelist.yaml` (GATE-07/D-05). Include a yaml example and a field table documenting exactly the BloomConfig keys + defaults from `pkg/config/config.go`: `server_url` (`http://localhost:8081`), `bloom_refresh_interval` (`6h`), `bloom_path` (`~/deepfry/bloom.dfbf`), `bloom_fetch_timeout` (`30s`), `refresh_retry_count` (`3`). Do not invent keys not present in BloomConfig.

    3. "Config Files in Docker" table (around line 298): add a row noting the bloom persisted-filter directory mount — `./data/bloom` (overridable via `BLOOM_DATA_PATH`) mounted to `/root/deepfry/bloom-data` in strfry, used by the bloom plugin — and note that when selecting bloom, set `bloom_path: /root/deepfry/bloom-data/bloom.dfbf` so the filter survives container restarts (GATE-05). This must match plan 04-01 Task 3.

    4. "File Structure" tree (around lines 304-338): add `cmd/bloom/main.go` (bloom gate plugin entry point) under `cmd/`, and `pkg/bloomgate/` (checker.go + fetcher.go) and `pkg/bloom/` (shared bloom library) under `pkg/`, mirroring the existing entries' comment style.

    5. "Build Commands" block (around lines 347-361): add `make build-bloom` and `make build-bloom-alpine` rows mirroring the `build-router` rows; update the "Build all three binaries" comment if it implies a fixed count.

    Do not duplicate the Router section's quarantine-specific content. Keep all existing README content intact — additive edits only.
  </action>
  <verify>
    <automated>cd /Users/g/git/deepfry/whitelist-plugin && grep -qF 'Bloom Gate Plugin' README.md && grep -qF '`/bloom`' README.md && grep -qF 'bloom_refresh_interval' README.md && grep -qF 'bloom_path' README.md && grep -qF 'bloom_fetch_timeout' README.md && grep -qF 'make build-bloom' README.md && grep -qF 'cmd/bloom' README.md && grep -qF 'bloom-data' README.md && grep -qiE 'condition|ETag|If-None-Match' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains a `## Bloom Gate Plugin (optional)` section with a "How It Works" numbered flow covering local-filter decisions, periodic conditional-GET fetch, disk persistence, and server-unreachable resilience.
    - README contains a `/bloom` HTTP API table row mentioning conditional GET / ETag / 304.
    - README documents the bloom config keys `server_url`, `bloom_refresh_interval`, `bloom_path`, `bloom_fetch_timeout`, `refresh_retry_count` with the defaults from `pkg/config/config.go` and no invented keys.
    - README documents the `/root/deepfry/bloom-data` persistence mount and the `bloom_path` recommendation, consistent with plan 04-01 Task 3.
    - README "Build Commands" lists `make build-bloom` and `make build-bloom-alpine`; "File Structure" lists `cmd/bloom` and `pkg/bloomgate`.
    - All pre-existing README sections remain present (additive edits only — `git diff` shows no deletions of existing rows/sections).
  </acceptance_criteria>
  <done>README documents the bloom gate plugin (config, accept/reject behavior, periodic fetch, persistence/resilience), the GET /bloom conditional-GET endpoint, the Docker persistence mount, and the bloom build commands — consistent with the existing plugin/endpoint docs; no existing content removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → documentation | Documentation-only change; no runtime input, no trust boundary crossed |

## STRIDE Threat Register

This plan modifies only `README.md`. No code, no network listener, no package install, no new trust boundary.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-I | Information Disclosure | README documentation | accept | Docs describe only config key names/defaults already public in source; no secrets or credentials documented. |
</threat_model>

<verification>
- README has a `## Bloom Gate Plugin (optional)` section with config table, "How It Works" flow, and resilience description.
- README HTTP API table has a `/bloom` conditional-GET row.
- Documented bloom config keys match `pkg/config/config.go` `BloomConfig` exactly.
- Docker persistence mount + `bloom_path` recommendation match plan 04-01 Task 3.
- No existing README content removed.
</verification>

<success_criteria>
- OPS-03: the README documents the bloom plugin (config, behavior, resilience) and the server's `/bloom` endpoint, consistent with the existing plugin/endpoint docs.
</success_criteria>

<output>
Create `.planning/phases/04-ops-integration/04-02-SUMMARY.md` when done.
</output>
